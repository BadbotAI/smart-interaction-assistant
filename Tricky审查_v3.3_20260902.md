# Tricky 审查 · v3.3（2026-09-02）

- 方法：机器坏人程序 skills/23「Tricky 审查」八关流程首跑
- 范围：v2.8 之后的全部新面积（页面式编辑器、数据回流链路、聚合、API Key、植入 SDK、静态演示站）+ 旧清单 P1 复查
- 结论：两批共整改 **1 个 P0 + 5 个 P1**（第一批：insights test 污染/导入上限/场景可删；第二批：有效期分钟级失效/同步导入上限/任务异常锁死）；旧 P1 三项确认已修；另记 6 项 P2/边界声明

## 上轮遗留复查（v1.0 清单，基于 v2.8）

| 旧编号 | 问题 | 现状 |
|---|---|---|
| L3 | 线上服务中的配置可被 API 直接删除 | **已修**：delete 拦截条件 = 有生效快照（published 或 draft v≥1），非仅 status==published |
| L2 | 改名不查重可撞名 | **已修**：update_card 同样做租户内同名校验 |
| L5 | card_submitted 无幂等可刷看板 | **已修**：按 (user, render_id) 去重 |

## 本轮新问题与整改

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| D1 | 数据一致性 | P1 | insights 主查询（漏斗/触发分布/分组件转化）未过滤 channel=test：在测试抽屉里点几下，业务转化看板数字就被污染；而 questions/回显早已排除 test，同一页面两半口径不一致 | dashboard.insights 查询加 `COALESCE(channel,'')!='test'`，与 questions/回显拉齐 | API 实测 ✓ |
| S1 | 恶意用户 | P1 | /v1/bank/import/start 无条数上限：一次提交 10 万条可锁死全局导入任务约 1 小时（单任务互斥，期间全租户不能导入） | 上限 5000 条/次，超出 422 提示分批 | 5001 条被拒 ✓ |
| U1 | 新手首日 | P1 | 自定义场景只能加不能删：手滑建错名字的场景永远留在场景表里 | 新增 /api/scenes/delete（仅自定义可删；场景下有题目时 409 拦截防数据悬空）+ 场景表行悬停出删除 icon | 建→删 ✓、内置场景 422 ✓ |

## 第二批（全量轮补查，2026-09-02）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L6 | 挑剔 PM | **P0** | 有效期承诺"精确到分钟"但服务端用日期字符串与 datetime 混比：设当天 10:00 上线的配置全天不触发；设 18:00 下线的到期日全天仍触发——承诺与实现相悖且产生错误触发 | _in_validity 归一比较（纯日期补 00:00/23:59，与当前分钟时刻比较）；前端 chip 判定同口径 | 六用例行为矩阵全 PASS ✓ |
| S5 | 恶意用户 | P1 | 同步版 /v1/bank/import 无条数上限（上轮只封了异步版），裸调可提交超大 payload 阻塞请求 | 同步版同样限 5000 条/次 | py_compile ✓ |
| O2 | 运维 | P1 | 异步导入任务中途异常（如 DB 错误）status 永远停在 running，全局导入任务从此 409 锁死，只能重启服务 | _run_import 包异常兜底：异常落 done + error 字段并审计 | 代码路径核查 ✓ |

通过项补充：模型启用开关服务端有覆盖率校验（未完成入池评测不可上线）；名称 15 字/触发 200 字服务端双端一致；staged relabel→commit 数据链正确；聚合成本占比在筛选态下口径自洽。

## 记录在案（P2 / 边界声明）

| 编号 | 关卡 | 说明 |
|---|---|---|
| S2 | 恶意用户 | UI.el 的 html: 属性走 innerHTML——当前全站无调用点传用户数据（已核实 0 处），保留为红线：新代码禁止对 html: 传入任何用户输入 |
| S3 | 恶意用户 | /v1/embed/envelope 的 ?key= 参数未校验（演示边界）；正式化时按《植入技术方案调研》做域名白名单 + 签名 |
| S4 | 恶意用户 | 多租户接口 tenant_id 由请求方声明（演示单租户）；正式化需鉴权层注入租户而非信任入参 |
| D2 | 数据一致性 | channel=embed（植入流量）计入业务看板——判定为正确口径（真实用户流量），已在此声明 |
| O1 | 运维 | 异步任务（导入/评测/画像）状态在内存，服务重启即丢；前端轮询遇 idle 自动停，无卡死，但进行中任务会静默中断——正式化需任务持久化 |
| R1 | 上线检查 | shortId 数字展示 ID 为 hash%1e6，理论可撞（仅展示用途，复制的是完整 UUID，无数据风险） |

## 通过项（抽查记录）

- 状态机：配置生命周期无图外转移；上线锁编辑双端一致；并发编辑有 lock_version 冲突检测
- 注入面：搜索 LIKE 已转义；CSV 导出有 BOM + 公式注入防护；无 innerHTML 用户数据注入点
- 渠道隔离：traces/标签池/回显/questions/insights（本轮修复后）全链路排除 test
- 导航健壮：hash 路由有 hashchange 监听，浏览器回退正常；深链全覆盖
- 轮询卫生：模型评测/画像/评审轮询均判断 activeTab，离开页面即停；评测轮询已局部化

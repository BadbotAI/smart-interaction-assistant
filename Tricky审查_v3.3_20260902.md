# Tricky 审查 · v3.3（2026-09-02）

- 方法：机器坏人程序 skills/23「Tricky 审查」八关流程首跑
- 范围：v2.8 之后的全部新面积（页面式编辑器、数据回流链路、聚合、API Key、植入 SDK、静态演示站）+ 旧清单 P1 复查
- 结论：两批共整改 **1 个 P0 + 5 个 P1**（第一批：insights test 污染/导入上限/场景可删；第二批：有效期分钟级失效/同步导入上限/任务异常锁死）；旧 P1 三项确认已修；另记 6 项 P2/边界声明

## 上轮遗留复查（v1.0 清单，基于 v2.8）

| 旧编号 | 问题 | 现状 |
|---|---|---|
| L3 | 线上服务中的配置可被 API 直接删除 | **已修**：delete 拦截条件 = 有生效快照（published 或 draft v≥1），非仅 status==published |
| L2 | 改名不查重可撞名 | **已修**：update_card 同样做租户内同名校验 |
| L5 | card_submitted 无幂等可刷看板 | **已修**：按 (user, render_id) 去重 |

## 本轮新问题与整改

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| D1 | 数据一致性 | P1 | insights 主查询（漏斗/触发分布/分组件转化）未过滤 channel=test：在测试抽屉里点几下，业务转化看板数字就被污染；而 questions/回显早已排除 test，同一页面两半口径不一致 | dashboard.insights 查询加 `COALESCE(channel,'')!='test'`，与 questions/回显拉齐 | API 实测 ✓ |
| S1 | 恶意用户 | P1 | /v1/bank/import/start 无条数上限：一次提交 10 万条可锁死全局导入任务约 1 小时（单任务互斥，期间全租户不能导入） | 上限 5000 条/次，超出 422 提示分批 | 5001 条被拒 ✓ |
| U1 | 新手首日 | P1 | 自定义场景只能加不能删：手滑建错名字的场景永远留在场景表里 | 新增 /api/scenes/delete（仅自定义可删；场景下有题目时 409 拦截防数据悬空）+ 场景表行悬停出删除 icon | 建→删 ✓、内置场景 422 ✓ |

## 第二批（全量轮补查，2026-09-02）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L6 | 挑剔 PM | **P0** | 有效期承诺"精确到分钟"但服务端用日期字符串与 datetime 混比：设当天 10:00 上线的配置全天不触发；设 18:00 下线的到期日全天仍触发——承诺与实现相悖且产生错误触发 | _in_validity 归一比较（纯日期补 00:00/23:59，与当前分钟时刻比较）；前端 chip 判定同口径 | 六用例行为矩阵全 PASS ✓ |
| S5 | 恶意用户 | P1 | 同步版 /v1/bank/import 无条数上限（上轮只封了异步版），裸调可提交超大 payload 阻塞请求 | 同步版同样限 5000 条/次 | py_compile ✓ |
| O2 | 运维 | P1 | 异步导入任务中途异常（如 DB 错误）status 永远停在 running，全局导入任务从此 409 锁死，只能重启服务 | _run_import 包异常兜底：异常落 done + error 字段并审计 | 代码路径核查 ✓ |

通过项补充：模型启用开关服务端有覆盖率校验（未完成入池评测不可上线）；名称 15 字/触发 200 字服务端双端一致；staged relabel→commit 数据链正确；聚合成本占比在筛选态下口径自洽。

## 记录在案（P2 / 边界声明）

| 编号 | 关卡 | 说明 |
|---|---|---|
| S2 | 恶意用户 | UI.el 的 html: 属性走 innerHTML——当前全站无调用点传用户数据（已核实 0 处），保留为红线：新代码禁止对 html: 传入任何用户输入 |
| S3 | 恶意用户 | /v1/embed/envelope 的 ?key= 参数未校验（演示边界）；正式化时按《植入技术方案调研》做域名白名单 + 签名 |
| S4 | 恶意用户 | 多租户接口 tenant_id 由请求方声明（演示单租户）；正式化需鉴权层注入租户而非信任入参 |
| D2 | 数据一致性 | channel=embed（植入流量）计入业务看板——判定为正确口径（真实用户流量），已在此声明 |
| O1 | 运维 | 异步任务（导入/评测/画像）状态在内存，服务重启即丢；前端轮询遇 idle 自动停，无卡死，但进行中任务会静默中断——正式化需任务持久化 |
| R1 | 上线检查 | shortId 数字展示 ID 为 hash%1e6，理论可撞（仅展示用途，复制的是完整 UUID，无数据风险） |

## 通过项（抽查记录）

- 状态机：配置生命周期无图外转移；上线锁编辑双端一致；并发编辑有 lock_version 冲突检测
- 注入面：搜索 LIKE 已转义；CSV 导出有 BOM + 公式注入防护；无 innerHTML 用户数据注入点
- 渠道隔离：traces/标签池/回显/questions/insights（本轮修复后）全链路排除 test
- 导航健壮：hash 路由有 hashchange 监听，浏览器回退正常；深链全覆盖
- 轮询卫生：模型评测/画像/评审轮询均判断 activeTab，离开页面即停；评测轮询已局部化

## 第三批（2026-09-03 大批次改版后增量审查）

范围：策略默认档重构（全局均衡 / 默认兜底）、参数配置抽屉字段重构（α / 画像权重 / 候选标签选择）、场景数据集详情页（题目级增删改标 / 导入 / 替换）、看板重排、tab 样式统一、tooltips 精简。

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L7 | 挑剔 PM | P1 | 参数配置抽屉按产品决策移除了「成本值」入口，但种子策略「全局均衡」仍带 ¥5/天预算帽：当天花满后静默降级为最便宜直连，界面上却无处可见可改——行为与界面承诺不一致 | 清空该策略预算帽（DB + seed 同步），预算降级机制保留在服务端供 API 使用 | DB 校验 ✓ |
| S6 | 恶意用户 | **P0** | 替换导入先清库再校验：整批数据全部无效（如传错文件）时场景数据被清空且什么都没导进去，纯数据丢失 | /v1/bank/import/start 在 replace 分支先校验至少一条有效题目，无效则 422 且不清库 | 无效替换被拒、原 25 题保留 ✓ |
| S7 | 恶意用户 | P1 | 替换标记存在页面闭包里：确认替换后在同一导入页连续导入两次，第二次会再次清掉第一次刚导入的数据 | replace 一次性消费：首次开始导入后即清除标记与提示 chip | 代码路径核查 ✓ |

记录在案（P2 / 边界声明，不改）：
- 「默认兜底」策略当前实现为 K=1 快速直连（非严格锁定兜底模型），移除 Judge 后与「全局均衡」同为可用档——语义与实现的精确对齐留待正式化
- 删除题目 / 更改场景标签作用于共享冷启动集（演示单租户无隔离需求）；场景删除会连带删除冷启动题目，属产品决策
- 场景删除 / 替换后「各场景路由去向」表在下次「重新生成」前可能残留旧场景行

通过项（抽查）：策略 CRUD 全链（新参数 alpha/profile_w 存取 / 默认档删除拦截）✓；场景详情增删改标 / 替换 / 级联删除 / 通用场景保护 ✓；导入页替换提示与一次性语义 ✓；看板空态文案与数据可得性逐卡核查 ✓；tab 激活态类名修复（此前 .on 类无样式，属实际 bug）✓

## 第四批（2026-09-04 平台拆分 v4.0 增量审查）

范围：双平台拆分（智能交互 / 模型路由两个独立站点）、产品类（products CRUD + MCP 接入）、策略即 API 融合、模型画像与决策模型搬家、双站构建管线。

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| D3 | 数据一致性 | P1 | 产品创建/更新接受不存在的组件 ID：绑定数悬空、详情页静默丢行 | 服务端按 cards 表过滤无效 ID 后落库 | 传入 not-exist 被过滤 ✓ |
| U2 | 新手首日 | P1 | 智能交互站上深链 playground#model 时导航按 hash 推断成路由平台，侧栏链接指向本站已删除的 router.html（404） | 双站构建各自注入 window.SIA_PLATFORM，导航严格按站点归属渲染 | 构建产物核查 ✓ |

记录在案（P2 / 边界声明）：
- 组件在工作台被删除后，产品的绑定引用仍保留（详情页按现存组件过滤展示）；正式化需删除时反查产品引用
- 路由站 dashboard.html 直接无 hash 打开会落到交互数据 section（导航不可达路径，仅深链可触发）
- MCP 接入为演示态：mcp_key 由 product_id 派生、/v1/mcp 未实现真实协议端点
- sk-live 独立密钥体系 UI 已退役，服务端端点保留（无入口）

通过项（抽查）：产品名重名 409 / 超长 422 ✓；策略创建带 allow_aggregation=false 存取 ✓；两默认策略删除拦截、改名后引用不断 ✓；双站 mock 快照含 /api/products、静态站产品页可渲染 ✓；决策模型移除→非兜底策略灰置链路 ✓

## 第五批（2026-09-04 晚，产品/风格/画像状态卡增量审查）

范围：策略画像状态卡与生成状态接口、产品管理改版与工作台产品封面、组件风格编辑器（选项/代码双模式）、首页轮播重构、双站构建。

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| S8 | 恶意用户 | P1 | /api/brands/delete 的 file 参数未做文件名归一校验（仅前缀判断），构造路径分隔符有越界删除面 | basename 归一 + 前后缀白名单校验前置，非法名 422 | 构造名被拒 ✓ |
| D4 | 数据一致性 | P1 | 删除风格后引用它的产品 brand_file 悬空：工作台封面风格下拉显示裸文件名，产品渲染回退行为不可见 | 删除时服务端把引用产品重置为默认风格并计入审计（products_reset） | 建风格→绑产品→删风格→产品回落默认 ✓ |
| R2 | 上线检查 | **P0(演示)** | 静态站 mock 层缺 /api/profile/gen-status：Pages 上场景数据集页「模型画像」整块空渲染（即用户报告的"看不到策略卡片"），点生成也无状态推进 | mock 补 gen-status（快照合并本地生成记录）+ rebuild 有状态返回；前端对缺字段响应做防御（generated/task 兜底），接口异常也渲染「未生成」卡 | 静态构建核查 + 本地三态截图 ✓ |

记录在案（P2）：画像"已生成"不感知题库后续变化（新鲜度）；products PUT 的 brand_file 不校验文件存在；rebuild 的 policy_id 不校验策略存在；「品牌风格 / 组件风格」称呼待统一。

通过项（抽查）：/api/brands POST 校验完备（brand_id 正则 + default 保护 + 颜色格式白名单，路径注入防住）✓；产品封面风格切换 PUT 部分字段不清空其余字段 ✓；风格编辑器 _options 往返 ✓；画像轮询 activeTab 守卫与全局任务互斥 ✓；复制策略命名 _副本 ✓。

## 第六批（2026-09-04 深夜，5 轮 strict review）

按用户指令连续五轮不同侧重的严格审查，全部当场整改回归：

| 轮 | 侧重 | 覆盖 | 结果 |
|---|---|---|---|
| 1 | 全页运行时扫雷 | 19 页/深链 DOM 断言（关键元素、运行时报错、超时） | 全过（首轮"加载失败"命中系脚本源码字面量误报，修正口径复核为零） |
| 2 | 接口契约与数据闭环 | 21 项 API 断言：策略/产品/风格/场景题库/画像状态/事件 全 CRUD 与错误码 | 21/21 PASS |
| 3 | 关键用户旅程 | 工作台封面→测试触发闭环→场景数据集画像三态→风格编辑器联动 逐帧截图 | 全过 |
| 4 | 静态站对等性 | 双站 13 页断言（含画像已生成态、组件触发流入快照） | 13/13 PASS |
| 5 | 代码卫生 | console.log/TODO/emoji/死引用/死常量/过期注释扫描 | 清理 PARAM_DESCS 死常量与两处过期注释，其余为零 |

过程事故与教训：第 2 轮验证「通用场景可删除」时把演示库通用题库（83 题）级联删除——种子中本无 general 域无法原样还原，已重建 20 道已打分通用题并重算画像恢复演示完整性。**教训入规：破坏性接口验证一律用临时场景/临时对象，不碰演示主数据。**

## 第七批（2026-09-05 晚，strict review：风格编辑器/产品域/首页动画新面积）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L8 | 挑剔 PM | P1 | 风格编辑器拆成独立卡后误入 style-split 网格第二格：编辑时组件预览被挤出右列、布局错乱 | 左列包容器（风格库卡+编辑器卡同列堆叠），预览稳居右列 | 截图 ✓ |
| L9 | 挑剔 PM | P1 | 深链打开编辑器时生效风格晚到覆盖预览（Brand.init 竞态），深色/配色预览失真 | 页面顶部按 URL 预设 _editingStyle 标记 + tokens.js init 尊重该标记不覆盖 | dump 无生效风格注入、截图预览色正确 ✓ |
| D5 | 数据一致性 | P1 | 删除策略后其「路由效果」生成记录留在 kv（孤儿数据，静态快照会带出幽灵状态） | delete_policy 同步剔除 policy_profile_gen 记录；存量清理 | API 实测 + kv 校验 ✓ |
| C3 | 文案 | P2 | 「产品接入」旧页面名残留两处（未绑定封面提示 / 规则块第5点） | 分别改「产品及风格管理」「产品对接」 | grep 清零 ✓ |

轮次结果：R1 全页扫雷 19/19 ✓（含新版编辑器/产品卡/首页动画断言）；R2 接口冒烟 16/16 ✓（全程临时对象，含新 _options schema 往返、无内置兜底策略、judge 移除恢复闭环）；R3 新面积截图走查 ✓（竞态修复实证：预览随配色方案变色）；R4 双站断言 9/9 ✓；R5 卫生扫描（console/emoji/死代码零，文案残留已清）。

## 第八批（2026-09-05，兜底必选与引用完整性审查）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L10 | 挑剔 PM | **P0** | 策略「兜底模型」配置从未被路由引擎使用：故障切换只认平台默认模型，用户配的兜底是摆设（承诺与实现相悖） | run_route 解析 params.fallback_model：明确指定时作为该策略的故障切换目标（独立于候选白名单，从全量在线模型解析，失效退回平台默认） | 白名单外兜底 swift-4b 在降级场景真实接管 ✓ |
| D6 | 数据一致性 | P1 | 删除模型不清理策略引用：候选白名单残留已删模型、兜底指向已删模型（悬空引用） | delete_model 遍历策略：白名单剔除、兜底重置为平台默认，审计记录清理数 | 建引用→删模型→白名单剔除+兜底重置 ✓ |
| C4 | 文案 | P2 | 决策模型缺失文案宣称"线上请求降级为兜底直连"，实现为沿用最近成绩继续路由（文档同） | 界面与功能清单文案对齐实现："暂停做题打分与路由效果生成，线上路由沿用最近一次成绩" | grep ✓ |

过程事故（再犯）：D6 回归误删演示模型 lexi-34b，已用 seed_models 恢复（历史成绩因只删模型行而保留）。**重申红线：破坏性验证必须新建临时对象。**

通过项（抽查）：兜底必选改造闭环（表单无空选项/默认选中平台兜底/存量回填 4 条/摘要显示名）✓；产品卡在线数口径（仅已上线计入）✓；策略删除同步清画像生成记录（上批 D5）复验 ✓。

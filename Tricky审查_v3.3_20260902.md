# Tricky 审查 · v3.3（2026-09-02）

- 方法：机器坏人程序 skills/23「Tricky 审查」八关流程首跑
- 范围：v2.8 之后的全部新面积（页面式编辑器、数据回流链路、聚合、API Key、植入 SDK、静态演示站）+ 旧清单 P1 复查
- 结论：两批共整改 **1 个 P0 + 5 个 P1**（第一批：insights test 污染/导入上限/场景可删；第二批：有效期分钟级失效/同步导入上限/任务异常锁死）；旧 P1 三项确认已修；另记 6 项 P2/边界声明

## 上轮遗留复查（v1.0 清单，基于 v2.8）

| 旧编号 | 问题 | 现状 |
|---|---|---|
| L3 | 线上服务中的配置可被 API 直接删除 | **已修**：delete 拦截条件 = 有生效快照（published 或 draft v≥1），非仅 status==published |
| L2 | 改名不查重可撞名 | **已修**：update_card 同样做租户内同名校验 |
| L5 | card_submitted 无幂等可刷看板 | **已修**：按 (user, render_id) 去重 |

## 本轮新问题与整改

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| D1 | 数据一致性 | P1 | insights 主查询（漏斗/触发分布/分组件转化）未过滤 channel=test：在测试抽屉里点几下，业务转化看板数字就被污染；而 questions/回显早已排除 test，同一页面两半口径不一致 | dashboard.insights 查询加 `COALESCE(channel,'')!='test'`，与 questions/回显拉齐 | API 实测 ✓ |
| S1 | 恶意用户 | P1 | /v1/bank/import/start 无条数上限：一次提交 10 万条可锁死全局导入任务约 1 小时（单任务互斥，期间全租户不能导入） | 上限 5000 条/次，超出 422 提示分批 | 5001 条被拒 ✓ |
| U1 | 新手首日 | P1 | 自定义场景只能加不能删：手滑建错名字的场景永远留在场景表里 | 新增 /api/scenes/delete（仅自定义可删；场景下有题目时 409 拦截防数据悬空）+ 场景表行悬停出删除 icon | 建→删 ✓、内置场景 422 ✓ |

## 第二批（全量轮补查，2026-09-02）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L6 | 挑剔 PM | **P0** | 有效期承诺"精确到分钟"但服务端用日期字符串与 datetime 混比：设当天 10:00 上线的配置全天不触发；设 18:00 下线的到期日全天仍触发——承诺与实现相悖且产生错误触发 | _in_validity 归一比较（纯日期补 00:00/23:59，与当前分钟时刻比较）；前端 chip 判定同口径 | 六用例行为矩阵全 PASS ✓ |
| S5 | 恶意用户 | P1 | 同步版 /v1/bank/import 无条数上限（上轮只封了异步版），裸调可提交超大 payload 阻塞请求 | 同步版同样限 5000 条/次 | py_compile ✓ |
| O2 | 运维 | P1 | 异步导入任务中途异常（如 DB 错误）status 永远停在 running，全局导入任务从此 409 锁死，只能重启服务 | _run_import 包异常兜底：异常落 done + error 字段并审计 | 代码路径核查 ✓ |

通过项补充：模型启用开关服务端有覆盖率校验（未完成入池评测不可上线）；名称 15 字/触发 200 字服务端双端一致；staged relabel→commit 数据链正确；聚合成本占比在筛选态下口径自洽。

## 记录在案（P2 / 边界声明）

| 编号 | 关卡 | 说明 |
|---|---|---|
| S2 | 恶意用户 | UI.el 的 html: 属性走 innerHTML——当前全站无调用点传用户数据（已核实 0 处），保留为红线：新代码禁止对 html: 传入任何用户输入 |
| S3 | 恶意用户 | /v1/embed/envelope 的 ?key= 参数未校验（演示边界）；正式化时按《植入技术方案调研》做域名白名单 + 签名 |
| S4 | 恶意用户 | 多租户接口 tenant_id 由请求方声明（演示单租户）；正式化需鉴权层注入租户而非信任入参 |
| D2 | 数据一致性 | channel=embed（植入流量）计入业务看板——判定为正确口径（真实用户流量），已在此声明 |
| O1 | 运维 | 异步任务（导入/评测/画像）状态在内存，服务重启即丢；前端轮询遇 idle 自动停，无卡死，但进行中任务会静默中断——正式化需任务持久化 |
| R1 | 上线检查 | shortId 数字展示 ID 为 hash%1e6，理论可撞（仅展示用途，复制的是完整 UUID，无数据风险） |

## 通过项（抽查记录）

- 状态机：配置生命周期无图外转移；上线锁编辑双端一致；并发编辑有 lock_version 冲突检测
- 注入面：搜索 LIKE 已转义；CSV 导出有 BOM + 公式注入防护；无 innerHTML 用户数据注入点
- 渠道隔离：traces/标签池/回显/questions/insights（本轮修复后）全链路排除 test
- 导航健壮：hash 路由有 hashchange 监听，浏览器回退正常；深链全覆盖
- 轮询卫生：模型评测/画像/评审轮询均判断 activeTab，离开页面即停；评测轮询已局部化

## 第三批（2026-09-03 大批次改版后增量审查）

范围：策略默认档重构（全局均衡 / 默认兜底）、参数配置抽屉字段重构（α / 画像权重 / 候选标签选择）、场景数据集详情页（题目级增删改标 / 导入 / 替换）、看板重排、tab 样式统一、tooltips 精简。

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L7 | 挑剔 PM | P1 | 参数配置抽屉按产品决策移除了「成本值」入口，但种子策略「全局均衡」仍带 ¥5/天预算帽：当天花满后静默降级为最便宜直连，界面上却无处可见可改——行为与界面承诺不一致 | 清空该策略预算帽（DB + seed 同步），预算降级机制保留在服务端供 API 使用 | DB 校验 ✓ |
| S6 | 恶意用户 | **P0** | 替换导入先清库再校验：整批数据全部无效（如传错文件）时场景数据被清空且什么都没导进去，纯数据丢失 | /v1/bank/import/start 在 replace 分支先校验至少一条有效题目，无效则 422 且不清库 | 无效替换被拒、原 25 题保留 ✓ |
| S7 | 恶意用户 | P1 | 替换标记存在页面闭包里：确认替换后在同一导入页连续导入两次，第二次会再次清掉第一次刚导入的数据 | replace 一次性消费：首次开始导入后即清除标记与提示 chip | 代码路径核查 ✓ |

记录在案（P2 / 边界声明，不改）：
- 「默认兜底」策略当前实现为 K=1 快速直连（非严格锁定兜底模型），移除 Judge 后与「全局均衡」同为可用档——语义与实现的精确对齐留待正式化
- 删除题目 / 更改场景标签作用于共享冷启动集（演示单租户无隔离需求）；场景删除会连带删除冷启动题目，属产品决策
- 场景删除 / 替换后「各场景路由去向」表在下次「重新生成」前可能残留旧场景行

通过项（抽查）：策略 CRUD 全链（新参数 alpha/profile_w 存取 / 默认档删除拦截）✓；场景详情增删改标 / 替换 / 级联删除 / 通用场景保护 ✓；导入页替换提示与一次性语义 ✓；看板空态文案与数据可得性逐卡核查 ✓；tab 激活态类名修复（此前 .on 类无样式，属实际 bug）✓

## 第四批（2026-09-04 平台拆分 v4.0 增量审查）

范围：双平台拆分（智能交互 / 模型路由两个独立站点）、产品类（products CRUD + MCP 接入）、策略即 API 融合、模型画像与决策模型搬家、双站构建管线。

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| D3 | 数据一致性 | P1 | 产品创建/更新接受不存在的组件 ID：绑定数悬空、详情页静默丢行 | 服务端按 cards 表过滤无效 ID 后落库 | 传入 not-exist 被过滤 ✓ |
| U2 | 新手首日 | P1 | 智能交互站上深链 playground#model 时导航按 hash 推断成路由平台，侧栏链接指向本站已删除的 router.html（404） | 双站构建各自注入 window.SIA_PLATFORM，导航严格按站点归属渲染 | 构建产物核查 ✓ |

记录在案（P2 / 边界声明）：
- 组件在工作台被删除后，产品的绑定引用仍保留（详情页按现存组件过滤展示）；正式化需删除时反查产品引用
- 路由站 dashboard.html 直接无 hash 打开会落到交互数据 section（导航不可达路径，仅深链可触发）
- MCP 接入为演示态：mcp_key 由 product_id 派生、/v1/mcp 未实现真实协议端点
- sk-live 独立密钥体系 UI 已退役，服务端端点保留（无入口）

通过项（抽查）：产品名重名 409 / 超长 422 ✓；策略创建带 allow_aggregation=false 存取 ✓；两默认策略删除拦截、改名后引用不断 ✓；双站 mock 快照含 /api/products、静态站产品页可渲染 ✓；决策模型移除→非兜底策略灰置链路 ✓

## 第五批（2026-09-04 晚，产品/风格/画像状态卡增量审查）

范围：策略画像状态卡与生成状态接口、产品管理改版与工作台产品封面、组件风格编辑器（选项/代码双模式）、首页轮播重构、双站构建。

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| S8 | 恶意用户 | P1 | /api/brands/delete 的 file 参数未做文件名归一校验（仅前缀判断），构造路径分隔符有越界删除面 | basename 归一 + 前后缀白名单校验前置，非法名 422 | 构造名被拒 ✓ |
| D4 | 数据一致性 | P1 | 删除风格后引用它的产品 brand_file 悬空：工作台封面风格下拉显示裸文件名，产品渲染回退行为不可见 | 删除时服务端把引用产品重置为默认风格并计入审计（products_reset） | 建风格→绑产品→删风格→产品回落默认 ✓ |
| R2 | 上线检查 | **P0(演示)** | 静态站 mock 层缺 /api/profile/gen-status：Pages 上场景数据集页「模型画像」整块空渲染（即用户报告的"看不到策略卡片"），点生成也无状态推进 | mock 补 gen-status（快照合并本地生成记录）+ rebuild 有状态返回；前端对缺字段响应做防御（generated/task 兜底），接口异常也渲染「未生成」卡 | 静态构建核查 + 本地三态截图 ✓ |

记录在案（P2）：画像"已生成"不感知题库后续变化（新鲜度）；products PUT 的 brand_file 不校验文件存在；rebuild 的 policy_id 不校验策略存在；「品牌风格 / 组件风格」称呼待统一。

通过项（抽查）：/api/brands POST 校验完备（brand_id 正则 + default 保护 + 颜色格式白名单，路径注入防住）✓；产品封面风格切换 PUT 部分字段不清空其余字段 ✓；风格编辑器 _options 往返 ✓；画像轮询 activeTab 守卫与全局任务互斥 ✓；复制策略命名 _副本 ✓。

## 第六批（2026-09-04 深夜，5 轮 strict review）

按用户指令连续五轮不同侧重的严格审查，全部当场整改回归：

| 轮 | 侧重 | 覆盖 | 结果 |
|---|---|---|---|
| 1 | 全页运行时扫雷 | 19 页/深链 DOM 断言（关键元素、运行时报错、超时） | 全过（首轮"加载失败"命中系脚本源码字面量误报，修正口径复核为零） |
| 2 | 接口契约与数据闭环 | 21 项 API 断言：策略/产品/风格/场景题库/画像状态/事件 全 CRUD 与错误码 | 21/21 PASS |
| 3 | 关键用户旅程 | 工作台封面→测试触发闭环→场景数据集画像三态→风格编辑器联动 逐帧截图 | 全过 |
| 4 | 静态站对等性 | 双站 13 页断言（含画像已生成态、组件触发流入快照） | 13/13 PASS |
| 5 | 代码卫生 | console.log/TODO/emoji/死引用/死常量/过期注释扫描 | 清理 PARAM_DESCS 死常量与两处过期注释，其余为零 |

过程事故与教训：第 2 轮验证「通用场景可删除」时把演示库通用题库（83 题）级联删除——种子中本无 general 域无法原样还原，已重建 20 道已打分通用题并重算画像恢复演示完整性。**教训入规：破坏性接口验证一律用临时场景/临时对象，不碰演示主数据。**

## 第七批（2026-09-05 晚，strict review：风格编辑器/产品域/首页动画新面积）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L8 | 挑剔 PM | P1 | 风格编辑器拆成独立卡后误入 style-split 网格第二格：编辑时组件预览被挤出右列、布局错乱 | 左列包容器（风格库卡+编辑器卡同列堆叠），预览稳居右列 | 截图 ✓ |
| L9 | 挑剔 PM | P1 | 深链打开编辑器时生效风格晚到覆盖预览（Brand.init 竞态），深色/配色预览失真 | 页面顶部按 URL 预设 _editingStyle 标记 + tokens.js init 尊重该标记不覆盖 | dump 无生效风格注入、截图预览色正确 ✓ |
| D5 | 数据一致性 | P1 | 删除策略后其「路由效果」生成记录留在 kv（孤儿数据，静态快照会带出幽灵状态） | delete_policy 同步剔除 policy_profile_gen 记录；存量清理 | API 实测 + kv 校验 ✓ |
| C3 | 文案 | P2 | 「产品接入」旧页面名残留两处（未绑定封面提示 / 规则块第5点） | 分别改「产品及风格管理」「产品对接」 | grep 清零 ✓ |

轮次结果：R1 全页扫雷 19/19 ✓（含新版编辑器/产品卡/首页动画断言）；R2 接口冒烟 16/16 ✓（全程临时对象，含新 _options schema 往返、无内置兜底策略、judge 移除恢复闭环）；R3 新面积截图走查 ✓（竞态修复实证：预览随配色方案变色）；R4 双站断言 9/9 ✓；R5 卫生扫描（console/emoji/死代码零，文案残留已清）。

## 第八批（2026-09-05，兜底必选与引用完整性审查）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L10 | 挑剔 PM | **P0** | 策略「兜底模型」配置从未被路由引擎使用：故障切换只认平台默认模型，用户配的兜底是摆设（承诺与实现相悖） | run_route 解析 params.fallback_model：明确指定时作为该策略的故障切换目标（独立于候选白名单，从全量在线模型解析，失效退回平台默认） | 白名单外兜底 swift-4b 在降级场景真实接管 ✓ |
| D6 | 数据一致性 | P1 | 删除模型不清理策略引用：候选白名单残留已删模型、兜底指向已删模型（悬空引用） | delete_model 遍历策略：白名单剔除、兜底重置为平台默认，审计记录清理数 | 建引用→删模型→白名单剔除+兜底重置 ✓ |
| C4 | 文案 | P2 | 决策模型缺失文案宣称"线上请求降级为兜底直连"，实现为沿用最近成绩继续路由（文档同） | 界面与功能清单文案对齐实现："暂停做题打分与路由效果生成，线上路由沿用最近一次成绩" | grep ✓ |

过程事故（再犯）：D6 回归误删演示模型 lexi-34b，已用 seed_models 恢复（历史成绩因只删模型行而保留）。**重申红线：破坏性验证必须新建临时对象。**

通过项（抽查）：兜底必选改造闭环（表单无空选项/默认选中平台兜底/存量回填 4 条/摘要显示名）✓；产品卡在线数口径（仅已上线计入）✓；策略删除同步清画像生成记录（上批 D5）复验 ✓。

## 第九批（2026-09-06，接口死角与静态站状态化）

| 编号 | 关卡 | 级别 | 问题（失败场景） | 整改 | 回归 |
|---|---|---|---|---|---|
| L11 | 挑剔 PM | P1 | 产品改名（PUT）不查重：可把两个产品改成同名，工作台封面切换与统计口径混乱（创建有查重、更新漏了——典型单端校验） | update_product 加排除自身的同名校验，409 拦截 | PUT 撞名 409 ✓ |
| S9 | 恶意用户 | P1 | 风格代码（tokens JSON）无大小限制：500KB 垃圾字段照单落盘，可撑爆 brand 目录与静态快照 | 序列化后超 100KB 拒绝（422 提示只保留 token 字段） | 500KB 被拒 ✓ |
| R4 | 上线检查 | P2 | 静态站产品操作无状态：新建/改风格/删除后刷新即回快照（演示不可信） | mock 层产品操作会话内状态化（created/updated/deleted 合并渲染） | 构建核查 ✓ |

记录在案（P2）：策略并发编辑为后写覆盖（version 自增但无冲突检测，cards 已有 lock_version 可参照）；内置业务场景不可改名（自定义场景名也无改名入口，建错只能删除重建）。

通过项（抽查）：events 按 render_id 幂等与日上限清洗复核 ✓；产品创建查重/超长复核 ✓；brands 颜色格式白名单复核 ✓。

---

# 第十批（2026-09-07 · v5.0 通用数据集冷启动 + 数据飞轮 专项）

- 范围：v5.0 新面——数据飞轮（AB 采样 / /v1/feedback / 一键演化）、三层路由、请求级 aggregate、成本双类型、维度数据集迁移；顺带清理两处历史残留数据
- 上轮遗留复查：第九批 P2（策略并发写 last-write-wins、维度不可改名）仍记账未修

## 问题清单

| 编号 | 关卡 | 级别 | 问题（含失败场景） | 状态 |
|---|---|---|---|---|
| S10 | 恶意用户 | P0 | 偏好回流可拿任意历史 trace 刷：遍历 /api/traces 对每条 POST /v1/feedback（chosen=想捧的模型）即可批量污染胜率，进而让「一键更新画像」产出被操纵的模型排序——飞轮的核心数据资产无准入门槛 | 已修：AB 采样时把双答案模型写入 route_decisions.decision.ab_test；/v1/feedback 只认真实出过 AB 的请求，且 chosen 限定在当次两份答案内。回归：历史 trace 422「没有出过 AB 双答案」/ 被调但非选项模型 422 / 选项模型 ok |
| S11 | 恶意用户 | P0 | 配额降级可被 aggregate=on 绕过：预算帽触发后 force_agg 仍在，照样跳过快车道并发多模型聚合，成本继续膨胀 | 已修：quota 降级分支同时 pop force_agg（降级优先于请求级参数） |
| O10 | 运维 | P1 | 一键演化任务无异常兜底：_run_evolve 中途抛错则状态永远 running，此后所有更新请求被 running 守卫拒绝（与第 X 轮导入任务锁死同病） | 已修：外层 try/except 落 failed+error；前端轮询补 failed 分支 toast「可重试」 |
| C10 | 文案 | P1 | 模型路由测试预设问法仍是旧业务场景（价格行情/港口作业），与维度化三层路由完全脱节，演示时看不到硬规则层 | 已修：换成六条维度示例（通用问答/代码/数学聚合/多模态硬规则/长文写作/闲聊直答） |
| D10 | 一致性 | P2 | 已删除模型的历史偏好仍计入飞轮胜率与团簇排序（名称回退显示原始 id），可能误导排序解读 | 记账 |
| D11 | 一致性 | P2 | 演化与某策略「生成效果」并发时，后者完成会重新盖「已生成」章，但其做题数据快照早于演化并入的偏好 | 记账 |
| L19 | 挑剔 PM | P2 | AB 实际采样率低于名义值：只有多路作答的请求才可能出双答案，快车道/兜底单路请求不出（文案未做硬承诺，暂可接受） | 记账 |
| S13 | 恶意用户 | P2 | 演化完成后可立即重复触发：无新增回流时版本号空转 +1（有审计可追） | 记账 |

## 数据清理（本批顺带）

- 删除测试残留模型 asdfasdfasdf（走 /v1/models delete，确认策略白名单与兜底无引用残留）
- 删除 AB 实验残留策略 policy-scene-quality-ab-b（scope=scene 且无 scene 值：路由永不命中、UI 因 ab_group 被过滤，纯死数据）
- 删除 router.html 死代码 SCENE_NAMES（casual/analysis 旧映射，无引用）

## 结构调整（用户决策）

数据飞轮从通用数据集页的卡片提升为独立导航页（通用数据集与模型路由测试之间）：总览四指标+回流维度分布+采样率点改、一键更新画像（页头）、演化团簇表（带「去重新生成路由效果」跳转）、各模型采纳胜率全表（胜率条+采纳/对比数）、最近回流明细 20 条（/api/flywheel 新增 recent 字段）；看板飞轮卡指路文案同步。

## 回归方式

py_compile + node --check 全过；/v1/feedback 三态 curl 实测（历史 trace 拒 / 非选项拒 / 选项通过，测试数据即测即清）；一键演化闭环此前已全链路验证；本地与双静态站截图逐页目检（数据飞轮页 / 通用数据集页 / 预设问法）；静态站 DOM 冒烟 3 页断言全过（build ia c32fb6f4 / router 6824507e）。


---

# 第十一批（2026-09-07 · 间距与展示专项 + v5.1 版本化收尾）

- 范围：全页面视觉走查（用户反馈「较多组件间距和展示存在问题」「info 图标 hover 只出问号」「飞轮文案字太多」「版本应为可选择按钮」「全部生成四按钮对齐」）+ 数据集版本化新接口守卫
- 上轮遗留复查：第十批 4 P2 仍记账

## 问题清单

| 编号 | 关卡 | 级别 | 问题 | 状态 |
|---|---|---|---|---|
| V1 | 文案/展示 | P1 | info 图标悬停常常只见问号光标不见内容：原生 title 提示出现慢（1-2 秒）、样式不可控，用户划走就错过 | 已修：ui.js 自绘即时 tooltip（悬停/聚焦立现，深色气泡，视口内自动定位，滚动即收起），全站 .help 统一生效 |
| V2 | 文案/展示 | P1 | 数据飞轮页文案过载：总览说明三行长句、三步叙事每步两行、胜率说明一整行小字，读不出重点 | 已修：全部改一句话主句，细节收进 info 图标（新 tooltip 承接）；导入确认弹窗同步精简 |
| V3 | 交互 | P1 | 数据集版本只是文字 + 藏在「版本历史」里的按钮，切换版本要展开找 | 已修：版本改为下拉选择按钮（v1·冷启动版 / vN·演化版），选择即确认切换；版本历史表保留看构成 |
| V4 | 对齐 | P1 | 路由效果区「全部生成」与三张策略卡的「重新生成」右缘不对齐（标题行无 padding、卡有 16px 内边距） | 已修：标题行补 padding-right:16px，四个按钮同列对齐 |
| V5 | 展示 | P1 | 参数配置抽屉「策略 ID」标签后值仍带 ID: 前缀，读作「策略 ID ID:430522」 | 已修：抽屉内值只显示数字（卡片上无标签处保留前缀） |
| V6 | 展示 | P2 | 最近回流 20 行「待导入」灰 chip 重复噪音 | 已修：待导入改灰字文本，仅已导入用绿色 chip |
| V7 | 文案 | P1 | 改名「数据集」后残留 6 处「通用数据集」（返回链接 ×2、空态、模板文件名、入池评测提示、路由规则第 4 条） | 已修：全部统一；路由规则第 4 条同步改「导入回流数据生成新版本、可回滚」口径 |
| V8 | 文案 | P2 | 看板延迟表路径「fallback」未汉化；飞轮卡仍叫「画像版本」；audit 两个迁移动作与 profile_rebuild 显示原始英文/旧名 | 已修：兜底直连 / 数据集版本 / 三个动作名汉化（生成路由效果） |
| S18 | 恶意用户 | 核查 | 回滚接口传不存在版本 / 非数字 → 404 / 422；/v1/feedback/pending since 非数字 → 422（FastAPI 类型校验） | 守卫成立，无需修 |
| D12 | 一致性 | P2 | 回滚到 v1 后再导入生成 v3：新版本按「累计口径」包含曾被回滚掉的 v2 回流数据（<= 生效版本过滤所致）。语义可辩护（版本=累计快照）但未在界面说明 | 记账 |
| D13 | 一致性 | P2 | 已删除模型仍以原始 id 出现在看板路由分布 / 成本图（历史 trace 保留所致） | 记账（与第十批 D10 同源） |

## 回归方式

node --check + py_compile 全过；回滚/回流接口守卫 curl 实测；数据集页（版本下拉 + 四按钮对齐）、数据飞轮页（瘦身文案 + info 图标）、看板、导入、详情页截图逐页目检；双站重建（ia d8302a7c / router 0c6a670e）。

---

# 第十二批（2026-09-08 · v6.0 随机探索冷启动 + Query 池聚类方案 专项）

- 范围：v6.0 全部新面——随机探索路由、query 落池、聚类定版、版本级画像（Judge 回放 + AB 采纳自适应融合）、模块重设计后的页面
- 上轮遗留复查：S13（版本空转）已销——重聚有「无新增拒绝」守卫；D10（已删模型偏好）画像侧已销——生成只遍历在线模型（飞轮胜率表仍记账）；其余 P2 保留

## 问题清单

| 编号 | 关卡 | 级别 | 问题（含失败场景） | 状态 |
|---|---|---|---|---|
| S23 | 逻辑 | P0 | 冷启动期出不了 AB：双答案只复用多路候选，而探索期是单模型直答——最需要采纳数据的阶段反而永远无 AB，与方案「随机出模型 + 偶尔出 ABtest」直接矛盾 | 已修：探索路径命中采样时额外补调一个不同模型出第二份（真实成本，文案在飞轮说明与规则块标明）；回归实测探索层出双模型 AB |
| S24 | 恶意用户 | 核查 | 灌池绕门槛：重复文本已去重；变体垃圾 query 可刷满 500 门槛并聚出脏簇（演示环境无频控 / 无租户隔离） | 记账 P2（生产需来源鉴权与频控） |
| S25 | 恶意用户 | 核查 | 聚类与画像任务互斥（互相拒绝）、失败落终态可重试、门槛/Judge 守卫拒绝语明确 | 实测成立，无需修 |
| D15 | 一致性 | 核查 | 回滚后再定版：全量重聚生成新版本，旧版本行保留其版本号且 ≤ 生效版本仍生效，簇快照为全量口径 | 语义自洽，无需修 |
| D16 | 一致性 | P2 | 删除已定版 query 后，簇快照的样本数（kv 静态快照）不回刷，与实际行数有偏差 | 记账（重聚时自然修正） |
| L21 | 挑剔 PM | 核查 | 新接入模型未参与历史画像：效果表该列显示 -，悬停提示「未参与本次画像」，重新生成即纳入 | 成立，无需修 |
| C12 | 文案 | P1 | 「不额外调用模型」的说法在探索期不成立（S23 修复引入例外） | 已修：飞轮说明与规则块区分画像路由期（复用候选）与探索期（补调第二份） |

## 回归方式

py_compile + node --check 全过；服务端全链路两轮实测（随机探索 → query 落池 → judge 守卫 → 聚类定版 6 簇 → 画像 288 次回放 → α 换算构成透明 → 簇匹配路由 → 探索期 AB 双模型），测后恢复冷启动初始态；本地冷启动态 / 画像态整页截图目检；双静态站 DOM 冒烟 8 页断言全过；mock 重构后单份 getMock/postMock（清理了历史层积的 5 份重复 postMock——这也是「静态站行为偶尔对不上」的隐性根因）。

---

# 第十三批（2026-09-08 · 接口设计与关键数据可管理性 专项）

- 范围：全部 API 面盘点（78 个端点 vs 前端真实调用交叉比对）；关键数据对象（画像 / 数据集版本 / 问题池 / AB 回流 / Judge / 采样率）的查改删审计生命周期；并入本轮五条即时反馈（覆盖开关文案、采样率控件、回流配置入口、多模态能力、术语人话化）

## 一、接口面整改

| 编号 | 级别 | 问题 | 状态 |
|---|---|---|---|
| API1 | P1 | 死接口 17 个：入池评测三件套、labels 汇总/信号源、bank/health、AB 实验四端点、cards/debug、component-types、route/explain、group 投票两端点——方案已废但仍可调，纯攻击面 | 已删；快照键同步清理 |
| API2 | P1 | /v1/bank/question/delete 命名残留已废弃的 bank 概念 | 改名 /api/dataset/query/delete（前端 / mock 同步） |
| API3 | P1 | 问题池不可完整管理：只能看最近 15 条 | 新增 GET /api/dataset/pool（分页 + 主题/来源筛选 + 总数） |
| API4 | P1 | 画像原始数据不可获取：只有按策略换算后的视图，无法导出比对 | 新增 GET /api/profile/matrix?version=（原始 Judge/采纳/样本/权重，可查任意历史版本） |
| API5 | 核查 | 任务型接口三件套（触发/状态/结果落点）在归类与画像两处结构一致；错误结构统一 {error}；POST /delete 风格全站一致 | 成立 |
| API6 | P2 | /v1 与 /api 边界有历史包袱（/v1/models 实为管理面）；/v1/route 仅 SSE 无同步模式；策略 history/rollback 有接口无界面 | 记账 |
| 回归事故 | - | 删除死接口时误删相邻的 _get_setting/_set_setting 与 /v1/feedback/pending、EVOLVE_MIN_FEEDBACK（编译不报运行才炸） | 全量 GET 端点扫一遍后补回并回归通过——切块删除后必须跑运行时全端点扫描，已纳入流程 |

## 二、可管理性结论（关键数据对象逐个过）

画像：可生成/重生成/查原始矩阵（含历史版本）/审计 ✓；数据集版本：列表/切换回滚/审计 ✓（记录型不可删，合理）；问题池：分页查/筛/单条删 ✓（主题误归不可改，重聚修正，记账）；AB 回流：pending 接口拉取/时间范围明细/一票制 ✓（已归入明细的全量管理查询记账）；Judge：配置/更换/移除/画像留痕 ✓；采样率：读写 + 滑杆 ✓。

## 三、本轮即时反馈整改

| 反馈 | 整改 |
|---|---|
| 「允许调用方按次覆盖」看不懂 | 改「响应终端用户的聚合选择」：开=按用户选择执行 / 关=忽略用户选择（锁死成本）；策略卡摘要同步 |
| 采样率编辑方式不合理 | 弃「点数值变输入框」，改与权重同款滑杆（拖动即改、松手保存），从指标格提出独立成行 |
| 最近回流缺配置入口与时间范围 | 加时间范围下拉（最近 20 条 / 近 7/14/30 天，服务端 days/limit 参数）+「数据接口」弹窗（回传与拉取两向说明，可复制） |
| 多模态能力差异不可见不可管 | 模型表加「多模态」列；添加/编辑模型带「图像识别」开关（服务端能力可改）；策略候选与兜底下拉标注「· 图像」；路由层 vision 过滤此前已有 |
| 「聚类定版」等术语难懂 | 全站人话化：聚类定版→归类生成数据集、簇→问题分类、Query 池→问题池、归账→归入版本、回放→各答一遍；专业词只在解释里括号出现 |

## 记账（多模态边界，本轮明确）

多模态请求由词面硬规则判定（生产应按消息附件类型判定）；多模态没有独立分类画像（哪家看图更准无分数，需多模态评测样本或 AB 细分）；聚合层对多模态回答未特殊处理。

---

# 第十四批（2026-09-08 · 全量回归扫查 + mock 行为单测资产化）

- 范围：连续大改后的断链与偏差排查——前端调用 × 服务端路由全量交叉比对、POST 守卫抽查、全页截图走查、mock 状态机行为单测；并入「文案不正规」即时反馈

## 问题清单

| 编号 | 关卡 | 级别 | 问题 | 状态 |
|---|---|---|---|---|
| R14 | 发布工程 | 核查 | 前端 35 个调用路径 × 服务端 58 条路由交叉比对：无断链（差异项均为参数化模板前缀） | 成立 |
| R15 | 发布工程 | P1 | mock 层只有语法检查、没有行为验证——第十二批的重复 postMock 事故证明这不够 | 已修：新增 tests/mock_smoke.mjs——node 驱动完整动线（守卫拒绝 → 配 Judge → 归类生成 → 画像 → 按策略换算 α 断言 → 分数构成字段 → 原始矩阵 → 飞轮联动 → 模型操作联动 → 问题删除），13 组断言全过；纳入每轮固定回归 |
| C14 | 文案 | P1 | 「冷启动 · 随机探索进行中」等拼贴/口语文案不正规（含「攒满」「挑一份」「选哪份回传哪份」「池子还是空的」「不好可回滚」「想锁死成本」等 14 处） | 已修：标题改「冷启动数据收集」+ 状态 chip；全部口语改书面（达到数量门槛 / 选择其一 / 由客户端回传 / 暂无数据 / 效果不佳 / 需要严格控制成本时建议关闭）；Judge 警示条残留的「真实 query」改「真实用户问题」 |
| S26 | 恶意用户 | 核查 | POST 守卫抽查：Judge 非法 ID 422 文案明确、pool limit 越界 422（框架校验）、feedback 缺参 422 | 成立 |
| V15 | 视觉 | 核查 | 数据集页（新 hero）、参数配置抽屉（图像标注 / 覆盖开关新文案 / ID 无重复前缀）截图目检 | 成立 |

## 回归方式

node tests/mock_smoke.mjs 全过；py_compile + node --check；GET 全端点扫描（前一批已入流程）；本地截图目检 + 双站重建。

## 第十四批补充：端到端旅程自测（同日）

以首日管理员身份走完整旅程（探索路由 → 归类 → Judge 引导 → 画像 → 分类路由 → AB 回传 → v2 迭代 → 回滚），4 处不合理当场修复：

| 编号 | 级别 | 发现 | 整改 |
|---|---|---|---|
| W1 | P1 | 路由过程流术语过时且与决策层不一致：探索期先报「维度匹配·命中 0 条基准题」再报随机探索（自相矛盾）；正常期报旧维度名「通用问答」而决策层是主题「价格与行情」；「历史命中率」「聚合器」旧词 | 过程流全面对齐：探索期不再发匹配步骤；「分类匹配·归入（主题名）·命中 N 条相似问题」「画像得分」「聚合模型总结定稿」 |
| W3 | P1 | 版本切换后画像未重生成时，线上实际沿用旧版画像数据（行为合理）但界面未说明，易被误读为裸奔 | overview 新增 latest_profile_version；效果区空态补「线上路由暂沿用 v(N) 的画像数据，本版生成后自动切换」 |
| W5 | P1 | 归类完成 toast 引导「接着生成画像」，但未配 Judge 时按钮是灰的——引导断链 | toast 条件化：未配 Judge 时提示「配置 Judge 模型后即可生成画像」 |
| W2 | P2 | 仅 1 条新增问题也可生成新版本，版本易被琐碎增量灌水 | 重归类确认弹窗在新增 <50 条时提示「收益有限，建议积累更多」（不设硬门槛，保留管理员自由） |

旅程其余环节（守卫拒绝语、AB 复用与补调、回滚后画像恢复、迭代闭环）自测通过。

---

# Tricky 审查 · 第十五批（2026-09-09，v7.0 智能路由方案功能逻辑专审）

- 范围：v7.0 全量（router_core 三层路由 / benchmark 画像端点 / 模型画像页 / 决策层 / 看板 / mock 状态机 / 审计）；八关流程，重点第 1（承诺-实现）、2（接口渗透）、4（冷启动）、5（文案）关
- 上轮遗留复查：T14 各项已随 v7 下线或仍在 P2 记账；v6 记账项中问题池频控 / AB 平局 / 回流采样率偏差随飞轮下线不再适用

## 问题清单

| 编号 | 关卡 | 级别 | 问题（含失败场景） | 建议 | 状态 |
|---|---|---|---|---|---|
| T15-1 | L- | P0 | 闲聊硬规则名存实亡：chat 命中后仍撞智能路由模型硬依赖——未配置时先 emit「日常闲聊，轻量直答」紧接着「未配置…直连兜底 Atlas-72B」，自相矛盾且闲聊用最贵兜底；已配置时闲聊也付判维一跳 + benchmark 打分，「轻量直答」承诺未兑现 | chat 分支改为最便宜在线模型直答（不判维、不聚合、不依赖路由模型），失败走既有降级链 | 已修 |
| T15-2 | O- | P1 | 审计动作缺 v7 中文映射：router_model_set / benchmark_score_set / benchmark_score_reset / benchmark_refresh / migrate_bench_v7 在操作日志显示英文原名，值班无法一眼读懂 | ACTION_NAMES 补 5 项映射 | 已修 |
| T15-3 | L- | P1 | 模型添加成功 toast 仍说「回填评测集后上线」——入池评测 v6 已废、v7 接入即生效，承诺与实现相悖，新手会去找不存在的评测入口 | 文案改「已添加并生效；benchmark 成绩缺失时在模型画像页补录」 | 已修 |
| T15-4 | L- | P1 | 「省钱优先」策略（仅单模型）默认允许调用方 aggregate=on 翻转为聚合，单次成本翻倍——省钱档的成本承诺可被终端用户击穿 | 种子策略 allow_agg_override 置 0（经 PUT params 合并写入），实测 aggregate=on 被拒（denied=true） | 已修 |
| T15-5 | D- | P2 | explore 残留：_handle_turn 两处死赋值 explore_ratio=0、decision_summary.is_explore 恒 false、policy.explore_ratio 回传——v7 无探索概念，字段误导 API 消费方 | 全部删除（route_context.is_explore 保留兼容历史 trace 读取） | 已修 |
| T15-6 | U- | P2 | 模型画像页零模型冷启动：benchmark 成绩表在无在线模型时渲染空表体、无空态与引导，新租户断链 | 加空态卡 +「接入模型」引导按钮 | 已修 |
| T15-7 | C- | P2 | 「成绩 / 分数」混用：成绩表页面用「成绩」，服务端与弹窗错误提示用「分数」 | 用户可见错误文案统一为「成绩」（server / 弹窗 / mock 三处同步） | 已修 |
| T15-8 | R- | P2 | mock 与服务端闲聊行为不一致：mock 的 chat 检查在 no_router 之后（未配置时闲聊也兜底）、dimensions 给了 ["knowledge"] | mock chat 前置于硬依赖检查、dimensions 改空、文案对齐服务端；smoke 加 2 条闲聊断言 | 已修 |
| T15-9 | L- | P2 | 接口形状不一致：allow_agg_override 在创建策略走顶层字段、更新策略走 params 合并——同一字段两个位置，对接方易踩坑（本轮审查自己即踩中：顶层 PUT 被静默忽略） | 记账：统一到 params 或双端点都收顶层字段；短期在 API 说明块标注 | 记账 |
| T15-10 | D- | P2 | benchmark_overrides 并发修正 last-write-wins（读-改-写非原子），两人同时改不同格可能丢一格；删除模型后 overrides 残留，同 ID 重接会带旧修正 | 记账：与策略并发写同类，生产需行级写或版本冲突检测；删除模型时顺带清 overrides | 记账 |
| T15-11 | L- | P2 | multi 模式（多模型+择优）在未配置路由模型时被 no_router 拦截、静默降为单模型兜底——决策层有说明，但「多模型」选择项未兑现 | 记账：multi 可考虑绕过判维直接全候选并发；或在测试页选 multi 且未配置时前置提示 | 记账 |
| T15-12 | O- | P2 | 判维演示实现（关键词规则）与生产（真实小模型）行为差异未在界面声明；判维仅取前 2 维为硬编码 | 记账：画像规则折叠块可补一句演示口径；生产化时维度数做参数 | 记账 |

## 整改记录

- 第一批（P0/P1，当场修）：T15-1 闲聊直答重写（router_core chat 分支 + chat_rule 死代码清理 + testchat 推导文案 + mock 同步）；T15-2 审计映射；T15-3 toast 文案；T15-4 省钱优先硬约束。
- 第二批（P2 顺手修）：T15-5 explore 残留清理；T15-6 空态；T15-7 文案统一；T15-8 mock 一致化。
- 回归方式：py_compile + node --check（全部内嵌脚本）；curl 实测（未配置闲聊 rule 直答不再 no_router、配置后闲聊 0 次 dims、swift-4b 2% 模拟超时命中时降级链正常接管、省钱优先 aggregate=on denied=true、复合判维 math+writing 不回归）；全 GET 端点运行时扫描 non-200=none；`node tests/mock_smoke.mjs` 28 断言 ALL PASS；audit 页截图确认新映射中文渲染。
- 过程教训：外部 sqlite3 直写 WAL 模式下会被服务端 checkpoint 覆盖——改库一律走 API 或停服后写。

---

# Tricky 审查 · 第十六批（2026-09-09，用户触发的 API 接入线 PM 专审）

- 范围：API Key 接入全链路（文档 / 生命周期 / 校验 / 配置持久化）+ 画像页黑盒感
- 触发：用户三点反馈（聚合开关下缺 API 说明 / 全站无接入文档 / 刷新榜单快照黑盒）+ PM 视角主动排查

## 问题清单

| 编号 | 关卡 | 级别 | 问题（含失败场景） | 建议 | 状态 |
|---|---|---|---|---|---|
| T16-1 | D- | P0 | **种子策略与模型每次重启回滚出厂**：seed_policies / seed_models 用 INSERT OR REPLACE 且 run_all 每次启动执行——管理员改的候选、权重、聚合硬约束、模型单价、多模态开关、停用状态全部静默丢失（Tricky15 设置的省钱优先硬约束即被此 bug 抹掉，本轮截图目检发现开关状态异常才暴露；此前所有验证都在改完立即验证的窗口内，从未跨重启检查） | 改 INSERT OR IGNORE（新库插入、老库不覆盖）；跨重启回归纳入验证清单 | 已修 |
| T16-2 | S- | P1 | **无效 API Key 静默回落默认策略照常计费**：Key 拼错 / 已重置 / 策略停用时，请求悄悄按默认策略执行——调用方毫无感知，计费与归因全错，Key 重置形同虚设 | 明确拒绝：final 事件 error=invalid_api_key + 引导文案，不再回落 | 已修 |
| T16-3 | L- | P1 | **API Key 无生命周期管理**：Key 泄露 / 人员变动无处置手段（Key 由 policy_id 无盐派生，无法作废） | 派生加盐（kv policy_key_salt，盐 0 兼容现有 Key）；新增 POST /v1/policies/{id}/reset-key + 策略卡「更多 → 重置 API Key」（确认三要素 + 新 Key 一次性弹窗）+ 审计 | 已修 |
| T16-4 | C- | P1 | **全站无接入文档**（用户点名）：Key 发出去了，对接开发只有藏在策略抽屉里的折叠块，无法独立完成对接 | 新增「接入文档」页（导航配置组）：快速开始 curl / 请求参数表 / SSE 事件表 / final 字段表 / 聚合与成本约束 / Key 治理与稳态，逐块可复制 | 已修 |
| T16-5 | C- | P2 | 聚合覆盖开关下缺 API 说明（用户点名）：管理员不知道「响应终端用户的聚合选择」对应调用方的什么行为 | 开关下补 aggregate 参数说明 + applied.override_allowed 口径 + 接入文档链接 | 已修 |
| T16-6 | C- | P2 | 刷新榜单快照黑盒（用户点名）：不知道刷新的是哪些榜单 | 确认框列出七个维度的引用榜单清单 + 当前快照日期 | 已修 |
| T16-7 | D- | P2 | 策略卡摘要与抽屉读取的 params 依赖 /v1/policies 列表，硬约束等配置无版本一致性校验；策略并发写仍 last-write-wins | 记账（与 T15-10 同类） | 记账 |
| T16-8 | L- | P2 | Key 无按环境区分（测试/生产同一把）；用量统计按租户无按 Key 维度 | 记账：生产化需求 | 记账 |

## 整改记录

- 回归方式：Key 生命周期 curl 全链路（现有 Key 盐 0 不变 → 重置得新 Key → 旧 Key 调用 error=invalid_api_key → 新 Key 按对应策略路由）；配置持久化跨两次重启验证（策略 override=0 与模型单价均保留）；接入文档页与策略抽屉截图目检；mock 增 reset-key 会话态，smoke 增至 30 断言 ALL PASS。
- 教训：**幂等验证必须含跨重启回归**——「改完立即验证通过」不等于持久化正确；种子函数一律 INSERT OR IGNORE / 存在守卫，禁止 OR REPLACE。

---

# Tricky 审查 · 第十八批（2026-09-10，v2.1/v2.2 大改动专审）

- 范围：极简编辑器 / 样式设计器 / 组件库详情 / 模拟器 / Figma 级品牌编辑器 / 新配色 / 产品切换器 / 多产品种子 / Key 重置 / sia.css 端点 / 注册表版本戳 / 首页 / 组件视觉两轮

## 问题清单

| 编号 | 关卡 | 级别 | 问题（含失败场景） | 建议 | 状态 |
|---|---|---|---|---|---|
| T18-1 | S- | P0 | **产品 brand_file 无校验可路径注入**：PUT /api/products 接受 `../../server/app.py` 入库——实测被接受且打坏该产品品牌（渲染空样式）；若指向任意合法 JSON 文件即可将其内容注入 SDK CSS 分发 | update_product 校验（basename 一致 + .json 后缀 + 文件真实存在）；sia.css 端点 basename 兜底；被打坏产品数据修复 | 已修 |
| T18-2 | L- | P1 | **编辑已有风格不回填真实 token**：THEME_PRESETS 重构后编辑任何风格都显示墨蓝预设值而非该风格实际颜色，保存即静默覆盖 | existing 时加载该风格文件并回填 light/dark/chart 与形状选项 | 已修 |
| T18-3 | L- | P1 | 「明暗双份」半兑现：color_dark 只存不发——SDK CSS 无暗色输出 | sia.css 端点输出 `@media (prefers-color-scheme: dark)` 块（有 color_dark 才输出），curl 实测双份齐全 | 已修 |
| T18-4 | O- | P2 | 审计缺 product_key_reset / migrate_assistant_v21 / migrate_products_v22 中文映射 | 补映射 | 已修 |
| T18-5 | C- | P2 | 组件详情页头部按钮无「已启用」态（卡片有、详情没有） | 对齐 | 已修 |
| T18-6 | R- | P2 | 静态站产品 Key 重置无 mock：点击后弹窗显示 undefined | mock 补 reset-key | 已修 |
| T18-7 | C- | P2 | 「新建组件风格」入口文案啰嗦（走查 7.4 遗留） | 改「六套预设起步，逐 token 细调」 | 已修 |
| T18-8 | D- | P2 | tokens.js / 平台预览端未消费 color_dark（预览深色靠编辑器手动合成）；组件 SDK 暗色跟随宿主已兑现，平台内暗色预览链路待 Stage2 | 记账 | 记账 |

## 用户随批反馈（并入整改）

- 预设色板「太花花绿绿」：预设卡撤状态色，只摆主色 / 强调色 / 面色三块（克制的企业观感）
- 主题预设收敛为单行当前主题 +「更换主题」展开选择（Figma 式样式选择器形态）

## 整改记录

- 回归：路径注入复测 422；被打坏产品数据修复（1 行）；sia.css 明暗双块 curl 实测；编辑回填逻辑语法与页面渲染检查；全内嵌脚本 node --check；截图目检预设收敛与折线图横轴修复。
- 教训：**文件名字段一律 basename + 存在性校验**（brand_file 与当年 vault 凭证同类：接口层是主战场）；大重构（THEME_PRESETS）必须回归「编辑旧数据」路径，不只验证新建。

## 第十九批（2026-09-10 · 样式数值化 / 产品瘦身 / 品牌 mock 之后）

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| T19-1 | P1 流程 | 品牌 mock（上一轮的根因修复）没有冒烟断言，下次改 mock 可能无声回退 | mock_smoke 补品牌全链路断言：保存/列表出现/文件 fetch 预览/删除移除/默认品牌拒覆盖 |
| T19-2 | P1 越权 | /v1/embed/envelope 只验「key 是任意产品的合法 key」，产品 A 的 Key 可拉产品 B 的组件配置（含文案与样式） | 鉴权到具体产品：key 解析出所属产品，card 不在其 card_ids 内返回 403；实测财税 key 拉官网组件 403、自有组件 200 |
| T19-3 | P2 | 渲染端 px() 对 null/空串返回 0：style_overrides.radius=null 会被渲染成 0px 直角而非跟随品牌 | px() 先排除 null/""；服务端值域校验同步把 null 挡在写入前（422） |
| T19-6 | P2 | style_overrides 只校验 key 白名单、值不限：radius 9999、color.primary 注入串都能入库 | 服务端值域校验：数值 px 域（radius 0-28 / height 28-56 / spacing 8-24 / font_scale 12-16）+ 旧档位枚举兼容 + shadow 枚举 + 色值 HEX 正则；实测 radius 999 与 CSS 注入串均 422 |
| T19-7 | P2 | 删除品牌风格的确认文案承诺「使用它的产品回退默认」，服务端实际不改产品 brand_file（sia.css token 悬空） | delete_brand 同步 UPDATE products 回退 default；实测删除后产品 brand_file 变 brand-tokens.default.json |
| T19-4 | P3 | 主题重设计后旧亮蓝 #1DA1F2 残留 3 处（toast 示例文案、两个取色器默认值） | 全部换 #3E63DD |
| T19-5 | P3 | 品牌编辑器 ?r= 深链在圆角改 px 后语义漂移（旧档位索引会被当 px 解释） | r≤4 视为旧档位索引映射到 [2,8,16,22,28] |

附带发现（未改）：validate_card 内新增校验触发了 `_re` 作用域问题（import 在另一函数内），已在本批一并补导入并回归。

## 第二十批（2026-09-11 · 组件扩容 19 类之后）

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| T20-1 | P1 | rank 渲染器读 options，注册 schema 是 items——模型按 schema 传参渲染为空态 | items \|\| options 双格式 |
| T20-2 | P1 | likert 渲染器读 likert{steps}，schema 是 scale/low_label/high_label——刻度与标签全失效 | p.scale 组装 from/to + 标签兼容 |
| T20-3 | P2 | datetime 渲染器读 display，schema 是 mode——datetime 模式永远退回 date | mode \|\| display |
| T20-4 | P3 | slider 初始值读 recommended_default，schema 是 default | default 优先 |
| T20-5 | P2 | present 直渲不认识新 3 类（compare/list/highlight 落到折线兜底渲染降级） | gen_present_params 补三类 |
| T20-6 | P2 | SDK 截断兜底缺新数组参数（items/steps/events/slices/dimensions/fields 可被放大渲染） | sia.js 全部截断 |
| T20-7 | P3 | 编辑旧主题时新增 token（卡片面/凹陷底）无值回填成黑色 | 默认值兜底表 |
| T20-8 | P1 | 「优先级排序」种子发布失败留 draft：v1 校验要求 rank 预置 options ≥2，与 v2「条目由模型给出」冲突 | 校验放开为「预置才校验」，实例已发布，envelope 200 |

方法论沉淀：**新增注册类型时，params_schema 字段名必须以渲染器实际消费的字段为准对齐（或渲染器双格式兼容）**——schema 是模型的调用契约，写错字段名等于组件白做。

## 第二十一批（2026-09-11 · 三步编辑器 / 固定内容 / 瀑布图 / 工作台卡片化之后）

| # | 级别 | 检查项 / 问题 | 结果 |
|---|------|------|------|
| T21-A | 实测 | 固定内容全链路：固定选择实例（题目/选项/推荐/回显）发布 → 注册表 fixed 只读下发、params_schema 移除 options | 通过（fixed 携带 content_mode+options，schema 无 options） |
| T21-B | 实测 | 固定模式单选项发布拦截 | 通过（409 预置选项至少 2 个） |
| T21-C | 实测 | 动态内容开回显拦截 | 通过（422 动态内容无法聚合） |
| T21-D | P2 | 瀑布图未加入演示直渲参数生成——规则命中后落趋势图兜底渲染降级 | 已修（gen_present_params 补 waterfall） |
| T21-E | P3 | 注册表 fixed 元数据缺 recommended_default（渲染走 envelope 不受影响，但 agent 侧可见性不完整） | 已修 |
| T21-F | 数据 | 官网智能客服 brand_file 被此前测试残留改为 harbor | 已修回默认墨蓝 |
| T21-G | 记录 | 固定模式题目未显式填写时用默认兜底（编辑器已必填标注）；服务端不强拦——保持宽松 | 不改 |

## 第二十二批（2026-09-11 · 用户实盘截图触发：以真实 UI 逐屏走查为核心）

| # | 级别 | 检查项 / 问题 | 结果 |
|---|------|------|------|
| T22-1 | P1 | 选择组件步 UI 崩坏：tab 容器缺 `tab-bar` 类致裸描边按钮；`.tpl-grid` 三处定义打架（内嵌 4 列 > shared 2 列 > modal 3 列）；tpl-pick 内嵌旧样式压制新大卡；卡片 title 属性产生原生 tooltip 噪音 | 已修（删两处冗余定义、shared 唯一源、去 title），截图验收通过 |
| T22-2 | P1 | 展示类组件的内容配置步整页只有一个名称框（空步骤） | 已修：展示类 steps() 跳过内容步，名称字段移到样式步顶部，截图验收通过 |
| T22-3 | P0 | 页面式编辑器「上一步/下一步/保存」永远沉在页底看不见：`.modal.editor{overflow:hidden}` 把 sticky foot 困在容器内，且 `.modal.editor .modal-foot{position:static}`(0,3,0) 特异性一直压过 `.editor-inline .modal-foot{sticky}`(0,2,0)——吸底从未生效过 | 已修：inline 模式 overflow:visible + 足特异性重写 sticky；同时预览列 sticky 也因此恢复（top:112 对齐步骤条下沿），双截图验收 |
| T22-4 | P2 | 非法 / 已下线组件类型深链（?new=1&ct=xxx）进入残缺编辑器：无内容面板、预览降级为文本 | 已修：对照 V2_TEMPLATES 校验，非法回落到选择组件步；注意 /api/templates 只含交互类，不能拿它当全量校验表（曾误伤 table 深链，已改） |
| T22-5 | P1 | 首页 hero 轮播（用户截图怒斥）：案例二「信息表单」偷懒复用选项按钮样式，两个方框写「联系人 *」不像表单（现成的 .frow/.flab/.fin 字段样式没用上）；演示区 720px 宽但卡片只占左 300px 右侧大片空白；文案全是开发黑话（「字段由调用参数定义」「按 submit 结构回传」「内容全参数化」「token 定制」）；展示类数量写 7 类实为 11 类 | 已修：真实表单字段+填充动画、inner 收窄 560px、五个案例文案全部改人话、类数改 11，截图验收通过 |

方法论沉淀：**sticky 失效必查两件事——祖先链上的 overflow（hidden/auto 都会把 sticky 困在该容器）和特异性（后写的规则可能一直被更高特异性的旧规则压着，「看起来生效过」可能从未生效）**。UI 验收必须以真实截图为准，DOM 里有元素不等于用户看得见。

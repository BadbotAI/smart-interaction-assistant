# 智能交互平台 · 项目背景与记忆

- 更新：2026-09-02（v3.3）
- 英文名：Smart Interaction Assistant
- 仓库：https://github.com/BadbotAI/smart-interaction-assistant
- 在线演示（静态快照）：https://badbotai.github.io/smart-interaction-assistant/
- 植入演示页：https://badbotai.github.io/smart-interaction-assistant/embed-demo.html
- 设计系统：Figma「BadBot-for-MCP」→ `SIA Foundations` / `SIA Components` 两页；代码侧同源于 web/shared.css 顶部总注释

## 一、产品背景

一个 AI 应用的网页端管理平台，面向供应链基地客服场景演示。管理员把「交互组件」配置进 AI 对话——什么条件触发、出什么组件、收回来的数据去哪；对话从「用户自由输入—模型自由回复」变为「模型主动引导—用户结构化响应」。同时提供多模型统一接入与智能路由（JiSi，源于 Avengers-Pro 论文产品化）、行为埋点采集与运营看板：用户的每次选择回流为监督信号，持续优化模型调度。

对应标书条款：《供应链数据智能及多模态工具集建设项目需求规格说明书》2.5 智能助手与交互工具（F-5-01 ~ F-5-05）。

核心闭环：**交互产生标签 → 标签影响路由 → 效果在看板可见**。

## 二、四大核心模块（领导定稿口径）

1. **模型路由**：模型配置（两步添加 / 入池评测 / 供应商分组 / 启用开关）、路由策略（α 成本-效果 / 策略卡 / 接入 API Key）、模型对话测试（策略选择 + 决策 CoT）、模型画像（簇×模型矩阵）、数据集冷启动与回流（场景数据集 / 待并入校验 / Judge 评审 / 评测回填）、聚合回复机制
2. **智能交互**：智能组件库（15 器 + 呈现类，协议信封渲染降级）、组件管理（工作台 + 页面式五步编辑器）、组件触发测试
3. **数据展示**：交互数据（含转化六模块）、路由数据（含聚合效果）、导出、链路追踪与审计
4. **对外接入**：外部模型接入、/v1/route API、植入 SDK（sia.js：Shadow DOM 渲染 + 三种触发 + sendBeacon 回传）、数据契约、调用统计

## 三、版本大事记

| 版本 | 要点 |
|---|---|
| v2.6-2.8 | 信息架构定型、两步向导、12 器组件体系、五步弹窗、测试抽屉 |
| v2.9-3.0 | 整改三批（服务端守门/选项主键/承诺对齐）、组件交互系统（五态/定高/重置）、品牌 token 租户级 |
| v3.1 | 数据管道页重写、17 类卡、转化看板独立、自绘下拉、操作日志 |
| v3.2 | 15 器（追问并入入口跳转 / 地址卡片 / 表单收集 / 对比表格化）、组件文案并入气泡、组件内部设计系统、弹窗规范、版本概念摘除、上线锁编辑 |
| v3.3 | 页面式编辑器、数据回流重构（场景数据集/待并入校验/Judge/异步导入）、聚合产品化、策略 API Key、植入 SDK、GitHub Pages 静态演示、SIA 设计系统 v1.0、渲染优化 |

## 四、关键约定（工作记忆）

- **启动**：`cd ~/Desktop/富交互项目 && python3 -m uvicorn server.app:app --port 8787`；首启自动建库注种子；重置删 server/platform.db。curl 本机要 `--noproxy '*'`
- **验证流程（每改必验）**：node --check（HTML 内嵌脚本先抽出）+ py_compile + 无头 Chrome 截图逐页目检；components.js 切片改动必须跑运行时加载测试
- **文档约定**：功能说明书.md 改完同步桌面 vX.Y md/docx（旧版删除）；功能清单为四大模块七列表
- **发布约定**：git commit+push；web/ 改动后重建 docs/（路径改写 + mock 注入 + prefetch）再 push，Pages CDN 约 10 分钟生效
- **审查约定**：大版本前跑「Tricky审查」（机器坏人程序 skills/23，八关审查），产出问题清单并整改
- **设计规范**：SIA DS v1.0——主题蓝唯一品牌色、语义色只表状态、字号五档 20/16/15/13/12/11、字重 400/600/700、4px 间距基线、全站禁 emoji、ID 等宽胶囊可复制
- **已知坑**：Bash cwd 会重置（命令先 cd）；后台 uvicorn 后不要裸 wait；截图加 host-resolver-rules 防 OSM 超时；深链 `cards.html?new=1&ct=&step=`、`router.html#flow/#import/#review`
- **演示边界**：模型作答 / 连接校验 / AI 抓价 / 评审打分均为确定性模拟；凭证是 vault:// 引用不落真实密钥；对外 API 未启用鉴权硬化

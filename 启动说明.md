# 智能交互平台 · 代码包（2026-09-12）

## 跑起来

```bash
cd 智能交互助手_20260912
python3 -m uvicorn server.app:app --port 8787
```

浏览器打开 http://127.0.0.1:8787 —— 首次启动会自动建库、灌示例数据。

依赖：Python 3.10+、`fastapi`、`uvicorn`（`pip install fastapi uvicorn`）。前端无构建步骤，改完刷新即可。

## 目录

| 路径 | 是什么 |
|---|---|
| `web/` | 前端：`shared.css` 设计系统 · `ui.js` 原语库 · `components.js` 组件渲染引擎 · 各页面 |
| `server/` | FastAPI：`app.py` 路由 · `cards.py` 组件域 · `analytics.py` 数据分析 · `db.py` SQLite |
| `brand/` | 5 套风格主题的 design token |
| `docs/` | GitHub Pages 静态站（`build_docs.py` 产出，含 mock 数据快照） |
| `contracts/` | 对外数据契约 |

## 静态站

```bash
python3 build_docs.py --snapshot     # 先起 8787 服务，否则快照是旧数据
```

产出到 `docs/`，推到 GitHub 即上线。

## 文档

- `组件基线要求.md` —— 组件的 6 组 27 条基线，每条标了怎么核对
- `功能说明书.md` —— 产品功能清单与各轮变更（当前 v11.0）
- `产品功能开发方案.md` —— 技术架构与质量门禁

## 质量门禁

改完组件相关代码，按顺序跑完再提交：

```bash
node tests/mock_smoke.mjs        # 接口契约
node e2e_loops.js                # 主流程 E2E（13 项）
node visible_audit.js "<组件类型>"  # 每个配置项都要有可见效果
node coverage.js "<组件类型>"       # 图形/文字的配置要成套
node flow_check.js "<组件类型>"     # 四步流程与按钮位置
```

后三个脚本依赖页面上每个配置行带的 `data-sty-key`，新增配置行时别忘了带上。

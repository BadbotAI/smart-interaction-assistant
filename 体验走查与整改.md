# 体验走查与整改记录

> 视角：交互体验设计 + 视觉设计 + 产品。方法：先抽象每个功能的核心用户体验流程，
> 对照流程检查覆盖度与合理性，再逐页面审查交互与文案，最后整改。
> 日期：2026-07-31

## 一、核心用户体验流程抽象

### 角色

- **终端用户**：在对话里提问、用组件交互、给反馈（不知道也不需要知道背后有场景/路由）
- **管理员（运营）**：配置客服场景、看数据、调整
- **算法/实施**：管模型池、策略、排障（Trace）

### F1 终端用户 · 对话主流程

```
输入问题
  ├─ 命中场景 → 展示信息内容 + 交互组件 → 用户提交 → (回显：XX%同类答案) → AI 基于提交继续回答
  │                                   └─ 用户不想用组件？ → 【走查前：无路可走，被组件卡死】
  ├─ 未命中 → 路由（过程实时可见）→ 答案 + 结构化组件 → 反馈（赞踩/择优/复制/换模型）
  └─ 高风险意图 → 确认闸门 → 确认执行 / 取消
```

### F2 管理员 · 场景配置流程

```
新建场景 → 描述触发条件（AI 改写）→ 配置信息与交互（AI 匹配模版）→ 开关回显
  → 【走查前：配完看不到用户视角效果，只能发布后去对话里试】
  → 发布 → 触发调试验证命中 → 看板看数据 → 迭代编辑
  → 【走查前：编辑已发布场景 = 线上立刻消失，直到重新发布】
```

### F3 管理员/算法 · 路由运营流程

```
注册模型 → 入池回填 → 上线 → 策略调参/AB → 看成本-质量对比 → 回滚或固化
```

### F4 排障流程

```
看板发现异常 → 下钻 Trace → 逐 span 定位 → （需要时）解除脱敏（审计）
```

## 二、主流程覆盖度与合理性分析

| 编号 | 发现 | 严重度 | 判断 | 处理 |
|---|---|---|---|---|
| A1 | **场景误触发无逃生口**：命中场景后用户只能提交组件才能继续，若触发错了（触发匹配是概率行为）用户被卡死 | P0 | 主流程缺失分支 | 组件下加「跳过，直接让 AI 回答」；同时把跳过记为 card_abandoned（这本身是触发准确率的珍贵负样本） |
| A2 | **编辑已发布场景导致线上消失**：编辑会把状态改回草稿，而运行时只认 published，管理员一点「编辑」线上场景就下线了 | P0 | 与文档 §2.7 快照语义冲突 | 运行时改为服务「最新发布快照」；编辑中的草稿不影响线上，直到再次发布。列表状态显示「编辑中 · 线上 vN」 |
| A3 | **换模型重答发的是元指令**：重答发送"换个模型再回答一次刚才的问题"，路由收到的是一句无领域信息的话，bank 里没有相似题，重答质量必然差 | P1 | 实现错误 | 重答改为静默重发原问题（跳过场景匹配），不产生新的用户气泡 |
| A4 | **提交后组件仍可操作**：选项按钮提交后还能点，状态与数据不一致 | P1 | 状态机缺终态 | 提交后冻结整个组件的交互控件，再追加回显 |
| A5 | **配置无预览**：管理员配完场景，不发布就看不到用户会看到什么；文案/选项错了要发布-试-再改 | P1 | 流程断点 | 编辑器内置「预览用户看到的效果」，直接用线上同一套渲染器渲染 |
| A6 | **无法中断生成**：聚合路径要等数秒，用户不能停（文档 §2.3.3 有 control.interrupt，未落地） | P1 | 功能缺口 | 生成中「发送」变「中断」，中断即停并记 control_invoked |
| A7 | **用户绕开组件另起提问时，未提交的组件无任何记录**：漏斗里的"放弃"全靠超时，数据失真 | P2 | 埋点缺口 | 新消息发出时对未提交的组件补报 card_abandoned；看板漏斗加「放弃」列 |
| A8 | **文案体系不统一**：管理端已经改叫"场景"，但导航、门户、调试面板、错误提示、历史弹窗里还混着"卡片" | P2 | 心智模型割裂 | 全站文案清扫：面向用户和管理员统一说「场景」；"卡片"只保留在开发文档/代码层 |
| A9 | **回答脚注信息过载**：双维度赞踩（4 个按钮）+ 4 个决策 chip + 4 个操作按钮，单条回答下挂 12 个元素 | P1（用户实测反馈上调，原判"可接受"错误） | 主流程噪声 | 赞踩收成一对（点踩后追问原因，按"答得不对/不合需要"分组保住 capability/preference 标签语义，走 §2.3.4 合规路径二）；决策信息收成一行灰字 + 「详情」入口（链路入口并入详情弹窗）；聚合择优对比默认折叠 |
| A10 | 群体回显的"共 N 人回答"与看板口径（人次/次数）需一致 | P3 | 口径核对 | 回显读 respondents（去重人数），看板同口径，已一致 |

**更好解法的取舍说明**：A1 曾考虑「组件不阻塞、AI 同时开始回答」的并行方案，
但那会让"收集信息再答"的场景失去意义（信息还没收就答了），故采用"阻塞 + 显式逃生口"。
A2 曾考虑「编辑即热更新线上」，被否：与可追溯性（Trace 记录 card_id+version）冲突，
且管理员的中间态会直接暴露给用户；快照语义是正确解法。

## 三、逐页面审查结论（文案与交互）

### 对话演示（chat.html）
- 头部说明还在讲"采集卡片触发"，用户视角应讲人话 → 改
- 场景命中时输入框没有提示当前状态 → 组件即状态，加逃生口即可，不再加提示（避免双重引导）
- 决策 chips 密度偏高但面向演示可接受；「为什么选它」「查看链路」保留

### 场景模版管理（cards.html）
- 调试面板文案「命中哪张卡/竞争卡片」→ 场景口径
- 搜索占位、页面标题、删除/引用/历史弹窗中的"卡片" → 场景
- 编辑器三步结构合理；补预览（A5）
- 状态列：草稿/已发布/已下线清晰；补「编辑中 · 线上 vN」（A2）

### 路由控制台（router.html）
- 面向算法/实施角色，术语密度合理；超参 chip 有中文 tooltip，保留
- 无阻塞问题，不整改

### 运营看板（dashboard.html）
- 工具1/工具2 双视角结构与用户目标匹配
- 漏斗缺「放弃」（A7）→ 补
- 空态都有引导动作，保留

### 链路追踪（trace.html）
- 面向排障角色，瀑布 + 原始数据折叠合理；脱敏-审计闭环完整，不整改

## 四、整改清单（本次落地）

1. A2 运行时服务最新发布快照 + 列表「编辑中 · 线上 vN」状态
2. A1 场景组件「跳过，直接让 AI 回答」+ 跳过记 card_abandoned
3. A4 组件提交后冻结交互
4. A3 换模型重答改为静默重发原问题
5. A5 编辑器内置用户视角预览（线上同一渲染器）
6. A6 生成中可中断
7. A7 弃答补报 + 看板漏斗加「放弃」
8. A8 全站文案清扫（卡片 → 场景）

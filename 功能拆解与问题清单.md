# 智能交互平台 · 功能拆解与超级问题清单

- 版本：v1.0（2026-08-27，基于 v2.8 原型逐模块核查）
- 方法：以前后端一体的视角拆解每个模块的能力、表单与字段、字段联动与状态流转；再分别用「挑剔的产品经理」（逻辑严谨性，编号 L-）与「产品文案设计师」（命名 / 字段文案 / 空态 / icon 取舍 / 呈现方式，编号 C-）两个视角复审。所有结论均对照实际代码（web/*.html、server/*.py）核实，非凭界面印象。
- 严重度：**P0** 会产生错误数据、可被滥用或承诺与实现相悖；**P1** 逻辑缺口 / 一致性问题，影响可运营性；**P2** 打磨项。文案类问题统一 C 编号，另标建议。

## 〇、全局架构速览

| 层 | 组成 |
|---|---|
| 前端页面 | 组件工作台 cards.html（含五步配置弹窗、测试抽屉 testchat.js）· 组件库 library.html · 导入品牌风格 design.html · 模型工作台 / 调度策略 / 反馈优化 router.html · 交互数据 / 路由数据 dashboard.html · 用户端对话 chat.html |
| 管理接口 | /api/cards（CRUD + transition + echo + debug）· /api/templates（+suggest）· /api/scenarios/rewrite-trigger · /v1/models（注册/回填/状态/默认/凭证）· /v1/policies（+history/rollback/ab）· /v1/bank（health/import）· /api/profile（+rebuild）· /v1/labels/judge · /api/dashboard/* · /api/export/* · /api/brands · /api/audit |
| 运行时接口 | /v1/route（SSE 对话主入口）· /v1/events（埋点准入）· /v1/cards/{id}/responses（回显聚合）· /v1/group/vote + state（群体决策） |
| 存储 | cards / card_snapshots / card_refs / events / labels / traces / models / policies / policy_history / group_votes / audit / brand token 文件 |

核心数据流：管理员配置（cards）→ 发布生成不可变快照（card_snapshots）→ 对话命中触发（/v1/route 关键词匹配 ≥0.35 + 有效期过滤）→ 用户提交（/v1/events 准入）→ 回显（/v1/cards/{id}/responses）+ 看板（/api/dashboard/*）+ 标签（labels）→ 画像重建（profile）→ 路由更准。

---

## 一、组件工作台（列表与生命周期）

### 能力拆解
| 能力 | 前端 | 后端 | 关键字段 |
|---|---|---|---|
| KPI 总览 | 4 个 stat-tile | GET /api/dashboard/questions + GET /api/cards | rendered / submitted / echo_results / status+version |
| 待办条 | todoStrip | 同上 | stale_refs、完成率<60% |
| 列表与筛选 | 状态 tab + 搜索（250ms 防抖） | GET /api/cards?q= | name/trigger_description/trigger_examples/prompt 模糊搜索 |
| 行操作 | 回显开关 / 编辑 / 下线·上线·发布 / 更多 | POST /transition、POST /echo | status、echo_results |
| 版本历史 | 弹窗 | GET /api/cards/{id}、POST rollback、POST upgrade-refs | snapshots、referenced_by、lock_version |
| 触发调试 | 测试抽屉（testchat.js） | POST /v1/route | trigger 匹配得分、HIT_THRESHOLD 0.35 |

### 状态流转（服务端状态机）
```
draft(v0) --publish--> published(v1) --offline--> offline --publish--> published(v2)
published --PUT 编辑--> draft(v≥1，线上继续服务最新快照) --publish--> published(v+1)
非 published --delete--> deleted（引用中需 force，运行时降级纯文本）
rollback: 以历史快照生成新版本立即发布
```
前端展示口径（本轮已统一）：**有生效快照（published 或 draft 且 v≥1）即「已上线」**；KPI、状态 tab、看板同源。

### 问题清单
| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L3 | P1 | **线上服务中的配置可被 API 直接删除**：draft(v≥1) 的卡线上仍在服务快照，但服务端 delete 只拦 status=published，draft 态删除不需 force、不需先下线。UI 已隐藏入口，接口层不设防 | transition.delete 的拦截条件改为「有未归档快照」而非 status==published |
| L2 | P1 | **重名校验只在创建时**：create_card 校验租户内唯一，update_card 改名不查重 → 改名可撞名，破坏唯一性承诺与按名搜索 | update_card 增加同名检查（排除自身） |
| L5 | P1 | **card_submitted 无去重**：feedback_given 有同 turn 去重 + 单日上限，但组件提交事件无 render_id 幂等 → 重放请求可刷分布 / 看板 / 回显 | events.ingest 对 card_submitted 按 (user, render_id) 去重 |
| L10 | P2 | 触发条件冲突无预警：两条配置触发描述相近时，创建 / 发布环节无「与现有配置相似度过高」提示，只能事后在测试抽屉发现 | 发布前调用 match_cards 自检，相似度>阈值给非阻断警告 |
| C4 | 建议 | 用户端组件底部溯源「配置 xxxx · v1」对终端用户无意义，管理侧才需要 | 默认隐藏，悬停或调试模式展示 |
| C11 | 建议 | 「组件触发」KPI 与看板「组件触发分布」中混入呈现型组件（折线图 / 引用卡等），与 12 类采集组件并列易混 | 分布图分组为「采集组件 / 呈现组件」 |

## 二、交互配置编辑器（五步弹窗）

### 能力拆解与字段
| 步骤 | 字段 | 联动 |
|---|---|---|
| 1 选择组件 | 业务场景（textarea，内嵌「AI 自动匹配组件」）、组件单选网格 | 场景描述 → suggest 接口返回推荐 + 抽取选项预填；选中组件 → applyTemplate 灌入模板默认内容；场景文案带入第 2 步触发条件初稿；未选组件下一步置灰 |
| 2 触发条件 | 配置名称*、触发条件*（AI 改写：先预览采用才写入）、示例问法（tag ≤10）、有效期开关 + 起止日期 | 改写建议不直接覆盖原文；示例问法第 1 条作为预览用户气泡；有效期关闭即清空日期 |
| 3 配置组件 | 组件下拉（编辑态锁定）、选择方式（单/多选，编辑态锁定）、显示样式 segmented、提问*（加重样式）、说明文案、内容区（选项行 = 文案 + 跳转链接 + AI 跟进 / 卡片块 / 字段行 / 刻度 / 滑杆 / placeholder）、按钮文案 | 切组件 → applyTemplate 重灌内容并重绘；选项文案变更 → 推荐默认项下拉联动；显示样式 → 右侧预览实时切换；卡片配图 ≤200KB 转 dataURL |
| 4 确认 | 摘要表（逐项「修改」回跳）、回显开关、Webhook | 摘要与 state 同源；错误若在其他步骤自动跳回 |
| 5 获取 Key | 配置 ID、嵌入代码、接口契约（均可复制） | 保存 / 发布成功后进入；关闭清空本地草稿 |

字段校验分工：客户端 clientValidate（名称 / 触发 / 提问 / 选项≥2 且不重复 / 链接 https / 配图格式 / 滑杆区间 / 字段 key 格式与唯一 / Webhook https / 有效期先后）→ 每步只拦本步字段；服务端 validate_card（名称长度 / 组件类型合法 / 触发必填 / 示例≤10 / 模板括号配对 / style token 白名单 / group_mode 合法性）。

### 问题清单
| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L1 | P0 | **服务端校验远弱于客户端**：选项数量 / 重复、滑杆区间、字段 key 格式、Webhook https、有效期先后 全部只在前端校验。绕过页面直连 API 可保存并**发布**脏配置（如 0 个选项的单选器），线上渲染直接空组件 | validate_card 补齐与 clientValidate 对等的内容校验（发布时强校验即可） |
| L6 | P1 | **接口契约承诺未兑现**：契约展示 `signature: HMAC-SHA256(payload, secret)`，但平台无 secret 的生成 / 展示 / 轮换能力；Webhook 地址仅存储，服务端无投递逻辑（含演示模拟）。「演示不外发」文案只在编辑器出现，契约文本本身未标注 | 契约内注明「签名密钥在服务端配置（演示环境未启用）」；或补一个租户级 secret 管理字段 |
| L8 | P1 | **群体决策是隐藏能力**：协议、服务端（quorum / sealed / revisable / 聚合规则）、校验齐备，但编辑器没有任何配置入口，group_mode 永远为空 → 能力不可运营 | 要么在编辑器数据回收区加「群体决策」配置块，要么从协议文档中降级为预留能力 |
| L9 | 已修 | 编辑态可切换单/多选，但服务端禁止 component_type 变更 → 保存必 422（本轮引入，本轮修复：编辑态锁定选择方式并说明原因） | — |
| L11 | P2 | 能力孤儿字段：style_overrides（服务端有 token 白名单校验）与 validation 字段无任何 UI 入口 | 从 payload 中移除或补 UI |
| L7 | P2 | 幂等创建不校验归属：POST /api/cards 带已存在 card_id 直接返回该卡完整配置（单租户 demo 无实害，多租户会成为越权读取口） | 幂等命中时校验 tenant_id |
| C3 | 建议 | 「获取 Key」步骤名与内文「配置 ID（Key）」双名并存 | 统一：步骤名「获取配置 ID」或内文只叫 Key，二选一 |
| C5 | 建议 | 触发条件的 AI 改写仍是紫色魔法棒 icon-only 按钮，与第 1 步已改的「AI 自动匹配组件」文字按钮不同款（此前已明确不喜欢 AI 紫配色按钮） | 统一为「AI 改写」文字按钮内嵌输入框右下角 |
| C13 | 建议 | 步骤名「选择组件 / 配置组件」字面过近，扫读易混 | 第 3 步改「设计内容」或「填写内容」 |

## 三、组件库

能力：12 类采集组件真实对话式陈列（单一默认样式）+ 类型分段筛选 + 搜索 + 「创建配置」带模板预填深链（?new=1&ct=）。字段：无表单，纯陈列。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L-lib1 | P2 | 「创建配置」经 localStorage 传递 prefill，同时存在 ?ct= 深链两条通路，行为一致但实现冗余 | 收敛到 ?ct= 一条通路 |
| C-lib1 | 建议 | 卡底「可选样式：列表 / 胶囊 / 输入框浮现（创建配置时选择）」信息价值高但一行灰字易被忽略 | 可改为小 chip 排列 |

## 四、导入品牌风格

能力：预设风格卡（点击即预览）+ 导入设计风格参数（JSON token，生成预览 / 应用并保存）+ 12 类组件示例网格。**作用域已收敛：token 只写入 .brand-scope（渲染出的组件），平台界面不受影响。**

字段联动：预设卡点击 → fetch token → Brand.apply（组件级）→ 示例网格实时变化 → pending 状态 → 应用并保存才落盘 / 记忆；导入 JSON → 校验必需项（color.primary / bg_page / bg_surface / text_primary）→ 生成预览同上；保存走 POST /api/brands（brand_id 格式、default 不可覆盖、色值正则校验）。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L-brand1 | P1 | **生效风格是「设备级」而非「租户级」**：brand_file 记在 localStorage，换浏览器 / 同租户其他管理员看到的是各自的风格；「应用并保存，对所有渲染出的组件生效」文案与实际（仅本机记忆选择）不符。客户前端嵌入组件更无从读取该选择 | 服务端加「当前生效风格」租户级设置，页面与嵌入组件统一读取 |
| L-brand2 | P2 | 已导入品牌不可删除 / 重命名，导错只能不断新增文件 | 预设卡「更多」加删除（default 除外） |
| C1 | 建议 | 导航名「导入品牌风格」是动词短语且以偏概全（页面实际是组件风格管理：预设 + 导入） | 建议改「组件风格」，页内保留「导入设计风格参数」入口 |
| C-brand1 | 建议 | token 校验失败文案技术化（「JSON 解析失败：Unexpected token…」） | 前置一句人话 +「查看示例」快捷填入 |

## 五、对话测试（测试抽屉 + 用户端 chat）

能力：像用户一样提问 → /v1/route SSE 流（过程步骤 + final）→ 命中配置出组件（含降级链）→ 提交回流（含选项后续动作：AI 跟进 prompt 改写续轮 query、服务链接记 tool_call span 模拟调用）→ 赞踩 / 择优 / 隐式行为埋点。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L4 | P0 | **回显开关在服务端不生效**：/v1/cards/{id}/responses 不校验 echo_results、无鉴权，还返回 recent_texts（用户长文本原样）。关掉回显只是前端不请求，接口裸奔可拉全量分布与文本 | 接口校验卡片 echo_results，关闭时 403；recent_texts 做脱敏或摘要 |
| L-chat1 | P2 | 触发命中只取 top1，competitors 仅记录 trace；两条相近配置的实际竞争关系用户侧不可感知（与 L10 同源） | 同 L10 |
| C-chat1 | 建议 | 组件提交后的冻结态无「已提交」明示，只靠回显面板出现来暗示 | 提交按钮变「已提交」禁用态 |

## 六、模型工作台

能力与字段：注册（model_id / 显示名 / 厂商 / endpoint / vault:// 凭证 / 入出单价 / 能力）→ registering → bank 回填（进度 / 成本预估 / 暂停续跑）→ 覆盖率 100% 才可 active → 暂停 / 上线 / 设默认兜底 / 删凭证（转 paused）。默认兜底不可暂停 / 退役 / 删凭证（前端禁用 + 服务端 409）。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L16 | P1 | 注册接口 price float() 无范围校验（负数 / NaN 字符串直接 500 或入库），能力 / 延迟字段同样裸收 | 服务端补数值范围校验，422 返回字段级错误 |
| L14 | P2 | retired（退役）状态服务端支持、UI 无入口；「删除凭证」实际行为是「删凭证 + 暂停模型」，按钮语义偏轻 | UI 补退役入口或删状态；按钮文案改「删除凭证并暂停」 |
| C6 | 建议 | 「凭证」列 vault://cred****-4b 整列 mono 技术痕迹重，扫读干扰 | 列改「已配置 / 未配置」chip，完整引用悬停或收进更多 |
| C12 | 建议 | 注册表单 model_id 格式提示是正则式 | 改「2-24 位小写字母、数字或短横线」 |
| C2 | 建议 | 「模型工作台」页面实际只有一张接入表，名称过重 | 「模型接入」更贴切（或维持名称、补内容） |

## 七、调度策略（画像 + 路由规则）

能力：质量-成本 α 滑杆（即时重算矩阵，重建后新版本才用于线上）→ 路由画像矩阵（簇 × 模型综合分 + 路由去向）→ 重建画像（任务态轮询）→ 路由规则卡（业务参数摘要 + 右侧抽屉参数配置，版本化）。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L12 | P0 | **update_policy 服务端零校验**：explore_ratio 任意 float（前端限 0-1）、budget_cap / params 任意结构、非数字入参直接 500；策略是线上路由的直接输入，坏值即坏路由 | 服务端补范围校验（explore 0-1、预算非负、latency_tier 枚举） |
| L13 | P1 | A/B 实验 UI 已移除但接口与分流逻辑保留：resolve_policy 仍按 ab_group 分流，若存量实验在跑，无处可见也无处可停 | 保留接口可以，但在路由规则页对「进行中的实验」给只读提示 + 停止入口 |
| L15 | P2 | profile rebuild 无并发闸：重复 POST 重置任务进度（前端已禁用按钮，接口不设防） | running 态直接 409 |
| C8 | 建议 | 矩阵数字（0.65）无度量说明；「重建画像」（本页）与「更新画像」（反馈优化页）同一动作两个名字 | 表头加 ⓘ「综合分 = α×性能 + (1-α)×效率」；动作名统一为「更新画像」 |
| C-disp1 | 建议 | 无模型 / 无数据集时矩阵区域没有空态设计 | 补空态：「先在模型工作台接入模型」 |

## 八、反馈优化

能力（本轮已删人工复核）：收集反馈（赞踩 / 择优 / 组件提交 / 隐式行为自动变标签入库）→ 更新画像；高级工具：批量导入标注（Tab 分隔解析）、AI 评审补标（伪标签 0.5 直接并入）。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L-flow1 | P1 | 删除人工复核后，AI 评审伪标签（置信 0.5）与隐式行为低置信标签**无任何质量闸门直接参与画像**，此前「低置信需复核」的数据质量承诺失效 | 画像重建时按置信度加权（低置信降权），并在页面注明 |
| C-flow1 | 建议 | 步骤 1「收集反馈 89」数值是标签总量，与步骤语义（正在收集）静态并置，缺变化感 | 副文案改「近 30 天新增 N 条」 |

## 九、数据分析（交互数据 / 路由数据）

能力：交互数据（KPI + 埋点健康 + 每配置卡片分布 / 漏斗 / 触发分布 / 默认项接受）+ 路由数据（请求 / 成本 / 聚合率 / 快车道 / 分布 / 延迟分位对验收线 / Token / 质量 / 标签产出 / 失败降级）+ 筛选下钻 + CSV（与图表同查询口径）。

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L17 | P2 | 种子数据 rendered=0 而 submitted>0：KPI 已用「数据不全」兜底，但同页漏斗显示触发 66 次，两个数据源（漏斗全局 events vs 卡片行）矛盾并存无解释 | 卡片行完成率「-」时补 title 说明口径 |
| C7 | 建议 | 「模型多样性 2.365 / 健康」数值与判定词并置突兀；快车道 / 聚合率有 ⓘ 但判定线未写 | 统一「数值（判定）」格式并在 ⓘ 中写判定线 |

## 十、平台通用

| 编号 | 级别 | 问题 | 建议 |
|---|---|---|---|
| L-evt1 | 肯定 | 事件准入设计是全场最严谨的一段：event_id 幂等、同 turn 同类反馈去重、单日反馈上限、user_id 假名化——但（见 L5）card_submitted 不在保护范围 | 补 L5 即闭环 |
| C10 | 建议 | icon 取舍全站规则建议明文化：破坏性 / 主操作一律文字按钮；高频辅助操作（更多 / 复制 / 编辑入口）icon + title；当前唯一越界的是编辑器 AI 改写（见 C5） | 写进交互规范第七章 |

---

## 十一、超级问题清单（汇总，按严重度排序）

| # | 模块 | 级别 | 问题（一句话） |
|---|---|---|---|
| L1 | 配置编辑器 | P0 | 服务端内容校验缺失，API 直连可发布脏配置（0 选项 / 坏区间 / 非法 Webhook） |
| L4 | 回显 | P0 | responses 接口不校验回显开关且返回用户原文，开关只在前端生效 |
| L12 | 调度策略 | P0 | 策略更新接口零校验，坏参数直接进线上路由 |
| L2 | 工作台 | P1 | 改名不查重，租户内唯一承诺可被打破 |
| L3 | 工作台 | P1 | 线上服务中的配置（编辑态草稿）可被 API 无阻拦删除 |
| L5 | 埋点 | P1 | 组件提交事件无幂等去重，分布 / 看板 / 回显可被重放刷量 |
| L6 | 数据回收 | P1 | 契约展示的 HMAC 签名与 Webhook 投递无实现支撑，承诺大于能力 |
| L8 | 配置编辑器 | P1 | 群体决策能力前端无入口，成为不可运营的隐藏能力 |
| L13 | 调度策略 | P1 | A/B 分流逻辑仍在跑但 UI 已无观察与停止入口 |
| L16 | 模型工作台 | P1 | 模型注册数值字段服务端不校验范围 |
| L-brand1 | 品牌风格 | P1 | 生效风格记在浏览器本地，不是租户级设置，多端不一致且嵌入端读不到 |
| L-flow1 | 反馈优化 | P1 | 删除复核后低置信伪标签无质量闸门直接参与画像 |
| L7 | 配置接口 | P2 | 幂等创建命中已有 card_id 时不校验租户归属 |
| L10 | 触发 | P2 | 相近触发条件无冲突预警，只能事后调试发现 |
| L11 | 配置编辑器 | P2 | style_overrides / validation 为无 UI 的孤儿字段 |
| L14 | 模型工作台 | P2 | retired 状态无 UI；删凭证实际会暂停模型，语义偏轻 |
| L15 | 调度策略 | P2 | 画像重建任务无并发闸 |
| L17 | 交互数据 | P2 | 漏斗与卡片行两个数据源的口径矛盾无解释 |
| L-lib1 | 组件库 | P2 | 模板预填双通路冗余 |
| L-brand2 | 品牌风格 | P2 | 已导入风格不可删除 / 重命名 |
| L-chat1 | 对话测试 | P2 | 触发竞争关系仅 trace 可见 |
| L9 | 配置编辑器 | 已修 | 编辑态切换单/多选与服务端锁定冲突（本轮修复：编辑态锁定） |
| C1 | 导航 | 文案 | 「导入品牌风格」动词短语且以偏概全，建议「组件风格」 |
| C2 | 导航 | 文案 | 「模型工作台」名重于实，建议「模型接入」 |
| C3 | 配置编辑器 | 文案 | 「获取 Key」与「配置 ID（Key）」双名不统一 |
| C4 | 用户端组件 | 文案 | 组件底部溯源信息对终端用户无意义，建议悬停展示 |
| C5 | 配置编辑器 | 文案 | AI 改写仍是紫色 icon 按钮，与「AI 自动匹配组件」不同款 |
| C6 | 模型工作台 | 文案 | 凭证列 vault:// 整列技术痕迹重，建议 chip + 悬停 |
| C7 | 路由数据 | 文案 | 「2.365 健康」数值判定并置突兀，判定线未写明 |
| C8 | 调度策略 | 文案 | 矩阵分数无度量说明；「重建画像 / 更新画像」同义不同名 |
| C9 | 品牌风格 | 文案 | token 校验失败文案技术化，缺「查看示例」 |
| C10 | 全站 | 文案 | icon 与文字按钮的取舍规则未成文 |
| C11 | 交互数据 | 文案 | 触发分布混排采集型与呈现型组件，建议分组 |
| C12 | 模型工作台 | 文案 | model_id 格式提示是正则式，应写人话 |
| C13 | 配置编辑器 | 文案 | 步骤名「选择组件 / 配置组件」字面过近 |
| C-lib1 | 组件库 | 文案 | 可选样式一行灰字易被忽略，建议 chip 化 |
| C-brand1 / C-flow1 / C-chat1 / C-disp1 | 各页 | 文案 | 错误文案人话化 / 步骤副文案 / 提交终态明示 / 矩阵空态 |

## 十二、总体判断

架构主线是通的：**配置（快照版本化）→ 触发（阈值 + 有效期）→ 采集（事件准入）→ 回流（标签）→ 画像 → 路由**，状态机、乐观锁、幂等创建、事件假名化这些底层设计有真实产品的严谨度。当前最大的系统性风险只有一类：**校验与承诺集中在前端，服务端裸奔**（L1 / L4 / L12 / L16 / L5）——demo 阶段无碍，一旦有第二个客户端（嵌入 SDK 正是这样的客户端）就会全部暴露。建议下一轮集中做「服务端对齐」：把 clientValidate 复制到 validate_card、给 responses / delete / policy 三个接口补闸门，即可消掉全部 P0。

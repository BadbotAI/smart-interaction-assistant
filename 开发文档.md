# 智能助手交互与调度平台 — 开发文档

> 面向执行的开发规格。阅读者为工程实现方（含 Claude Code）。
> 版本：v0.1 / 2026-07-29
> 状态：产品设计阶段，本文档为唯一事实源；与任何历史需求文件冲突时以本文档为准。

---

## 0. 如何使用本文档

本文档描述三个相互关联的产品。它们可以分别实现，但共享一套数据契约（见 §5）。

**实现顺序有强依赖，不要跳序**：

```
第 1 步  统一数据契约（§5）          —— 三个产品共同依赖，先冻结
第 2 步  组件协议 + 预置组件库（§2）  —— 定义了回流数据长什么样
第 3 步  卡片管理端（§2.6–2.8）      —— 组件的配置与生命周期
第 4 步  路由器 MVP（§3，不含聚合）   —— 先跑通单模型路由
第 5 步  看板与链路追踪（§4）         —— 观测前两者
第 6 步  聚合路径 + 标签回流闭环      —— 系统开始自我优化
```

反序实现会导致每一环返工：回流字段没定，看板就没有可分析的原料；看板没有质量数据，路由器就没有标签来源。

**本文档的约定**：

- `必须` / `MUST`：不可协商的约束，违反即验收不通过
- `应当` / `SHOULD`：强烈建议，偏离需在实现说明里给出理由
- `可选` / `MAY`：可延后
- 【待定】：尚未拍板的决策，附负责人与截止时间，实现时按标注的**临时默认值**先行，不要自行发明

---

## 1. 项目背景与总体目标

### 1.1 背景

上游客户需求来自国际物流与大宗商品供应链场景（采购寻源、价格研判、运力调度、港航作业、仓储管理、合规审查、气象应对、经营分析）。客户方需求文档描述的是一个完整的行业智能体平台。

**本项目不承担该平台的全部内容。** 本项目交付其中三项通用能力，它们与行业无关，可复用到其他场景与其他品牌方：

| 编号 | 产品 | 一句话定义 |
|---|---|---|
| P1 | **富交互组件** | 让对话助手能在对话流中动态渲染结构化交互组件的 SDK / MCP 工具集 |
| P2 | **模型路由器** | 基于 JiSi 框架的多模型调度产品工程框架 |
| P3 | **数据看板与链路追踪** | P1 与 P2 的观测面 + 策略控制台 |

### 1.2 三者的关联关系（重要，决定架构）

三者不是并列的三个工具，而是一个闭环：

```
        ┌──────────────────────────────────────────────┐
        │                                              │
        ▼                                              │
  ┌───────────┐   交互事件 / 质量标签   ┌────────────┐  │
  │    P1     │ ─────────────────────▶ │     P2     │  │
  │ 富交互组件 │                        │  模型路由器 │  │
  │           │ ◀───────────────────── │            │  │
  └───────────┘   路由决策 / 模型回答   └────────────┘  │
        │                                    │         │
        │  交互事件                 路由决策/成本       │
        ▼                                    ▼         │
      ┌────────────────────────────────────────┐       │
      │                  P3                    │       │
      │        数据看板 + 链路追踪              │───────┘
      │                                        │  策略下发
      └────────────────────────────────────────┘
```

关键因果链，实现时必须理解：

1. **P2 的效力完全建立在「每个模型在每类问题上表现如何」的标签数据上**（详见 §3.2）。在公开 benchmark 上这个标签来自 ground truth；在真实业务里没有 ground truth。
2. **P1 是这个标签的生产装置**。用户点赞点踩、在多个模型回答中择优、接受还是修改了模型给出的推荐选项、群体投票的分布——这些都是监督信号。
3. **P3 既是观测面，也是策略控制台**：它把 P1 产生的信号聚合成 P2 可消费的标签，同时把 P2 的超参暴露成可运营、可 A/B、可回滚的配置。

因此对外的产品叙事是：**一个能自己产生监督信号、并用这些信号持续优化模型调度的对话系统**。

### 1.3 交付物清单

| 产品 | 交付物 | 形态 |
|---|---|---|
| P1 | 组件协议 Schema | JSON Schema 包，独立发布，P1/P2/P3 共同依赖 |
| P1 | 预置组件库 | 前端组件库（含渲染器、事件总线、token 注入） |
| P1 | MCP Server | 供模型侧调用的工具集 |
| P1 | 卡片管理端 | Web 控制台（卡片 CRUD、发布、调试） |
| P1 | 回流 SDK | 埋点与事件上报客户端 |
| P2 | 路由服务 | 服务端，含离线 bank 构建与在线推理 |
| P2 | 模型注册与策略管理 | Web 控制台 + API |
| P3 | 运营看板 | Web 端 |
| P3 | 链路追踪 | Web 端 |
| P3 | 数据管道 | 事件接入 → 清洗 → 聚合 → 标签生成 |

### 1.4 部署形态

必须同时支持：

- **SaaS 多租户**：接入统一认证与租户管理，租户间数据物理隔离
- **私有化部署**：单租户，可能无公网出口

**两种形态功能不完全一致**，必须显式维护差集（见 §6.2 非目标）。任何依赖公网的能力（外部模型 API、公共 embedding bank 更新）在私有化下降级，降级行为必须在产品里显式呈现，不能静默失败。

### 1.5 判断做对了的指标

上线 30 天后用以下数字验收：

| 指标 | 验收线 | 归属 |
|---|---|---|
| 组件触发准确率（模型选中的组件与人工标注一致的比例） | ≥ 80% | P1 |
| 组件交互完成率（渲染后用户完成交互 / 渲染总次数） | ≥ 60% | P1 |
| 路由首 token 延迟 P95（快车道） | ≤ 2s | P2 |
| 端到端延迟 P95（聚合路径） | 【待定 — 需实测 JiSi 基线后确定，责任人：算法侧，截止：立项后 2 周】临时按 ≤ 25s | P2 |
| 相同质量下的成本相对单一强模型 | 下降 ≥ 25% | P2 |
| 每日新增有效质量标签数 | ≥ 【待定，依赖 DAU，责任人：PM，截止：客户 DAU 确认后】 | P1→P2 |
| 看板成本数据延迟 | ≤ 5 min | P3 |

---

## 2. 产品一：富交互组件

### 2.1 产品目标

让对话助手在纯文本之外，能根据信息形态与交互意图，在对话流中动态渲染结构化组件；并把用户在组件上的行为结构化回流。

**核心约束：这是一个泛场景能力工具。** 交互样式、业务文案、字段结构需随对接的品牌方调整，且调整过程**不得修改代码、不得修改 prompt、不得重新训练模型**。

### 2.2 分层架构（决定一切的设计）

必须实现为四层，且**模型只能感知最上面一层**：

| 层 | 内容 | 变化频率 | 谁定义 |
|---|---|---|---|
| **语义层** | 交互行为的抽象契约（「这是一个从 N 选 1 的决策」） | 永不变 | 本项目，代码交付 |
| **结构层** | 字段、约束、状态机、回流数据格式 | 极少变 | 本项目，代码交付 |
| **业务层** | 行业默认字段、文案模板、选项来源 | 每个客户一套 | 配置，preset 包 |
| **表现层** | 色彩、圆角、字体、间距、密度、图表配色、动效 | 每个品牌一套 | 配置，design token |

**MUST：模型的输出中不得出现任何与视觉表现相关的内容。** 模型只输出「我需要一个单选，选项是 A/B/C，提示文案是 X」，绝不输出颜色、尺寸、位置、组件库名称。违反这一条，每接一个新品牌都要重做 prompt 工程。

对应到实体上是两级：

```
预置组件（Component）  —— 语义层 + 结构层，代码内置，租户不可编辑
      ↓  租户基于组件配置出实例
卡片（Card）           —— 业务层 + 表现层，平台配置，可编辑/复制/发布
```

### 2.3 交互范式全集

范式按**信息交换行为**分类，不按组件长相分类。共四类。

#### 2.3.1 呈现型（模型 → 用户，无用户输入）

由**信息的结构形态**决定选哪个组件。此映射是确定性的，可用规则实现。

| component_type | 适用信息结构 | 关键参数 |
|---|---|---|
| `text.emphasis` | 单值、需强调的结论 | value, unit, tone |
| `metric.card` | 标量指标（可带同比/环比） | value, unit, delta, baseline |
| `list.ordered` | 有序序列 | items[] |
| `timeline` | 带时间点的有序事件 | events[{ts, title, desc}] |
| `steps` | 流程的当前进度 | steps[], current_index |
| `table` | 实体 × 属性二维结构 | columns[], rows[], sortable, pagination |
| `chart.line` | 数值沿连续维（通常是时间）变化 | series[], x_axis, y_axis |
| `chart.area` | 同上，强调累积量 | 同 line |
| `chart.bar` | 类别间数量比较 | categories[], series[] |
| `chart.pie` | 部分与整体（类别 ≤ 7） | slices[] |
| `chart.stacked` | 部分与整体 + 沿维度变化 | 同 bar + stack_by |
| `chart.scatter` | 两变量相关性 | points[], x, y, size, color |
| `chart.range` | 数值的不确定性区间 | series[{x, low, high, mid}] |
| `chart.box` | 分布与离群 | groups[] |
| `tree` | 层级 / 包含关系 | nodes[] |
| `treemap` | 层级 + 数量占比 | nodes[] |
| `graph.network` | 多对多关联 | nodes[], edges[] |
| `map.geo` | 空间 / 地理分布 | markers[], regions[], routes[] |
| `gantt` | 时间段占用 | tasks[{start, end, resource}] |
| `calendar` | 时间点集合 | events[] |
| `matrix.compare` | **多方案 × 多维度权衡** | options[], dimensions[], values[][] |
| `flow.reasoning` | **过程 / 因果链条（推理轨迹）** | nodes[], edges[], evidence_refs[] |
| `citation.card` | 有外部依据的陈述 | claim, sources[{title, url, snippet, confidence}] |

**`matrix.compare` 与 `flow.reasoning` 优先级最高。** 前者是供应链等 B 端场景最高频的决策形态（方案比选）；后者是"可解释性"这一核心价值主张的唯一界面载体，配合 `citation.card` 使用。

#### 2.3.2 采集型（用户 → 模型，产生回流数据）

由**用户要做的决策的数学形态**决定选哪个组件。这一类是标签的主要来源。

| component_type | 决策形态 | 回流数据形态 |
|---|---|---|
| `select.single` | 有限集选一 | 类别值 |
| `select.card` | 有限集选一（选项信息量大） | 类别值 |
| `select.multi` | 有限集选多 | 集合 |
| `rank.priority` | 有限集排序 | 全序 / 偏序 |
| `slider.range` | 连续区间取值 | 标量 |
| `scale.likert` | 两极之间取态度 | 有序标量 |
| `form.structured` | 提供多字段结构化输入 | 对象 |
| `input.followup` | 自由文本补充 | 文本 |
| `picker.datetime` | 时间点 / 区间 | 时间 |
| `picker.location` | 空间点 / 区域 | 坐标 / GeoJSON |
| `upload.file` | 提供外部实体 | 文件引用 |
| `matrix.compare+select` | **权衡后取舍（呈现+采集复合）** | 类别值 + 权衡痕迹 |

最后一项回流数据质量最高：它同时记录「用户选了什么」和「用户是在什么信息面前选的」，可直接用于 P2 的偏好标签。

#### 2.3.3 控制型（用户 → 系统，对流程本身操作）

| component_type | 用途 |
|---|---|
| `control.confirm` | 高风险动作前的闸门（写操作、外发、付费调用） |
| `control.interrupt` | 中断当前生成 |
| `control.retry` | 重试 / 换模型重答 |
| `control.branch` | 沿当前方向继续 vs 换方向 |

#### 2.3.4 评价型（用户 → 系统，质量元数据）

| component_type | 回流信号 | 送往 |
|---|---|---|
| `feedback.binary` | 点赞 / 点踩 | P2 标签 + P3 看板 |
| `feedback.preference` | **多个模型回答中择优** | P2 标签（质量最高，pairwise） |
| `feedback.span` | 对回答的某一片段表态 | P3 看板，P2 可选 |

**MUST：`feedback.binary` 必须区分两种语义** ——「答得对不对」（能力标签）与「答得合不合我意」（偏好标签）。两者混在一个赞里会污染路由（能力强但风格不合的模型会被误降权）。实现上至少提供两个维度或在点踩后追问一次原因分类。

### 2.4 群体决策：正交维度，不是组件

**MUST：不要把群体决策实现为一个独立组件。** 它是任何采集型组件都可以开启的模式开关。

开启多主体模式后，卡片必须额外定义：

| 配置项 | 取值 | 说明 |
|---|---|---|
| `participants` | `session_all` / `explicit_list` / `role_based` | 参与者集合的确定方式 |
| `aggregation_rule` | `majority` / `unanimous` / `weighted` / `moderator` | 聚合规则 |
| `visibility` | `realtime` / `sealed` | 实时可见会产生从众效应，封闭后揭晓不会。这是产品决策，必须可配 |
| `deadline` | `{quorum: n}` / `{timeout_sec: n}` / `moderator` | 截止条件 |
| `revisable` | bool | 已投票是否可改 |
| `feedback_to_model` | `result_only` / `distribution` | 回给模型的形态 |

**`feedback_to_model` 默认必须是 `distribution`。** 只给最终结论，模型无法识别团队存在分歧；给分布（「3 人选 A，2 人选 B」），模型才能主动指出分歧并追问，这是群体决策的产品价值所在。

并发与一致性要求：
- 同一卡片实例的多人投票 MUST 通过服务端串行化，禁止前端合并
- 参与者中途离开、超时未投票 MUST 有明确语义（计入弃权 / 不计入分母，可配）
- `sealed` 模式下未揭晓前，任何客户端 MUST NOT 能取到他人选择

### 2.5 触发逻辑：谁决定出什么组件

按四类分工，**不要用同一套机制**：

| 类别 | 触发机制 | 理由 |
|---|---|---|
| 采集型、控制型 | **模型自主（MCP tool call）** | 只有模型知道自己缺什么信息、何时需要确认 |
| 呈现型 | **后置判定** | 内容已生成，选图表是纯数据形态判断，用规则或小模型即可，不占主模型能力也不烧 token |
| 评价型 | **系统固定注入** | 不由模型决定，按策略挂在回答上 |
| 全部 | **协议渲染兜底** | tool call 失败或 schema 非法时降级为纯文本 |

这个分工同时解决 token 成本问题：若所有组件都作为 tool 暴露，工具描述会占据大量上下文并稀释模型的选择准确率。把呈现型摘出去，暴露给模型的工具数可减少一半以上。

**MCP 工具设计 MUST 收敛，不要一个组件一个 tool。** 建议三个工具：

```
render(card_id | component_type, params)   —— 呈现型（模型可主动调，也可后置注入）
ask(card_id | component_type, params)      —— 采集型，阻塞等待用户输入
confirm(action_desc, risk_level, params)   —— 控制型确认闸门
```

工具数量爆炸会显著降低模型的工具选择准确率，这是已知问题。

#### 后置判定的降级链

呈现型的自动选择按以下顺序，前一步失败落到下一步，**任何一步都不得导致空白或报错**：

```
1. 模型显式调用 render()          → 按指定组件渲染
2. 结构化数据检测 + 规则匹配       → 按 §2.3.1 映射表选组件
3. 轻量分类模型判定                → 【待定：是否引入，责任人：算法侧，截止：MVP 后】
4. 降级为 Markdown 纯文本渲染      → 永远可用的兜底
```

### 2.6 卡片对象模型

卡片是租户可配置的实例。字段表：

| 分组 | 字段 | 类型 | 约束 |
|---|---|---|---|
| 标识 | `card_id` | uuid | 系统生成，不可变 |
| | `name` | string | 必填，租户内唯一，≤ 64 字符 |
| | `description` | string | 选填，≤ 256 字符，**仅供人看** |
| | `tenant_id` | uuid | 必填，不可变 |
| 语义 | `component_type` | enum | 必填，取值见 §2.3，**不可变更**（换组件 = 建新卡片） |
| 触发 | `trigger_description` | string | **必填**，≤ 512 字符，**供模型看**，等价于 tool description |
| | `trigger_examples` | string[] | 选填，≤ 10 条，示例问法 |
| | `model_invokable` | bool | 默认 true；false 表示仅可被系统注入 |
| 数据 | `field_bindings` | object | 组件参数 → 数据来源的映射 |
| | `option_source` | enum + config | `static` / `model_generated` / `api` |
| | `validation` | object | 必填性、类型、范围、正则 |
| 交互 | `group_mode` | object \| null | 见 §2.4，仅采集型可用 |
| 表现 | `style_overrides` | object | design token 覆盖，禁止任意 CSS |
| | `text_templates` | object | 文案模板，支持变量插值 |
| 回流 | `emit_fields` | string[] | 哪些字段进入回流事件 |
| | `emit_targets` | enum[] | `model` / `dashboard` / `label_store` 的组合 |
| | `label_polarity_map` | object \| null | 用户选择 → 标签极性的映射规则 |
| 生命周期 | `status` | enum | 见 §2.7 |
| | `version` | int | 每次发布 +1 |
| | `published_at` | ts | |
| | `referenced_by` | agent_id[] | 只读，反查 |

#### 关于 `trigger_description`

**这是整个 P1 最关键的一个字段。** 它是卡片与模型之间唯一的接口，决定了模型是否会在正确时机选中这张卡。

MUST 配套实现调试能力：管理端提供一个输入框，输入一句用户问法，实时返回「模型是否命中这张卡 / 命中了填入什么参数 / 与哪些卡片产生了竞争」。

没有这个闭环，运营配了卡片而模型不用时，收到的反馈只会是「这功能没效果」，根因无法定位。

#### 关于 `emit_targets` 与 `label_polarity_map`

这两个字段是 P1 → P2/P3 的物理接口。`label_polarity_map` 定义如何把用户行为翻译成质量标签，例如：

```json
{
  "component_type": "feedback.preference",
  "label_polarity_map": {
    "rule": "pairwise",
    "positive_for": "$.selected_model_id",
    "negative_for": "$.unselected_model_ids",
    "confidence": 0.9,
    "label_kind": "capability"
  }
}
```

`label_kind` MUST 取 `capability` 或 `preference` 之一（对应 §2.3.4 的两种语义）。

### 2.7 卡片状态机

**MUST 实现完整状态机，只有 happy path 视为未实现。**

| 状态 | 前端标识 | 触发进入 | 可执行操作 | 退出条件 |
|---|---|---|---|---|
| 草稿 | `draft` | 新建卡片；编辑已发布卡片 | 编辑、删除、调试、发布 | 发布 → `published`；删除 → 终态 |
| 已发布 | `published` | 草稿发布成功 | 编辑（产生新草稿版本）、复制、下线、查看引用 | 下线 → `offline`；被新版本取代 → `archived` |
| 已下线 | `offline` | 运营主动下线 | 重新发布、删除 | 重新发布 → `published`；删除 → 终态 |
| 历史版本 | `archived` | 被更高版本取代 | 查看、回滚 | 回滚 → `published`（生成新版本号） |
| 已删除 | `deleted` | 删除操作 | 无 | 终态 |

状态迁移的触发者只能是：用户操作、系统事件、运营操作、定时器。**「用户查看了页面」不得作为迁移触发器。**

#### 版本引用语义（必须拍板，影响结果可复现性）

**决策：Agent 引用的是发布时的版本快照，不是最新版。**

理由：可解释性要求结论可追溯，追溯的前提是当时所用卡片版本可还原。

由此产生的必须实现项：
- 每次发布 MUST 生成不可变快照并长期保留
- 卡片更新后，管理端 MUST 显示「N 个 Agent 仍在使用旧版本」并提供批量升级入口
- Trace 记录（§4.3）MUST 记录当次使用的 `card_id + version`

#### 删除的级联规则

删除一张被引用的卡片时：
- MUST 阻断删除并列出引用方，除非运营显式选择「强制删除」
- 强制删除后，运行时遇到失效引用 MUST 降级为纯文本，并产生一条 `card_ref_missing` 告警事件
- 历史 Trace 中的快照 MUST 保留，不随卡片删除而消失

### 2.8 卡片管理端流程与异常

创建流程：`选择组件类型 → 配置数据绑定 → 配置触发描述 → 配置样式 → 调试 → 保存草稿 → 发布`

**MUST：展示型卡片与交互型卡片走两套创建流程**，不要试图用一张表单覆盖。交互型需额外配置选项来源、聚合规则、截止条件、回流映射，字段量差 2–3 倍。

异常覆盖（逐条实现，缺一不可）：

| 类别 | 场景 | 要求 |
|---|---|---|
| 网络 | 保存超时、发布无响应 | 幂等提交，重复提交不产生重复卡片；提示含重试入口 |
| 内容 | 触发描述超长、文案模板变量不存在、样式 token 非法 | 保存前校验并定位到具体字段 |
| 内容 | 选项来源 API 返回错误或超时 | 运行时降级为空态提示，不阻塞对话；管理端调试时明确报错 |
| 并发 | 两人同时编辑同一卡片 | 乐观锁 + 版本冲突提示，展示差异，由用户选择合并或覆盖 |
| 并发 | 编辑期间卡片被他人删除 | 保存时提示「卡片已被删除」，提供另存为新卡片 |
| 空态 | 卡片列表为空、搜索无结果、无引用 | 各自独立文案与引导动作 |
| 中断 | 编辑中刷新/退出 | 本地草稿保留，重进时提示恢复 |
| 出错 | 发布失败 | 卡片保持 `draft`，错误提示含发生条件与下一步动作 |

**每条错误提示 MUST 包含：发生条件 + 文案 + 用户下一步动作。** 只有文案没有恢复路径的提示视为未完成。

### 2.9 多品牌适配

品牌切换 MUST 只需替换两个包，不改代码：

```
brand-tokens.json    表现层：色板、圆角、字体族与字号阶、间距阶、密度、
                     图表配色序列、动效时长、组件形状
industry-preset.json 业务层：各组件的默认字段、默认文案模板、
                     行业术语表、默认选项来源
```

示例：`matrix.compare` 在供应链行业的 preset 默认维度为「价格 / 时效 / 风险 / 合规」，在其他行业替换为该行业维度。组件代码与模型 prompt 均不变。

**MUST：样式覆盖只允许通过 token，禁止开放任意 CSS 注入。** 一旦允许任意 CSS，跨品牌的组件一致性和可维护性即刻失控。

### 2.10 P1 的生命周期与保留策略

| 实体 | 保留 | 清理 | 级联 |
|---|---|---|---|
| 卡片草稿 | 无限 | 手动删除 | 无 |
| 卡片发布快照 | ≥ 与 Trace 同期（见 §4.5） | 定时归档至冷存 | 不随卡片删除消失 |
| 卡片渲染实例（会话内） | 随会话 | 会话删除时级联 | 事件已上报的不回收 |
| 上传文件（`upload.file`） | 【待定：责任人 PM，截止立项后 2 周】临时 90 天 | 定时清理 | 删除时同步删除对象存储 |

---

## 3. 产品二：模型路由器

### 3.1 产品目标

把 JiSi 学术框架改造为可运营的产品工程框架：多模型调度、成本可控、决策可解释、策略可运营、数据可回流。

**参考实现**：
- `ZhangYiqun018/AvengersPro`（DAI 2025，arXiv:2508.12631）— 前身，基于聚类的性能-效率路由
- `magent4aci/openJiSi`（ICML 2026，arXiv:2601.01330）— 本项目基线
- `LLMRouterBench`（arXiv:2601.07206）— 公共数据来源，用于冷启动

三者均出自上海人工智能实验室，实现前 SHOULD 直接与作者对齐三件事：延迟实测分布、bank 规模与效果的收益曲线、正确率向量由 0/1 改为加权值后的效果衰减。

### 3.2 JiSi 机制拆解（实现基线）

#### 离线阶段：构建 Embedding Bank

对一批采样 query，让 M 个候选模型各回答一次，记录：

- 问题的 embedding（论文用 `gte-Qwen2-7B-instruct`）
- 每个模型回答的 embedding
- 每个模型的**正确与否 0/1**，构成能力向量 `v_j ∈ {0,1}^N`
- 每个模型回答消耗的 completion tokens `c_ij`

**整个框架 training-free**，"训练集"仅用于构建 bank，不训练任何神经网络。

#### 在线阶段：单次请求的五步

```
Step 1  新 query 求 embedding e，与 bank 内全部问题 embedding 求余弦相似度 s
        取 support set：s_i ≥ γ × (第 N_base 高的相似度)，γ=0.95，N_base=50

Step 2  粗粒度分数 g = v^sup · s^sup   （模型在这批相似题上的加权历史正确率）
        g 最高者 → 聚合器 A_agg
        g 前 K 名 → 候选模型集（K=3）

Step 3  【并发】把 query 发给 K 个候选模型，拿回 K 份回答与各自 completion tokens

Step 4  混合相似度重新过滤 support set：
        s_flt = ε·K·s + σ·s_res + δ·s_cost      ε=0.5, σ=0.3, δ=0.2
          s_res  = 候选模型回答 embedding 与 bank 内该模型历史回答 embedding 的相似度
          s_cost = 本次 token 消耗与 bank 内历史消耗的接近程度（用于推断题目难度）
        保留前 β 比例（β=0.5），得细粒度分数 g_f

Step 5  自适应开关：保留 g_f ≥ t × max(g_f) 的模型，t=0.8
          若剩 1 个 → 直接返回该模型回答（退化为路由）
          若剩 >1 个 → 拼接这些回答交给 A_agg 重写，产出最终回答
        约束：3 份回答 token 总和 > 13000 时，aggregatee 截断为 2
```

三项创新分别对应：Step 3–4（用回答内容与长度推断题目难度，而非只看题面文字相似）、Step 2（聚合器需在领域专精与综合能力间取平衡，故用更大的 support set 选它）、Step 5（阈值剪枝，防止劣质回答把聚合器带偏）。

论文附录的两个失败案例说明了 Step 5 的必要性：有模型答对了，但聚合器被另外两个自信而错误的回答带跑。自适应开关就是在这种情形下放弃聚合、直接信任最强的单个模型。

#### 论文超参默认值（实现时作为初始值）

```
K = 3           候选模型数
N_base = 50     support set 基准大小
β = 0.5         过滤保留比例
γ = 0.95        support set 相似度阈值系数
ε, σ, δ = 0.5, 0.3, 0.2    query / response / cost 相似度权重
t = 0.8         聚合准入阈值
max_agg_tokens = 13000     超过则 aggregatee 截断为 2
```

### 3.3 从论文到产品的五个必须处理项

#### 落差一：这不是省钱的路由器

JiSi **每次请求并发调用 K 个模型**，还可能追加第 K+1 次聚合调用。它相对 Gemini-3-Pro 便宜（论文报告省 53%）是因为开源模型 API 单价低 5–10 倍，不是因为调用次数少。

**MUST：产品的价值主张是「在给定预算下拿到更高的能力上限」，不是「省钱」。** 若客户与「直接用一个便宜模型」对比，成本必然上升。看板必须能呈现「质量-成本」二维对比，而不是单独的成本数字。

#### 落差二：延迟是对话产品的生死线，而论文完全没有测量

一次聚合路径的耗时 ≈ K 个模型中最慢者（含 thinking 模型）+ 回答 embedding + 聚合器读 13000 token 再重写。用户等待的是**两轮串行的慢模型**。

更严重的是：**走聚合路径时无法流式输出**。在拿到全部 K 份回答之前，系统不知道会走路由还是聚合，因此什么都不能先吐给用户。

MUST 实现的缓解机制：

1. **快车道**：低难度 / 闲聊类请求直接 K=1，跳过 Step 3–5。入口判据复用 Step 2 的分数分布（例如 top-1 与 top-2 的 g 差距 > 阈值，或 top-1 的 g 绝对值高于阈值）
2. **等待期填充**：聚合路径的等待时间用 P1 的 `flow.reasoning` 组件实时呈现执行过程（正在询问哪几个模型、已返回几个、当前判断走路由还是聚合）。把工程劣势转为可解释性卖点
3. **档位**：暴露 `极速 / 均衡 / 高质` 三档，映射到不同的 K、t、是否允许聚合，可按场景与租户配置
4. **超时与降级**：任一候选模型超时，MUST 用已返回的模型继续，不等待；全部超时 MUST 降级到默认模型单调

#### 落差三：超参必须变成可运营策略

论文中所有超参都是写死的常数。产品化的实质工作之一，就是把它们变成**按租户、按场景分组、可 A/B、可回滚**的策略配置，且效果在 P3 看板上可见。

#### 落差四：新模型接入成本必须显式暴露

模型池新增一个模型，其能力向量 `v_j` 为空，必须把整个 bank（N 条 query）跑一遍才能参与路由。

**接入一个模型的边际成本 = N 次推理。** MUST 实现为一个带进度、成本预估、可暂停、可断点续跑的「模型入池」任务，而不是改配置文件。入池未完成的模型 MUST NOT 参与线上路由。

#### 落差五：多租户下 bank 必须隔离

企业客户的 query 与 response 均为商业机密。SaaS 下共用单一 bank 等于跨租户泄露。

**MUST 实现双层 bank**：

```
公共底座 bank    公开数据构建（可用 LLMRouterBench），全租户共享，只读
租户增量 bank    租户自有数据，物理隔离
                 MUST 仅向路由计算输出分数，不输出原文内容
```

路由时两层的分数按权重合并，权重可配（租户数据越多，权重越高）。私有化部署下仅有租户层，冷启动更依赖导入与标注工具（§3.5）。

### 3.4 数据模型

#### ModelRegistry

| 字段 | 类型 | 说明 |
|---|---|---|
| `model_id` | string | 内部唯一标识 |
| `display_name` | string | |
| `provider` | enum | 供应商 |
| `endpoint` | string | |
| `credential_ref` | string | **MUST 引用凭证存储，禁止明文入库** |
| `price_input` / `price_output` | decimal | $/M tokens，用于成本计算 |
| `capabilities` | object | `{tool_call, vision, streaming, context_window, thinking}` |
| `status` | enum | `registering` / `active` / `paused` / `retired` |
| `bank_coverage` | float | 已回填的 bank 覆盖率，< 100% 不得上线 |

**MUST：`capabilities` 必须建模。** 路由若不知道模型是否支持 tool call，就无法为需要调用 P1 采集型组件的请求选出可用模型。

**凭证三件套 MUST 全部实现**：加密存储、界面展示脱敏、明确的删除路径。凭证失效或模型下线时，正在进行的会话 MUST 有明确处理（切换到备选模型并在 Trace 中标记，而非静默失败）。

#### EmbeddingBank

```
QueryRecord {
  query_id, tenant_id (公共层为 null), embedding vector,
  text_ref (指向加密存储，不直接存原文), domain_tags[],
  created_at, ttl, source (public | tenant_online | tenant_imported)
}

ResponseRecord {
  query_id, model_id, response_embedding vector, completion_tokens,
  label_value float [0,1],        -- 见下方扩展说明
  label_confidence float [0,1],
  label_source enum,              -- explicit_preference | explicit_binary |
                                  -- implicit_behavior | llm_judge | ground_truth
  label_kind enum,                -- capability | preference
  created_at, updated_at
}
```

**对论文的扩展：`label_value` 由 0/1 扩展为 `[0,1]` 加权值，并附 `label_confidence`。**

理由：真实业务中没有 ground truth，标签来自强弱不一的信号。JiSi 的核心计算式 `g = v · s` 在 v 为实数时依然成立，公式无需改动，但能容纳弱标签。实现时 `g` 的计算 SHOULD 用 `label_value × label_confidence` 作为有效权重。

#### RoutePolicy

| 字段 | 说明 |
|---|---|
| `policy_id` | |
| `scope` | `global` / `tenant` / `scene`，就近覆盖 |
| `K`, `N_base`, `beta`, `gamma`, `eps`, `sigma`, `delta`, `t`, `max_agg_tokens` | JiSi 超参 |
| `latency_tier` | `fast` / `balanced` / `quality` |
| `allow_aggregation` | bool |
| `explore_ratio` | 强制探索流量比例，默认 0.05，见 §3.6 |
| `model_whitelist` | 本策略允许使用的模型子集 |
| `budget_cap` | 配额上限，见 §3.7 |
| `enabled`, `ab_group`, `version` | A/B 与回滚支持 |

#### RouteDecision（每次请求产出，进入 Trace）

```
RouteDecision {
  trace_id, tenant_id, policy_id, policy_version,
  support_set_ids[],              -- 用于可解释性展示
  coarse_scores {model_id: score},
  candidate_models[], aggregator_model,
  model_calls[{model_id, latency_ms, tokens_in, tokens_out, cost, status}],
  fine_scores {model_id: score},
  switch_result: routed | aggregated,
  final_model_or_aggregator,
  is_explore bool,
  total_cost, total_latency_ms
}
```

**`support_set_ids` 与两组 scores MUST 完整记录。** 这是"为什么选了这个模型"可解释性展示的唯一数据来源：界面上呈现「与你的问题相似的历史问题有这些，各模型在其上的历史命中率是这样」。

### 3.5 数据从哪来、怎么回流

这是 P2 的命门。JiSi 需要 `(query, 各模型 response, 各模型对错, token 消耗)` 四元组。**运行时天然可得前两项与第四项，唯一困难是"对错"这一列。**

#### 标签来源与优先级

| 来源 | 信号 | 质量 | 覆盖率 | label_source |
|---|---|---|---|---|
| 多模型回答用户择优（P1 `feedback.preference`） | pairwise preference | 最高 | 低 | `explicit_preference` |
| 点赞点踩（P1 `feedback.binary`） | 二元 | 中 | 中 | `explicit_binary` |
| 隐式行为 | 采纳/复制/未追问=弱正；重问/换模型/中断=弱负 | 低 | 高 | `implicit_behavior` |
| LLM-as-judge 离线批标 | 伪标签 | 可控 | 可达 100% | `llm_judge` |
| 导入的标注数据集 | 强 | 高 | 取决于导入量 | `ground_truth` |

**组合策略（MUST 实现全部四条链路）**：在线用隐式行为做高覆盖弱标签；显式反馈做高质量锚点；离线 judge 批量补全；提供数据导入与人工标注工具供冷启动。

置信度参考初值（可配）：

```
explicit_preference  0.90
ground_truth         1.00
explicit_binary      0.60      -- 需先区分 capability / preference
llm_judge            0.50
implicit_behavior    0.25
```

#### 标签写入管道

**MUST NOT 由前端事件直接写入 bank。** 必须经过：

```
P1 交互事件 → 事件总线 → 清洗与准入 → 候选标签池 → 审核/自动准入 → EmbeddingBank
```

准入规则 MUST 至少包含：
- 同一用户对同一 turn 的重复反馈只计一次
- 单用户单日反馈数超阈值的标记为可疑，不自动入库
- A/B 实验期间的流量按实验组隔离，不混入基线 bank
- `preference` 类标签不得写入 `capability` 用途的计算

#### 冷启动

新租户 bank 为空时的行为 MUST 明确定义：

1. 完全使用公共底座 bank 路由
2. 租户 bank 累积到 【待定：阈值需实测确定，责任人：算法侧，截止：MVP 后 4 周】临时 500 条有效标签后，开始按权重混合
3. 管理端 MUST 展示 bank 健康度：条数、覆盖领域分布、标签来源构成、最近更新时间

### 3.6 探索预算（防止模型池坍缩）

**这是一个必须显式实现的机制，不能依赖自然结果。**

正反馈风险：路由偏好某模型 → 该模型样本更多 → 其分数估计更准更高 → 更被偏好 → 其他模型样本枯竭 → 系统退化为单模型。论文 Fig.7 观察到"选择熵较高"，但那是实验观察，不是机制保障。

MUST 实现：
- `explore_ratio` 比例的流量强制随机打散候选模型选择（默认 5%）
- 探索流量的结果 MUST 标记 `is_explore=true`，在看板中可单独过滤（避免污染质量统计）
- P3 看板 MUST 监控**模型使用分布的熵**，低于阈值时告警

### 3.7 成本与配额

**没有配额 = 成本敞口无上限。** MUST 实现：

| 项 | 要求 |
|---|---|
| 配额维度 | 租户 / 用户 / 场景，三级，就近生效 |
| 配额单位 | token 数与金额双口径 |
| 超限行为 | 可配：`降级到单模型` / `排队` / `拒绝并提示`，默认降级 |
| 并发上限 | 单租户并发请求数上限，超出排队 |
| Token 上报口径 | **MUST 分开统计**：输入 / 输出 / 思考 token / 缓存命中；聚合路径下按模型分别归集后再汇总。此口径直接关系客户结算，不得含糊 |
| 成本归集 | 每次请求的成本 MUST 可下钻到具体模型调用 |

### 3.8 路由服务 API（草案）

```
POST /v1/route
  request:  { tenant_id, session_id, messages[], scene?, policy_override?, stream? }
  response: { trace_id, content, decision_summary, usage }
  说明：stream=true 时，快车道直接流式；聚合路径先流式推送执行过程事件
        （供 P1 flow.reasoning 渲染），最终内容就绪后再流式推送

GET  /v1/route/{trace_id}/explain
  response: { support_set[], coarse_scores, fine_scores, switch_result, evidence[] }
  说明：可解释性展示专用

POST /v1/models            模型注册
POST /v1/models/{id}/backfill    触发 bank 回填任务
GET  /v1/models/{id}/backfill/status

GET/PUT /v1/policies       策略读写
POST /v1/policies/{id}/rollback

GET  /v1/bank/health       bank 健康度
POST /v1/bank/import       标注数据导入
```

### 3.9 P2 的生命周期

| 实体 | 保留 | 淘汰规则 |
|---|---|---|
| QueryRecord | 按 ttl | 时效性内容（如行情类）ttl 更短，需按 domain_tag 分组配置 |
| ResponseRecord | 跟随 QueryRecord | 级联删除 |
| 标签 | 随记录 | **模型迭代后旧标签失效**：同一 model_id 若底层模型版本变更，MUST 支持标记旧标签衰减或作废 |
| RouteDecision | 跟随 Trace（§4.5） | |

**MUST 定义标签衰减策略。** 同一模型名下的能力会随版本变化，一年前的标签可能完全失效；行情类 query 的正确答案本身随时间改变。缺少衰减策略的 bank 会越来越大且越来越错。临时方案：按 `created_at` 做指数衰减，半衰期按 domain 配置，默认 180 天。

---

## 4. 产品三：数据看板与链路追踪

### 4.1 产品目标

P3 有两个明确区分的部分，**MUST 作为两个交付物分别设计，不要合并**：

| 部分 | 面向 | 数据形态 |
|---|---|---|
| **运营看板** | 客户管理员、业务负责人 | 聚合指标 |
| **链路追踪** | 客户决策者、实施人员、本方研发 | 单次请求的完整链路 |

参考形态：百度千帆将观测拆为「Agent 监控」（聚合）与「Trace 追踪」（单链路）两个入口，本项目采用同样的拆法。

**链路追踪不是可选项。** 上游需求中"提升复杂分析的可解释性、让决策者能追溯推理路径、评估依据质量、定位偏差环节"这一核心价值主张，聚合看板无法承载，只能由链路追踪实现。

### 4.2 运营看板：两类视角

#### A. 成本与运行视角（客户管理员）

| 指标 | 维度 | 说明 |
|---|---|---|
| Token 消耗 | 模型 / 场景 / 租户 / 部门 / 时间 | 分输入、输出、思考、缓存命中四类 |
| 金额消耗与预算进度 | 同上 | 含预算告警 |
| **单次会话成本分布** | 直方图 + P50/P90/P99 | 最具说服力的单一指标 |
| 路由分布 | 模型 | 各模型被选中占比 + **选择熵**（见 §3.6） |
| **聚合触发率** | 场景 / 策略 | 成本的最大杠杆项 |
| 快车道命中率 | 场景 | |
| 延迟分布 | 路径（快车道/路由/聚合）× 分位 | |
| 失败与降级次数 | 类型 | 模型超时、凭证失效、配额超限、卡片引用失效 |

#### B. 洞察视角（业务负责人 / 本方产品）

**这是 P3 的差异化所在。** A 类指标每个 AI 中台都有；B 类必须有 P1 才产得出来。

| 指标 | 说明 |
|---|---|
| 组件触发分布 | 哪些交互范式真被用到，哪些做了没人用 |
| 交互完成漏斗 | 提问 → 组件渲染 → 用户开始交互 → 完成提交，各步流失率 |
| **选项选择分布** | 模型给出 A/B/C，用户实际选了什么。最接近"用户调研"的一块数据 |
| 默认值修改率 | 用户接受模型推荐值 vs 自行修改的比例，反映模型推荐质量 |
| 群体决策分歧度 | 哪些议题团队内部分歧最大 |
| 模型质量对比 | 按领域的胜率、点踩率、被换掉率 |
| 标签产出速率 | 每日新增有效标签数与来源构成，反映飞轮健康度 |

### 4.3 链路追踪

一条 Trace 覆盖一次用户提问到最终回答的完整过程，**跨越 P1 与 P2**。这是唯一能证明三个模块是一个整体的界面。

Span 类型与必录字段：

| span_type | 必录内容 |
|---|---|
| `user_input` | 原始问题、附件引用、场景标识 |
| `embed` | 耗时 |
| `route_score` | support set 摘要（相似历史问题 + 各模型在其上的命中率）、粗粒度分数、候选模型、聚合器 |
| `model_call` | model_id、耗时、tokens、成本、状态、是否超时 |
| `route_switch` | 细粒度分数、是否走聚合、剪掉了谁及原因 |
| `aggregate` | 聚合器、输入 token 数、是否触发截断 |
| `card_render` | card_id + version、component_type、触发方式（模型调用/后置判定/系统注入）、渲染是否降级 |
| `card_interact` | 用户操作、耗时、是否放弃、（群体模式）参与者与分布 |
| `label_emit` | 产出了哪些标签、极性、置信度、去向 |
| `tool_call` | 外部工具调用（若有） |

Trace 界面 MUST 支持：按 trace_id 直达、从看板任一指标下钻到样本 Trace、按失败/降级/高成本/高延迟筛选。

### 4.4 数据延迟：分层，不统一

**MUST NOT 把所有数据都做成实时。**

| 数据类 | 延迟 | 理由 |
|---|---|---|
| 成本与配额 | 准实时，≤ 5 min | 唯一有金钱风险的一类，超支需实时止损 |
| 故障与降级告警 | 准实时，≤ 1 min | 需要立即介入 |
| 用量趋势 | 小时级 | 无人按秒看趋势 |
| 洞察类聚合（B 类全部） | T+1 | 行为洞察需足够样本才有统计意义 |
| Trace | 实时可查（写入后 ≤ 30s 可检索） | 排障与可解释性展示需要 |

**洞察类做成实时是反效果的**：样本量不足时漏斗与分布波动极大，会让业务方基于噪声做决策。此结论 SHOULD 写进产品说明，避免反复讨论。

### 4.5 看板必须能反向控制

**P3 MUST NOT 是只读展示层。** 它同时是 P2 的策略控制台，必须支持：

- 修改 RoutePolicy 全部超参（K、t、档位、探索预算、白名单、配额）
- 按场景 / 租户分组下发
- A/B 实验：创建、分流、对比、结论
- 一键回滚到任一历史策略版本
- **变更前后的「成本-质量」曲线对比**，这是策略调整唯一有意义的验收视图

只读看板客户看两周就不再打开；能调参且能看见调参效果的看板才会成为日常工具。

### 4.6 P3 的权限与隐私

| 项 | 要求 |
|---|---|
| 数据边界 | SaaS 下 MUST 严格租户隔离；跨租户视图仅本方运营角色可见 |
| 导出脱敏 | **对话原文默认 MUST NOT 可导出**。需导出时走单独审批 + 全量审计日志 |
| Trace 内容 | 原文默认脱敏展示，查看完整内容需额外权限 |
| 审计 | 所有导出、策略变更、权限变更 MUST 记审计日志 |

### 4.7 数据保留

| 数据 | 保留 | 说明 |
|---|---|---|
| 原始事件 | 90 天 | 之后仅保留聚合结果 |
| Trace | 30 天热存 + 90 天冷存 | 【待定：客户合规要求可能更长，责任人：PM，截止：客户对齐会】 |
| 聚合指标 | 2 年 | |
| 审计日志 | ≥ 3 年 | 合规要求 |

删除租户时 MUST 级联清理：事件、Trace、租户 bank、卡片、上传文件、凭证。公共 bank 不受影响。

---

## 5. 统一数据契约（三个产品共同依赖，MUST 首先冻结）

### 5.1 标识体系

```
tenant_id    租户
user_id      用户（存储时 MUST 哈希或假名化）
session_id   会话
turn_id      一轮问答（一次用户提问 + 一次系统回答）
trace_id     一次完整执行链路，与 turn_id 一一对应
card_id      卡片定义
render_id    一次卡片渲染实例（同一卡片可在一个 turn 内渲染多次）
```

**MUST：所有事件、Trace、路由决策、标签都携带 `trace_id`。** 这是三个产品之间唯一的关联键，缺失即无法串联。

### 5.2 交互事件 Schema

```json
{
  "event_id": "uuid",
  "trace_id": "uuid",
  "tenant_id": "uuid",
  "session_id": "uuid",
  "turn_id": "uuid",
  "user_id": "hashed",
  "ts": "iso8601",
  "event_type": "card_rendered | card_interaction_started | card_submitted |
                 card_abandoned | feedback_given | control_invoked |
                 render_degraded | card_ref_missing",
  "card": {
    "card_id": "uuid",
    "card_version": 3,
    "component_type": "select.single",
    "semantic_category": "collect",
    "trigger_source": "model_tool_call | post_classification | system_injected"
  },
  "route_context": {
    "policy_id": "uuid",
    "selected_models": ["m1", "m2"],
    "aggregator": "m1",
    "switch_result": "aggregated",
    "is_explore": false
  },
  "payload": {
    "options_offered": ["A", "B", "C"],
    "recommended_default": "B",
    "user_selection": "A",
    "modified_from_default": true,
    "time_to_interact_ms": 4200,
    "time_to_submit_ms": 9800
  },
  "group": {
    "enabled": true,
    "participants_count": 5,
    "aggregation_rule": "majority",
    "distribution": {"A": 3, "B": 2},
    "final": "A",
    "abstained": 0
  },
  "label_hint": {
    "label_kind": "capability | preference",
    "polarity": 1.0,
    "confidence": 0.9,
    "target_models": ["m1"],
    "source": "explicit_preference"
  }
}
```

字段规则：

- `label_hint` 仅在该卡片配置了 `emit_targets` 含 `label_store` 时产出
- `route_context` 由路由服务注入，前端 MUST NOT 自行填写
- `payload` 的结构随 `component_type` 变化，MUST 为每个 component_type 定义子 schema
- **采集失败 MUST NOT 阻塞用户操作**：上报走异步队列，失败本地缓存重试，重试上限后丢弃并记本地日志

### 5.3 事件字典的维护责任

**MUST：事件字典是产品交付物，不是开发内部实现。** 它必须与看板指标定义**同一份文档、同一次评审**产出。

原因：看板要分析的维度，埋点没埋就永远分析不出来；埋点埋了但看板没定义，就是无效数据。两者分开设计必然错位。

具体要求：§4.2 的每一个指标 MUST 能追溯到 §5.2 中的具体字段；§5.2 的每一个字段 MUST 至少被一个指标或一条标签规则消费，否则删除。

### 5.4 Schema 版本管理

- 组件协议 Schema 与事件 Schema 独立版本号，语义化版本
- 破坏性变更 MUST 升主版本，且服务端在过渡期内同时接受新旧两版
- 事件中 MUST 携带 `schema_version`

---

## 6. 交付计划与范围边界

### 6.1 里程碑

| 阶段 | 交付内容 | 完成判据 |
|---|---|---|
| M0 | 统一数据契约（§5）+ 组件协议 Schema | Schema 通过评审并冻结；每个指标可追溯到字段 |
| M1 | 预置组件库骨架（呈现型 + 采集型各覆盖 60%）+ 渲染器 + token 注入 | 在 demo 宿主中可渲染全部已实现组件，切换 token 包生效 |
| M2 | 卡片管理端（CRUD + 状态机 + 调试）+ MCP Server | 可配置一张卡片并被模型正确触发；调试面板可用 |
| M3 | 路由器 MVP（Step 1–2 + 快车道，**不含聚合**）+ 模型注册 + 公共 bank 冷启动 | 单模型路由可用，Trace 记录完整，延迟达标 |
| M4 | 事件管道 + 运营看板 A 类 + 链路追踪 | 成本可下钻，Trace 可回放 |
| M5 | 聚合路径（Step 3–5）+ 策略控制台 + A/B | 聚合可开关，策略可回滚，成本-质量曲线可见 |
| M6 | 标签管道 + 租户 bank + 看板 B 类 | 飞轮闭合：交互产生标签，标签影响路由，效果在看板可见 |

**M3 早于 M5 是刻意的**：先让单模型路由跑通并把 Trace 打穿，再引入聚合。反过来做会导致延迟与成本问题在没有观测手段的情况下暴露。

### 6.2 非目标（本期不做，写出来防止范围蔓延）

- ❌ 行业智能体本身（市场研判、运力匹配、合规审查等业务 Agent）——由客户方或其他标段实现
- ❌ 知识库、RAG、文档解析
- ❌ 模型微调、模型托管、推理加速
- ❌ 智能体编排、多智能体协作框架
- ❌ 资产管理体系（报告库、产出物归档）
- ❌ 对外开放 API 网关的计费、开发者门户
- ❌ 私有化环境下的联网搜索（无公网出口时该能力不可用，MUST 在产品中显式标注，不做静默降级）
- ❌ 移动端原生应用（本期仅 Web 与 SDK）

### 6.3 SaaS 与私有化的功能差集

| 能力 | SaaS | 私有化 |
|---|---|---|
| 公共底座 bank 自动更新 | ✅ | ❌ 需离线导入 |
| 外部商业模型 API | ✅ | 取决于客户网络，MUST 可配置为纯内网模型池 |
| 跨租户看板 | ✅（本方运营） | 不适用 |
| 联网搜索 | ✅ | ❌ |

**MUST：私有化部署的安装包中，不可用能力在界面上显式置灰并说明原因，禁止调用时才报错。**

---

## 7. 待定事项清单

| 编号 | 事项 | 临时默认值 | 责任人 | 截止 |
|---|---|---|---|---|
| TBD-01 | 聚合路径端到端延迟验收线 | ≤ 25s | 算法侧 | 立项后 2 周 |
| TBD-02 | 每日有效标签数验收线 | 待 DAU 确认 | PM | 客户 DAU 确认后 |
| TBD-03 | 是否引入轻量分类模型做呈现型后置判定 | 不引入，先用规则 | 算法侧 | MVP 后 |
| TBD-04 | 上传文件保留期 | 90 天 | PM | 立项后 2 周 |
| TBD-05 | 租户 bank 启用的标签数阈值 | 500 条 | 算法侧 | MVP 后 4 周 |
| TBD-06 | Trace 保留期（受客户合规约束） | 30 热 + 90 冷 | PM | 客户对齐会 |
| TBD-07 | 标签衰减半衰期 | 180 天，按 domain 可配 | 算法侧 | M6 前 |
| TBD-08 | 群体决策的参与者认证与权限模型如何与客户统一认证对接 | 待定 | 架构侧 | M2 前 |
| TBD-09 | 客户方需求文档中 F-01～F-09 的完整清单与本项目的接口边界 | 未获取 | PM | 尽快 |

**TBD-09 优先级最高。** 上游需求文档的产品定位章节承诺了本项目范围之外的能力（智能体调用、资产管理、开放 API）。若不明确划清交付边界，验收时存在被上游定位章节绑定的风险。

---

## 8. 给实现方的执行提示

1. **先冻结 §5，再写任何代码。** 三个产品都依赖它，改动成本随时间指数上升。
2. **异常路径先于正常路径收口。** §2.8 的异常清单是验收项，不是补充说明。
3. **状态机是后端事实，前端是它的投影。** 若前端出现了 §2.7 状态表中不存在的显示状态，说明状态机漏了，补状态而非补显示。
4. **任何涉及外部模型调用的地方，都要回答四个问题**：单次消耗量级、失败重试策略、配额归属、成本承担方。缺一即为成本敞口。
5. **凡是"待定"，按 §7 的临时默认值实现，并在代码中以显式常量标注 TBD 编号**，便于后续替换与追踪。
6. **不要在 P1 的组件代码里出现任何业务词汇。** 出现"承运商""港口""提单"即为分层泄漏，说明业务层没有正确分离。

# 智能交互平台 · 产品功能说明书

- 产品名称：智能交互平台（富交互组件 + JiSi 多模型路由）
- 产品原型 Demo：`http://127.0.0.1:8787/`（本地启动：`cd ~/Desktop/富交互项目 && python3 -m uvicorn server.app:app --port 8787`）
- 版本：v4.0（2026-09-04。v4.0 拆分为两个独立平台/站点：智能交互平台（badbotai.github.io/smart-interaction-assistant）与模型路由平台（badbotai.github.io/smart-model-router）；新增产品类（1 产品 = 1 品牌风格 + N 组件 + 1 个 MCP 接入点，组件级嵌入代码保留）、组件风格页转风格库；路由策略与 API 融合（一条策略一个 API Key，新建即发 Key，参数=候选/兜底/成本-效果权重/聚合开关）、策略命名改省钱优先/质量优先、模型画像与路由决策模型（原打分裁判）移入场景数据集页、API 接入管理页裁撤；build_docs 双站构建。v3.4（2026-09-03。v3.4 路由策略默认双档（全局均衡/默认兜底）与命名去场景化、参数配置抽屉重构（候选标签选择/成本-效果权重/画像参考权重/保存应用）、场景数据集详情页（题目级增删改标/导入/替换）、对话测试独立双模式页、看板重排（成本聚拢/失败降级上移/空置卡清理）、tab 组件统一下划线式、tooltips 全站精简、操作日志动作中文化、首页路由动画曲线化；v3.3（2026-08-31。v3.3 编辑器由弹窗改为页面式（保留左侧导航）、反馈优化底层重构（场景数据集 / 对话回流去重采集 / Judge 评审模型配置 / 评测回填闭环）、模型测试抽屉调度策略选择与路由决策 CoT、入池回填独立块、时间范围日期区间选择器、全站货币 RMB、工作台异常标签、状态色与操作按钮体系统一；v3.2 组件层级合并（组件文案并入对话气泡）、15 器体系（追问引导器并入入口跳转器、地点选择器改地址卡片、信息登记表改表单收集工具、对比选择器表格化）、组件内部设计系统 v1、弹窗设计规范 v1（含选项编辑块与步骤条自由跳转）、回显范围收敛、工作台行改版（6 位 ID / 状态开关 / icon 操作）、版本概念从 UI 摘除与上线锁定编辑、购物卡片紧凑化；v3.1 反馈优化页按数据管道重写（冷启动底座 / 回流阈值 / 信号源闸门 / 评测回填闭环）、17 类交互卡、转化看板独立、全站自绘下拉、操作日志（可导出）、组件风格两列化、添加模型两步化、工作台下划线 Tab 与三层行、全站截图走查 20 项闭环；v3.1 反馈优化页按数据管道重写（冷启动底座 / 回流阈值 / 信号源闸门 / 评测回填闭环）、17 类交互卡、转化看板独立、全站自绘下拉、操作日志（可导出）、组件风格两列化、添加模型两步化、工作台下划线 Tab 与三层行、全站截图走查 20 项闭环；v3.0 组件交互系统（结构 token / 五态 / 定高展开 / 重置）、17 类交互卡（新增物流轨迹卡、地点选择器去地图化）、商品卡与工作台列表重设计、icon 三档统一、参数配置抽屉分组重排、看板两整卡合并；v2.9 整改三批（服务端守门 / 选项主键与生命周期 / 承诺对齐）、16 类组件（新增图片上传器、追问引导器、商品下单器、入口跳转器，地点选择器地图化）、设计系统五档字号、看板可视化重构、品牌风格租户级、审计日志页、性能与连点治理、供应链客服化数据；v2.8 十二类「器」组件体系、固定尺寸五步新建弹窗（含获取 Key）、回显面板重设计、导入品牌风格页、删除人工复核、浅蓝灰画布与日期控件统一；v2.7 测试抽屉与全站复审循环；v2.6 信息架构三大模块定型（组件工作台合并、模型路由三入口、底部账户）、两步新建向导、有效期、看板分区与导出。v2.4 及以前见各章；v2.5 换 Sky 主题、选项级动作、模型选择收进输入框、草稿箱、设计风格管理重构、全局命名与去重。原记录：v2.1 交互走查；v2.2 四维度复审；v2.3 左侧导航 + 文案精简 + 对话式组件卡；v2.4 AI 功能复审、设计系统页（品牌 Token 导入）、对话测试重构为真实助手形态、链路追踪重构为业务化「对话记录」、去除 A/B 实验）
- 对应标书条款：《供应链数据智能及多模态工具集建设项目需求规格说明书》2.5 智能助手与交互工具（F-5-01 ~ F-5-05）
- 本版变更：见第二十章；历史轮次见第六至十九章

## 一、产品定位

一个 AI 应用的网页端管理平台。管理员把「交互组件」配置进 AI 对话——在什么条件下触发、出什么组件、收回来的数据去哪；对话从「用户自由输入—模型自由回复」变为「模型主动引导—用户结构化响应」。同时提供多模型统一接入与智能路由（JiSi）、行为埋点采集与运营看板，用户的每次选择都回流为监督信号，持续优化模型调度。

## 二、产品功能清单（四大核心模块）

核心模块：模型路由 · 智能交互 · 数据展示 · 对外接入

| 模块 | 子模块 | 功能 | 功能说明 | 细分字段 | 优先级 | 交付版本 |
|---|---|---|---|---|---|---|
| 平台结构 | 双平台 | 两个独立站点 | 智能交互平台与模型路由平台各自独立网址、独立导航；智能交互首页作门户互跳 | 平台归属、导航 | P0 | v4.0 |
| 智能交互 | 产品接入 | 产品管理 | 一个产品 = 一个品牌风格 + 多个组件 + 一个 MCP 接入点；产品卡列表、新建（名称 / 风格单选 / 组件多选）、配置抽屉、删除 | product_id、brand_file、card_ids | P0 | v4.0 |
| 智能交互 | 产品接入 | MCP UI 对接 | 每个产品一个 MCP 接入 Key，一键复制客户端接入配置；组件在对接方产品里经 MCP UI 渲染并自动应用产品风格；组件级嵌入代码保留为补充方式 | mcp_key、接入配置 | P0 | v4.0 |
| 智能交互 | 组件风格 | 风格库 | 风格卡片库：预设 + 导入（新增卡片式入口），点卡预览组件效果，显示被哪些产品使用；风格生效关系落在产品上 | brand token、使用产品 | P0 | v4.0 |
| 智能交互 | 组件库 | 组件协议与渲染引擎 | 前端解析引擎（components.js）：模型返回的协议信封解析为可交互组件，五态状态机 / 提交冻结 / 渲染失败自动降级纯文本 / 按 render_id 幂等 | 信封、render_id | P0 | v2.8 |
| 智能交互 | 组件库 | 15 器组件集 | 采集（选择 / 排序 / 评分 / 表单 / 上传等）、交易（商品下单）、引导（入口跳转）、呈现（物流轨迹）、步骤说明书（1-10 步翻页）；对话式陈列、一键创建配置 | 组件类型、配置项 | P0 | v3.4 |
| 智能交互 | 组件管理 | 配置工作台 | 配置列表：搜索、状态开关上下线、异常标签（完成率低 / 跳转异常）、回显数据入口 | 配置 ID、状态 | P0 | v3.4 |
| 智能交互 | 组件管理 | 配置编辑器 | 页面式五步编辑：选组件 → 交互配置 → 设计内容 → 数据收集 → 获取 ID；触发条件口语化 + AI 改写；实时对话预览 | 触发、config | P0 | v3.4 |
| 智能交互 | 对话测试 | 智能交互测试页 | 像用户一样提问：验证触发词、组件效果与提交链路，可从组件直接进编辑；测试数据不计入业务看板 | 触发命中、事件 | P0 | v4.0 |
| 智能交互 | 交互数据 | 交互看板 | 日期区间选择器；整体 KPI → 转化表现（漏斗 / 分组件转化 / 触发分布 / 推荐默认项 / 埋点健康）→ 每配置数据卡（点名称跳工作台定位） | 完成率、分布 | P0 | v3.4 |
| 模型路由 | 路由策略 | 策略即 API | 一条策略 = 一个 API：新建策略即发 API Key（弹窗展示可复制），策略卡带 Key 脱敏展示与引用代码；换策略只需换 Key；内置默认双档「全局均衡」「默认兜底」，示例档「省钱优先」「质量优先」 | policy_id、api_key、默认档 | P0 | v4.0 |
| 模型路由 | 路由策略 | 参数配置 | 抽屉：策略 ID + API Key（只读）、名称、候选模型（标签式多选）、兜底模型、成本-效果权重滑杆、聚合开关；保存即应用 | alpha、allow_aggregation、whitelist | P0 | v4.0 |
| 模型路由 | 场景数据集 | 场景与题目管理 | 场景表（添加 / 编辑 / 删除，删除级联题目）；场景详情逐条管理：批量添加、删除、更改场景标签、打分状态与来源；导入页（模板 / 示例 / 替换模式，替换先校验防误清） | 场景、query、ideal | P0 | v3.4 |
| 模型路由 | 场景数据集 | 路由决策模型 | 页顶横幅管理（原打分裁判）：独立接入、不回答用户问题，专门评估各模型答题表现；未配置时除默认兜底外策略灰置 | judge_model | P0 | v4.0 |
| 模型路由 | 场景数据集 | 模型画像 | 各场景路由去向表：逐场景各模型成绩与去向（成绩接近标聚合），按全局均衡权重计算；「重新生成」= 补打分 + 重建画像 | 场景成绩、去向 | P0 | v4.0 |
| 模型路由 | 对话测试 | 模型路由测试页 | 预设跨场景问题；底部组合选择器（模式 + 策略 / 模型）；每次回答展示打分过程、最终去向与本次成本 | 模式、成本 | P0 | v4.0 |
| 模型路由 | 路由数据 | 路由看板 | 三张复合 KPI 卡（成本与用量 / 路由行为 / 聚合效果）→ 路由分布与失败降级 → 成本走势 / 按模型 → 延迟分位 → Token 与模型质量 | 成本、P50/P99 | P0 | v3.4 |
| 模型路由 | 模型接入 | 模型列表与入池评测 | 两步注册（校验 + AI 读定价页补价）；表格管理（思考模式 / 启用 / 编辑抽屉 / 默认兜底保护）；新模型全量作答建档、覆盖率 100% 自动入池 | model_id、覆盖率 | P0 | v3.4 |
| 平台底座 | 路由引擎 | JiSi 路由引擎 | 服务端路由内核：支撑集检索 → 场景归类 → 各模型历史成绩打分 → K 路并发 → 细排胜出或聚合重写；快车道 / 探索预算 / 故障切兜底；决策全量记录可解释 | 粗排/细排、聚合、降级 | P0 | v3.3 |
| 平台底座 | 评测管道 | 作答与打分管道 | 确定性模型模拟作答 + 路由决策模型打分 + 画像离线重建（异步任务、进度可轮询、异常兜底不锁死） | 评测任务、画像版本 | P0 | v3.3 |
| 平台底座 | 事件管道 | 事件准入与渠道隔离 | 交互事件幂等去重、日上限清洗；test / embed / 业务渠道隔离，测试流量不污染看板 | channel、幂等 | P0 | v3.1 |
| 平台底座 | 统计层 | 看板聚合查询 | 看板、导出 CSV、明细同一服务端查询口径；操作全量审计（动作中文化） | 查询口径、审计 | P0 | v3.4 |
| 平台底座 | 对外协议 | 路由与事件 API | POST /v1/route（SSE 流式，带决策摘要与组件信封）+ POST /v1/events 回传；数据契约 Schema 版本化 | SSE、schema | P0 | v3.3 |
| 平台底座 | 植入层 | sia.js 嵌入 SDK | 一段 snippet 嵌入对接方页面：隔离沙箱渲染、对话 / 规则 / 显式三种触发、数据自动回传（channel=embed） | data-key、Shadow DOM | P0 | v3.3 |
| 平台底座 | 发布管线 | 双站静态构建 | build_docs 一键构建两个演示站：数据快照 + mock 拦截层 + 资源内容哈希（防 CDN 新旧混跑）+ Service Worker 缓存 | 快照、哈希、SW | P0 | v4.0 |

## 三、Loom 设计系统（视觉规范）

参考 UAV 巡检 Demo 的设计语言（冷灰底 + 靛蓝品牌 + 深海军蓝面板），针对智能交互平台微调：

| Token | 值 | 用途 |
|---|---|---|
| 页面底色 | `#EEF0F4` | 冷灰蓝底，卡片白色悬浮其上 |
| 品牌色 | `#3B5FCE`（悬停 `#4A6EDE`） | 主操作、激活态、进度、图表首色 |
| AI 强调色 | `#6E4BD8`（紫罗兰） | 所有 AI 能力统一标识：智能匹配、AI 改写、AI 匹配组件、重建画像、离线批标 |
| 深面板 | `#26304F → #181F38` 渐变 | 工作台欢迎区等强调区域 |
| 语义色 | 成功 `#1FA971` / 警告 `#B97A17` / 危险 `#D9453C` | 状态标签、告警、危险操作 |
| 字体 | Noto Sans SC；数字用等宽字体（tabular-nums） | KPI 大数字、成本、耗时、ID |
| 圆角 | 卡片 10px / 控件 8px / 标签胶囊 | 统一形状语言 |
| 层次 | 细描边 + 双层柔和投影 | 卡片、弹窗、悬浮菜单 |
| 图表配色 | `#3b5fce` `#e8792b` `#1fa971` `#b97a17` `#d95f8e` `#2b8a3e` `#6e4bd8` `#d9453c` | 分类序列，网格静默 |

全站禁用 emoji 与装饰性符号；必填标记仅用 ASCII 星号；关闭按钮用 ×。

## 四、信息架构（v2.3：左侧分组导航）

```
工作台                 KPI + 待处理 + 配置数据与回显开关 + 路由概览 + 快捷入口
智能交互
  组件库               对话式组件陈列 + 智能匹配 + 组件 key / 调用示例
  交互配置             列表 + 触发调试 + 编辑器（三步表单 / 对话式预览）
  对话测试             真实助手形态：欢迎页 + 消息流 + 胶囊输入框（圆形发送 / 中断按钮）
  设计系统             当前 Token（色板 / 复制 / 下载）+ 品牌 Token 导入（校验 → 预览应用 → 保存为品牌）+ 组件跟随效果
模型路由
  模型池 / 数据集与画像 / 路由策略 / 回流优化   （按 #hash 渲染，侧栏二级直达；A/B 实验已移除）
运营分析
  交互看板 / 路由看板   （同页按 #hash 渲染，左侧二级直达）
  对话记录             业务叙事时间线（提问 → 命中配置 → 用户选择 → 模型回答 → 反馈沉淀），span 瀑布收进「技术详情」
```

命名：产品侧统一使用「智能交互」指代富交互组件能力（「富交互」为内部技术称谓，不再出现在界面）。

## 五、核心用户体验路径（v2.4 重梳）

三类角色：管理员（配置与运营）、终端用户（对话）、对接方（数据与品牌）。所有路径均可从左侧导航一步进入，页面间靠深链贯通（?new / ?edit / ?q / ?status / #hash / ?trace_id）。

### 管理员

```
P0 首次上手（10 分钟建立认知并跑通第一条配置）
   工作台(空态) → 组件库：看 8 段真实对话示例，理解「组件长在对话里」
   → 智能匹配输入业务场景（或点示例）→ 建议卡带内容预览 → 创建配置(预填)
   → 编辑器三步：触发条件(AI 改写建议卡→采用) / 信息与交互(右侧对话式预览) / 数据回收
   → 保存并发布 → 更多·在对话中测试(?q=示例问法) → 发送验证触发 → 返回工作台看首条数据

P1 日常新增 / 修改配置
   交互配置 → 新建(或编辑，线上不受影响) → 三步表单 → 触发调试验证命中与竞争
   → 发布新版本(即时生效) → 有旧引用时在「版本历史与引用」批量升级

P2 数据驱动迭代
   工作台·待处理(旧引用/未发布修改/待复核/低完成率/熵告警，带直达入口)
   → 交互看板：按配置 ID 看触发/提交/完成率/群体选择分布 → 筛选·下钻·CSV
   → 完成率低 → 对话记录看单次业务叙事(提问→命中→选择→回答)定位原因
   → 编辑配置调整文案/选项 → 发布新版本 → 看板复验

P3 模型治理
   模型池：添加模型 → 入池回填(进度/成本可见) → 上线 → 设默认兜底(受保护)
   → 路由策略调参(常用/高级分层，版本化可回滚) → 路由看板监控成本/延迟/多样性

P4 回流优化（让路由越用越准）
   回流优化三步卡：1 收集反馈(自动) → 2 人工复核(采纳/剔除 AI 的判断) → 3 更新画像
   → 数据集与画像查看簇×模型矩阵与 α 旋钮 → 路由看板验证效果
```

### 终端用户

```
P5 一次对话
   欢迎页(建议卡)或直接输入 → 命中配置：说明文案 + 交互组件(胶囊选项/表单/打分)
   → 提交 → 群体回显「XX% 的用户选择了同类答案」 → AI 基于提交继续跟进
   未命中：按调用模式回答(智能路由/手动选模型/多模型回答)
   任意回答可：赞踩(踩追问原因) / 复制 / 换模型重答 / 看「为什么选它」
   逃生口：触发错了可「跳过，直接回答」；生成中可中断；随时新会话
```

### 对接方

```
P6 品牌接入（设计系统）
   设计系统 → 从当前起稿复制 token → 改色/字体/圆角 → 预览应用(临时生效可还原)
   → 保存为品牌 → 左下角切换，全部组件与平台即时换肤

P7 数据接出
   组件库：复制组件 key 与调用示例(协议信封 JSON)
   → 交互配置·数据回收：配置 ID + Webhook 推送契约 / GET /v1/cards/{id}/responses 拉取聚合
   → 事件按 card_id + component_type + render_id 关联，与看板同口径
```

横切保障：编辑不影响线上（快照）、破坏性操作二次确认、全对象 ID 可追溯、失败可重试、原文脱敏审计。

## 六、交互走查与整改记录（v2.1）

以产品经理 + 设计师双视角逐页走查每个交互元素与表单，共发现 46 项问题，全部整改完成并通过语法检查、API 测试与无头浏览器渲染验证。

| 编号 | 页面 | 问题 | 影响 | 整改 | 状态 |
|---|---|---|---|---|---|
| G1 | 全站 | 删凭证、解除脱敏、恢复草稿等用浏览器原生 confirm，样式与设计系统割裂，文案不可控 | 体验断裂、无法标注危险等级 | 新增统一确认框 UI.confirm（标题 / 说明 / 危险样式 / 自定义按钮文案），全站替换 | 已完成 |
| G2 | 全站 | 弹窗不能 Esc 关闭；错误 toast 3 秒即消失读不完 | 可用性、可达性 | 弹窗支持 Esc、aria-modal；错误 toast 延长到 5 秒 | 已完成 |
| G3 | 全站 | 组件类型处处显示内部 ID（select.single） | 运营人员不懂，认知负担 | 新增 UI.ctName / ctChip，全站展示中文名，悬停显示 ID | 已完成 |
| G4 | 全站 | 多处异步按钮无忙碌态，可重复点击（保存并发布、注册、导入、调参下发） | 重复提交、重复发布 | 新增 UI.withBusy，所有异步按钮统一禁用 + 文案反馈 | 已完成 |
| G5 | 全站 | 次要与破坏性操作和主操作平铺，一行 5 个按钮 | 误触、视觉噪音 | 新增「更多」下拉菜单 UI.menu，危险项红色 | 已完成 |
| G6 | 全站 | 标签页切换期间内容区空白 | 用户以为卡死 | 新增 UI.loading 占位；标签页支持 #hash 深链与刷新保持 | 已完成 |
| W1 | 工作台 | 「交互配置数据」近 30 天、「模型路由概览」近 7 天，同屏两套口径 | 数据误读 | 统一一个「统计范围」分段控件（7 / 30 天，记忆选择）控制全部数据块 | 已完成 |
| W2 | 工作台 | 已下线配置仍可切回显；「回显开启 N/M」把下线配置计入分母 | 操作无效、指标失真 | 下线配置开关禁用并提示原因；KPI 只统计已发布配置；开关加 role=switch 与忙碌态 | 已完成 |
| W3 | 工作台 | 选项分布静默截断到 5 项 | 用户不知道还有其他选项 | 追加「其余 N 项合计 X%，完整分布见看板」 | 已完成 |
| W4 | 工作台 | 「数据契约」直接在当前页打开 JSON 文件 | 离开工作台、无返回 | 新标签页打开并在文案中说明 | 已完成 |
| W5 | 工作台 | KPI 无口径说明；导出 CSV 不跟随统计范围 | 指标歧义、导出与页面不一致 | KPI 悬停口径；导出带 days 参数 | 已完成 |
| L1 | 组件库 | 示例描述只能看不能用 | 首次使用门槛 | 4 个示例变为可点击填入并立即匹配 | 已完成 |
| L2 | 组件库 | 「以此新建配置」与「用它新建」两套文案；无结果无空态；结果不能清空 | 认知不一致 | 统一为「用它新建配置」；补空态与改写建议；加「清空结果」 | 已完成 |
| L3 | 组件库 | 输入框无长度限制、无无障碍标签 | 可达性 | maxlength 300、隐藏 label、回车提示 | 已完成 |
| C1 | 交互配置 | 删除配置无二次确认（仅被引用时才弹窗）；下线、回滚也无确认 | 误删不可逆、线上直接受影响 | 三类操作统一确认框，删除为危险样式并说明数据保留策略 | 已完成 |
| C2 | 交互配置 | 列表行信息过载（8 个标签 + 5 个按钮） | 扫描困难 | 两行信息结构；操作收敛为编辑 + 主状态操作 + 更多（版本历史 / 复制 ID / 删除） | 已完成 |
| C3 | 交互配置 | 搜索每次击键即请求；列表缺更新时间与触发摘要 | 性能、无法判断配置用途 | 250ms 防抖；显示触发摘要、版本、更新时间、引用数、总条数 | 已完成 |
| C4 | 交互配置 | 编辑器单列超长，预览在最底部，改字段看不到效果 | 核心价值（所见即所得）失效 | 改为左表单 / 右预览分栏，预览吸顶常驻，弹窗加宽；操作栏吸底 | 已完成 |
| C5 | 交互配置 | 必填字段无标记；校验失败只 toast，不知道错在哪 | 反复试错 | 星号标记必填；客户端校验 + 字段级标红 + 滚动到第一个错误；服务端错误同样映射到字段并用中文字段名 | 已完成 |
| C6 | 交互配置 | 已发布配置仍可点击切换组件，保存时才被服务端拒绝 | 白填一遍 | 组件卡片锁定态（置灰、不可点、提示原因），隐藏 AI 匹配按钮 | 已完成 |
| C7 | 交互配置 | 推荐默认项用文本框手填，容易填成选项里没有的值 | 默认值失效 | 改为从已填选项中下拉选择，选项变更后提示重新确认 | 已完成 |
| C8 | 交互配置 | 档位数无范围；滑条最小值可大于最大值；Webhook 无格式校验 | 脏配置进入线上 | 档位 2-10、区间校验、Webhook 必须 https | 已完成 |
| C9 | 交互配置 | 信息登记表字段用「key\|标签\|必填」文本语法 | 非技术人员无法使用 | 行编辑器：字段标识 / 显示标签 / 必填勾选 / 删除 / 添加字段，兼容旧草稿 | 已完成 |
| C10 | 交互配置 | 保存 / 发布按钮可重复点击；无取消按钮；点遮罩即关闭丢失编辑 | 重复发布、误关 | 忙碌态；加取消按钮；编辑器弹窗改为不可点遮罩关闭；底部常驻当前版本与保存后果说明 | 已完成 |
| C11 | 交互配置 | 恢复草稿用原生 confirm；关闭自动触发后没有任何说明 | 不知道关闭意味着什么 | 统一确认框；关闭时显示「仅可由外部系统显式调用」 | 已完成 |
| C12 | 交互配置 | 编辑中的已发布配置，主按钮仍叫「发布」 | 不知道会覆盖线上 | 文案改为「发布新版本」，删除项改为「删除配置（含线上版本）」 | 已完成 |
| H1 | 对话演示 | 没有任何方式重置对话，只能刷新页面 | 演示 / 测试效率 | 头部显示会话 ID + 「新会话」按钮（确认后清空，未提交组件记为放弃） | 已完成 |
| H2 | 对话演示 | 生成中仍可切换调用模式与模型 | 请求参数与展示不一致 | 生成中锁定分段控件与模型下拉；模式与模型记忆上次选择 | 已完成 |
| H3 | 对话演示 | 复制一次后按钮永久禁用 | 不能再复制 | 按钮可反复点击，隐式正信号只上报一次 | 已完成 |
| H4 | 对话演示 | 手动模式下仍显示「换模型重答」 | 语义矛盾（模型是固定的） | 手动模式文案改为「重新回答」，悬停说明两者含义 | 已完成 |
| H5 | 对话演示 | 请求失败只报错，需要重新输入；发送后焦点丢失 | 操作成本 | 原地「重试」按钮；发送后焦点回输入框；手动模式未选模型阻止发送 | 已完成 |
| H6 | 对话演示 | 模型下拉无标签；「详情」链接不说明内容 | 可理解性 | 加「指定模型」标签并标注默认兜底；链接改为「为什么选它」 | 已完成 |
| R1 | 模型路由 | 「删凭证」危险按钮混在凭证列；暂停 / 设默认无确认；默认兜底模型可被暂停或删凭证，导致所有降级路径失效 | 线上稳定性风险 | 删凭证移入「更多」菜单并确认；暂停 / 设默认确认；默认模型禁止暂停、退役、删凭证（前端禁用 + 服务端 409） | 已完成 |
| R2 | 模型路由 | 注册表单英文字段名、无必填、无校验、无取消、按钮文案过长 | 填错才知道 | 中文标签 + 必填星号 + 客户端校验（ID 格式 / https / vault:// / 非负价格）+ 取消 + 重复 ID 定位到字段 | 已完成 |
| R3 | 模型路由 | 调参弹窗 9 个数学符号参数平铺；保存无错误处理、无取消 | 管理员不敢动 | 分「常用 / 高级（折叠）」，常用项用业务语言；范围校验；忙碌态；取消 | 已完成 |
| R4 | 模型路由 | A/B 开始无参数校验、可重复开实验；结束无确认 | 实验数据污染 | 校验 t∈[0,1]、分流 5-95%；进行中禁止再开；结束确认 | 已完成 |
| R5 | 模型路由 | α 文案称「即时反映到后续路由」但实际只改画像视图；重建画像可重复触发 | 误导 | 文案如实说明「重建后新版本才用于线上路由」；重建前确认、运行中禁用 | 已完成 |
| R6 | 模型路由 | 待复核列表静默截 12 条看不到总量；导入工具 Tab 分隔格式无示例 | 不知道还有多少、不会用 | 显示总数 + 「显示更多」+ 行级忙碌态；导入加「填入示例」与解析预览，领域分布中文化 | 已完成 |
| R7 | 模型路由 | 切换标签页内容区空白；刷新回到第一个标签 | 体验断裂 | 加载占位；#hash 深链 | 已完成 |
| D1 | 运营看板 | 「导出 CSV」塞在指标块里；富交互视角无法按配置筛选 | 找不到、配置多时难用 | 独立工具栏：配置筛选 + 导出；单卡导出 | 已完成 |
| D2 | 运营看板 | 配置卡标题用提问文案，与工作台用配置名不一致 | 跨页面对不上 | 标题配置名、提问文案作副标题，组件中文名 | 已完成 |
| D3 | 运营看板 | 明细下钻不跟随调用模式与时间窗，与图表不同源；固定 200 条 | 标书要求「图表与明细同源」不成立 | /api/traces 增加 mode / final_model / days / offset 服务端筛选；下钻按当前筛选分页加载（100/页，≤1000） | 已完成 |
| D4 | 运营看板 | KPI 无口径说明；筛选后不知道当前条件 | 指标歧义 | KPI 悬停口径；显示筛选后请求数与「清空筛选」 | 已完成 |
| D5 | 运营看板 | 完成率显示 460%（提交数大于渲染数） | 指标不可信 | 提交 > 渲染时显示「数据不全」并在悬停说明原因，不展示大于 100% 的比率 | 已完成 |
| T1 | 链路追踪 | 路径筛选缺「手动指定」「降级」；路径映射缺 manual | 新模式的请求查不到 | 补齐筛选项与映射 | 已完成 |
| T2 | 链路追踪 | 列表固定 60 条无法翻页；trace_id 输入无回车 | 找不到老请求 | 「加载更多」分页；回车查询；URL 直达时回填输入框 | 已完成 |
| T3 | 链路追踪 | 选中项无高亮；路径 chip 用颜色同时表达失败 | 不知道看的是哪条、语义混杂 | 选中高亮；路径与「失败」状态分开标识 | 已完成 |
| T4 | 链路追踪 | 解除脱敏用原生 confirm；trace_id 不能一键复制 | 体验与效率 | 统一确认框（说明审计后果）；复制按钮 | 已完成 |

## 七、全站交互规范（v2.1 起执行）

| 类别 | 规则 |
|---|---|
| 确认 | 影响线上（下线、暂停模型、设默认、回滚、下发策略、重建画像、结束实验）必须二次确认；不可逆（删除配置、删除凭证）用危险样式确认框并说明数据保留策略；禁止使用浏览器原生 confirm / alert |
| 忙碌态 | 所有异步按钮在请求期间禁用并改文案（保存中…… / 发布中…… / 匹配中……），防重复提交；长任务（回填、重建、批标）运行中按钮禁用并显示进度 |
| 操作收敛 | 每行最多「主操作 + 1 个状态操作 + 更多」；版本历史、复制 ID、删除等进入「更多」菜单，危险项红色置底 |
| 表单 | 必填字段星号标记；保存前客户端校验；错误定位到字段（标红 + 字段下说明）并滚动到第一个错误；服务端错误同样映射到字段，用中文字段名；枚举值用下拉而非自由文本；长表单采用左表单 / 右预览分栏，操作栏吸底，提供取消，遮罩点击不关闭 |
| 命名 | 面向运营人员只展示中文名（组件类型、领域、路径、调用模式、策略参数），内部 ID 以 ID 胶囊展示（可复制）；英文黑话（bank / support set / judge / tool）一律翻译；按钮文案动词 + 对象（用它新建配置 / 发布新版本 / 解除脱敏并记审计） |
| 可统计性 | 每个可统计对象（交互配置、组件类型、策略、Trace）在列表、编辑器、看板、用户端组件上都露出 ID；回收数据与事件以 card_id + component_type + render_id 关联 |
| AI 动作 | AI 能力统一紫色：输入框内右下角魔法棒图标（改写 / 匹配），按钮型 AI 动作带魔法棒图标；运行中图标旋转并禁用 |
| 数据口径 | 同屏只有一个时间范围；比率分母为 0 显示「-」，分子大于分母显示「数据不全」并悬停说明；KPI 悬停给出口径；导出与下钻必须和图表同一服务端查询条件 |
| 列表 | 显示总条数；超过一页提供「加载更多」或「显示更多（还有 N 条）」，不静默截断；搜索防抖 250ms；选中项高亮 |
| 反馈 | 成功 toast 3 秒、错误 toast 5 秒；失败给出原地重试；标签页切换显示加载占位；标签页与弹窗支持 #hash / 深链 |
| 文案密度 | 页面与卡片默认不放解释性段落：标题 + 信息图标悬停说明；表单以 placeholder 承担示例，字段下不放提示；按钮用动词或图标（下载 / 复制 / 关闭）；toast 一句话以内；确认框正文不超过两句 |
| 安全兜底 | 默认兜底模型不可暂停、退役、删凭证；已有配置组件类型不可变更；关闭自动触发时明确告知后果 |

## 八、第二轮四维度复审记录（v2.2）

逐页按「产品功能逻辑 / 前后端逻辑 / 交互与视觉 / 文案」四个维度复审，共 31 项，全部完成。

| 编号 | 页面 | 维度 | 问题 | 优化 |
|---|---|---|---|---|
| P01 | 全站 | 功能逻辑 | 可统计对象（配置、组件、策略）没有露出 ID，后台统计与对接无法对上号 | 新增 ID 胶囊（点击复制）：配置列表 / 编辑器元信息条 / 工作台卡 / 看板卡 / 调试命中结果 / 组件库组件卡 / 策略卡；用户端组件底部带「配置 ID · 版本 · 组件类型」溯源信息，DOM 挂 data-card-id / data-render-id |
| P02 | 全站 | 交互视觉 | AI 动作是一个普通文字按钮，位置远离输入内容 | 内联 SVG 魔法棒图标按钮，嵌在文本框右下角（触发条件描述 / 智能匹配输入框 / 触发调试），AI 紫色，运行中旋转 |
| P03 | 全站 | 交互视觉 | 多值字段（示例问法）用换行文本框，看不出条数、不好删改 | 标签式输入：回车添加、退格或点 × 删除、去重、到 10 条自动锁定 |
| P04 | 全站 | 交互视觉 | 布尔开关用原生复选框，与工作台 toggle 不一致 | 统一开关行控件（开关 + 标题 + 说明），用于自动触发 / 群体回显 |
| P05 | 全站 | 文案 | bank / support set / judge / tool / 聚合器等英文黑话散落在管理端 | 统一译为 评测数据集 / 相似题集 / AI 评审 / 工具调用 / 总结模型 |
| P06 | 工作台 | 功能逻辑 | 首页只有数据没有「要我做什么」 | 新增待处理条：旧版本引用、未发布修改、待复核样本、完成率低于验收线、熵告警，每项带直达入口 |
| P07 | 工作台 | 前后端 | 待处理数据分别来自 /api/cards、/api/labels/review、overview，失败会阻塞首页 | 待办聚合失败静默降级，不影响 KPI 与配置卡 |
| P08 | 组件库 | 功能逻辑 | 组件卡只有描述与 JSON，管理员不知道「什么时候用、收回来什么字段」 | 每卡事实卡：适用场景 / 回收字段；JSON 示例折叠保留 |
| P09 | 组件库 | 交互视觉 | 智能匹配按钮与输入框分离；ID 胶囊对可读 ID 截断成 select.s | 魔法棒内嵌输入框；shortId 只对 UUID 取前 8 位，可读 ID 全显 |
| P10 | 交互配置 | 前后端 | 列表搜索只匹配名称与恒为空的 description | 服务端搜索扩展到触发条件 / 示例问法 / 提问文案 |
| P11 | 交互配置 | 交互视觉 | 编辑器三步平铺无分区感，短字段单列浪费空间 | 三个白底分区 + 编号徽标；配置名称与自动触发、选项与默认项、量表 / 滑条参数两列或三列排布 |
| P12 | 交互配置 | 功能逻辑 | 编辑器看不到配置 ID、版本、状态 | 顶部元信息条 |
| P13 | 交互配置 | 交互视觉 | 预览只有组件，缺少对话上下文 | 预览用第一条示例问法渲染用户气泡，再接说明文案与组件，贴近真实对话 |
| P14 | 交互配置 | 功能逻辑 | AI 推荐组件只换类型不带内容 | 推荐应用时若识别到选项且当前为空，自动填入选项 |
| P15 | 交互配置 | 功能逻辑 | 「更多」缺少到对话与看板的闭环入口 | 新增「在对话中测试」（带示例问法 ?q= 深链）与「查看数据」 |
| P16 | 交互配置 | 前后端 | 字段标识无格式校验，可能生成非法 JSON key | 客户端校验：英文 / 数字 / 下划线，不能以数字开头 |
| P17 | 交互配置 | 交互视觉 | 关闭编辑器不提示未保存修改 | 有改动时关闭（取消 / ×）需确认，并说明本机暂存可恢复 |
| P18 | 交互配置 | 文案 | 「版本」「卡片」「场景」混用 | 统一为「配置」；版本表状态改为「线上版本 / 历史版本」；空版本表有空态 |
| P19 | 交互配置 | 交互视觉 | 调试结果参数 JSON 直接铺开 | 命中行显示配置 ID + 组件 + 「编辑该配置」，参数折叠；相似度条加命中线刻度 |
| P20 | 对话演示 | 交互视觉 | 无消息时大片空白 | 空态提示；新会话后恢复空态 |
| P21 | 对话演示 | 文案 | 「路由执行过程」「详情」对终端用户是黑话 | 改为「AI 处理过程」「为什么选它」；头部说明改为面向演示者的一句话 |
| P22 | 对话演示 | 功能逻辑 | 从配置页无法直接验证触发效果 | 支持 ?q= 预填问题并提示回车发送 |
| P23 | 模型路由 | 文案 | 表头「bank 覆盖率」「价格（入/出 $/M）」、能力 chip「tool」、状态 chip 过长 | 改为「数据集覆盖率」「单价（输入 / 输出，$/百万 token）」「工具调用」；状态短标签 + 悬停解释 |
| P24 | 模型路由 | 交互视觉 | 策略卡平铺 9 个数学参数 chip | 只显示「候选 N 路」「聚合阈值」，高级参数悬停；场景 key 中文化；策略 ID 胶囊 |
| P25 | 模型路由 | 文案 | 数据集层命名「公共底座 bank / 租户增量 bank」，标签来源英文 key | 「公共评测数据集 / 租户回流数据集」，标签来源中文化，领域分布中文化 |
| P26 | 运营看板 | 前后端 | 成本走势 Y 轴被强制 ≥1，小数成本曲线被压平 | 折线图按真实最大值缩放 |
| P27 | 运营看板 | 文案 | 成本按模型显示「0.568 $」；延迟表出现未映射的 manual | 美元格式 $0.568；补「手动指定」「等待用户（触发组件）」映射 |
| P28 | 运营看板 | 文案 | 「Token 四分口径」「标签产出速率」「模型选择熵」不自解释 | 改名并补悬停口径说明 |
| P29 | 运营看板 | 功能逻辑 | 配置卡无 ID，无法与回收数据对账 | 配置 ID 胶囊 |
| P30 | 链路追踪 | 文案 | span 名称「粗粒度打分 / 自适应开关 / 聚合重写」为内部术语；组件渲染不显示配置 | 业务化命名；组件渲染 span 显示配置 ID 与版本、触发方式中文 |
| P31 | 用户端组件 | 交互视觉 | 表单必填用「（必填）」后缀 | 统一星号标记 |

## 九、第三轮：信息架构、文案密度与视觉复审（v2.3）

| 编号 | 范围 | 问题 | 优化 |
|---|---|---|---|
| N01 | 导航 | 顶部平铺 7 个入口，无层级，对话演示与管理功能混在一起 | 改为左侧固定导航，按「工作台 / 智能交互 / 模型路由 / 运营分析」分组；对话演示更名「对话测试」并入智能交互；窄屏自动收为图标栏 |
| N03 | 导航 | 左侧二级导航与页内横向 tab 同时存在（模型路由 5 个 tab、看板 2 个 tab），同一层级出现两处入口 | 删除页内 tabbar：模型路由与看板均按 #hash 渲染板块，左侧导航为唯一入口并随 hash 高亮；看板两个视角提升为侧栏二级「交互看板 / 路由看板」；页面标题与浏览器标题随板块联动 |
| N02 | 命名 | 「富交互」是内部技术词 | 界面统一为「智能交互」（看板 tab、模块名、说明书） |
| C01 | 全站 | 每页顶部一段产品说明、每个卡片一句解释、每个字段一行提示，文案密度过高 | 页面说明全部收进标题旁的信息图标；字段提示删除、示例移入 placeholder；分区副标题删除；导出 / 复制 / 清空等改图标按钮；toast 与确认框缩到一句 |
| C02 | 工作台 | hero 段落、统计范围说明、各区块副标题 | 全部删除；统计范围分段控件移入 hero 右侧；快捷入口改为图标 + 名称一行 |
| C03 | 对话测试 | 头部三行说明 + 模式提示语 | 删除，仅保留标题 + 信息图标 + 示例问题；模式说明改为按钮悬停 |
| C04 | 模型路由 | 页面导语、模型池说明段、调参弹窗导语、回流链路整段说明、数据集层长注释 | 删除或收进信息图标；策略卡高级参数改为信息图标悬停 |
| C05 | 运营看板 | 页面导语、每张图表的副标题、KPI 长口径 | 删除；口径收进 KPI 标题旁信息图标；「数据不全」不再作为大数字展示，改为「-」+ 小字原因 |
| V01 | 组件库 | 组件卡是「标题 + 裸组件 + 事实表 + JSON」，看不出它在对话里长什么样 | 改为迷你对话：用户气泡（真实问法）→ AI 头像气泡（说明文案 + 组件），8 类组件各配一个真实业务场景 |
| V02 | 组件库 | 组件标识是普通胶囊，对接方无法直接复制使用 | 改为 API key 样式字段（label / 等宽 key / 复制），并提供「调用示例」复制完整协议信封 |
| V03 | 组件库 | 组件视觉跟随品牌 token，但管理员看不到也拿不到 token | 新增「品牌风格 Token」面板：JSON 复制 / 下载、色板与字体圆角摘要、接入方式代码块 |
| V04 | 图标 | 魔法棒图标由多颗碎星拼成，16px 下糊成一团 | 重绘为一根斜杖 + 杖头实心四角星 + 一粒光点，描边 2px，小尺寸清晰 |
| V05 | 图标 | 导航与按钮无图标，纯文字 | 新增一套 1.6px 描边线性图标（home / grid / sliders / chat / cpu / layers / route / split / refresh / chart / activity / download / copy / info 等），统一 16px、随 currentColor 着色 |
| V06 | 编辑器预览 | 预览只是灰底上的组件 | 与组件库同一套对话式容器：示例问法气泡 + AI 气泡（说明 + 组件 + 溯源行） |
| V07 | 看板 | 组件触发分布仍有 timeline / citation.card 等英文 | 补齐呈现型组件中文名 |
| V08 | 表格 | 模型池表头过长导致按钮与标签换行 | 表头精简、chip / 按钮 nowrap、关键列 min-width |

## 十、第四轮：AI 功能复审与业务化改造（v2.4）

AI 功能盘点结论：智能匹配（预填内容渲染预览）、AI 推荐组件（不覆盖已填内容）、智能路由解释（为什么选它）、AI 评审补标签（人工复核把关）交互合理保留；两处不合理已改造，见 A1 / A2。

| 编号 | 范围 | 问题 | 改造 |
|---|---|---|---|
| A1 | AI 改写触发条件 | 点魔法棒直接覆盖管理员原文，无对比、无撤销，违背「AI 建议须可审阅」原则 | 改为 AI 建议卡：改写稿与示例问法先以预览卡展示（紫色边框 + AI 标识），管理员选择「采用 / 重试 / 放弃」，采用才写入表单 |
| A2 | 回流优化 | 概念多（bank / 标签 / 置信度 / judge），管理员不知道该做什么 | 重构为三步卡「1 收集反馈 → 2 人工复核 → 3 更新画像」，每步带数字与行动按钮；待复核表列改为「AI 的判断（答对了 / 答错了）+ 依据」；数据集缩为两行摘要；导入与 AI 评审收进「高级工具」折叠 |
| A3 | 链路追踪 | span 瀑布是工程视角，业务管理员看不懂 | 更名「对话记录」，详情改为业务叙事时间线：用户提问 → 命中配置（可跳编辑）→ 用户选择 → 模型回答（方式 / 耗时 / 成本）→ 反馈沉淀；span 瀑布收进「技术详情」折叠；筛选项改业务语言（触发了组件 / 快车道直答 …） |
| A4 | 对话测试 | 页面是「卡片 + 列表」布局，不像真实助手 | 重构为标准助手形态：细顶栏、居中欢迎页（头像 + 问候 + 建议卡）、消息流独立滚动、助手消息带头像、用户气泡右侧、底部胶囊输入框 + 圆形发送按钮（生成中变红色停止），模式切换收进输入框上方 |
| A5 | 设计系统 | 只能看和复制 token，无法导入自己的品牌 | 新增「设计系统」页：当前 Token（色板 / 字体 / 圆角 / 图表色 + 复制 / 下载）、导入品牌 Token（粘贴 JSON → 客户端与服务端双重校验 → 预览应用（临时生效可还原）→ 保存为品牌，POST /api/brands 落盘，出现在品牌切换中）、组件跟随效果实时预览；组件库入口指向此页 |
| A6 | 模型路由 | 「注册新模型」为工程词；A/B 实验对当前业务阶段过重 | 改为「添加模型」；移除 A/B 实验入口与页面（后端能力保留） |
| A7 | 组件卡 | 对话气泡用品牌蓝，与卡片外的主按钮同色难分层 | 用户气泡改深海军蓝渐变，与操作按钮明确区分；「用它新建配置」改为「创建配置」 |
| A8 | 交互配置列表 | 行内信息拥挤 | 行高与间距加大、名称加大、ID 与版本移至次行、次要标签精简 |

## 十一、第五轮：体验路径闭环与视觉精修（v2.4.1）

沿第五章 P0-P7 路径走查，修复三处断链，并做一轮视觉质感精修。

| 编号 | 类型 | 内容 |
|---|---|---|
| U1 | 路径闭环 | 发布成功不再只弹 toast：出「已发布」对话框，主按钮「在对话中测试」带第一条示例问法深链到对话测试（P0/P1 终点接到 P5 验证） |
| U2 | 路径闭环 | 工作台空态改为三步上手引导卡（认识组件 → 创建配置 → 对话验证），每步带直达按钮 |
| U3 | 路径闭环 | 交互看板每张配置卡新增「编辑」图标，发现数据问题可直达编辑器（P2 闭环） |
| V1 | 视觉 | 卡片圆角 10 → 12px（token 同步），阴影改为更柔的双层投影；KPI 块去描边纯白浮起 |
| V2 | 视觉 | 主按钮改纵向渐变 + 双层投影，hover 提亮、active 压暗；输入框 hover 描边加深 |
| V3 | 视觉 | 侧栏激活项加左侧 3px 指示条；页面标题 20 → 22px；表头加浅底；弹窗圆角 14px |
| V4 | 视觉 | 全局细滚动条与选区色，贴合品牌 |

## 十二、第六轮：Sky 主题与业务化收敛（v2.5）

| 编号 | 需求 | 落地 |
|---|---|---|
| S1 | 按用户提供的 tailwind/oklch token 重做视觉 | 全站换 Sky 主题：白底 + 浅灰卡片（#F7F8F8）、主色天蓝 #1DA1F2、圆角 1.3rem（卡片 20px / 控件 12px / 按钮胶囊）、阴影全部关闭改描边分层、Open Sans 字体、图表色随 token；brand-tokens.default.json 同步 |
| S2 | 选项背后可配 prompt / 服务 API | 编辑器每个选项可展开「后续动作」：AI 跟进 prompt + 服务接口（https 校验）；存 config.option_actions；组件信封下发；用户提交后服务端按所选项执行——有 API 则在思考过程中显示「调用服务接口 …（演示模拟）」并记 tool_call span，有 prompt 则按其跟进回答；端到端已验证 |
| S3 | 组件库草稿箱 | 组件库顶部分段「组件模版 / 草稿箱 N」：草稿箱列出未发布配置（含「线上 vN」标记），一键继续编辑 |
| S4 | 模型选择收进对话框 | 移除调用模式分段，输入框左侧内嵌模型下拉（自动选择为默认 + 各在线模型），参考 ChatGPT / Claude；选定即手动模式 |
| S5 | 命名重审 | 设计系统→设计风格管理、模型池→模型管理、数据集与画像→模型画像、路由策略→路由规则、回流优化→反馈优化、注册新模型→添加模型、数据契约→接口契约、链路追踪→对话记录（前轮） |
| S6 | 设计风格管理重构 | 版式改为：上方 token 代码块（可编辑，附色板/复制/下载/恢复默认）→「生成」应用预览 → 下拉切换 8 类组件查看渲染效果 →「应用并保存」；一次只有一套风格，移除侧栏品牌切换与品牌列表 |
| S7 | 触发调试改对话框形式 | 调试区变成迷你对话：用户气泡提问 → AI 头像气泡回复，命中时直接渲染真实组件（含配置 ID / 编辑入口），相似度收进折叠 |
| S8 | 表单/组件溢出排查 | AI 改写建议卡文本限高滚动 + 断词；chip 全局 260px 省略 + title；示例问法标签省略；组件选项胶囊省略；key 字段已有省略 |
| S9 | 选项独立填槽 + 默认「提交」可见 | 选项从大文本框改为逐行填槽（文本 + 后续动作 + 删除 + 添加选项），推荐默认项联动；按钮文案默认值「提交」以值形式直接显示 |
| S10 | 删除自动触发开关 | 编辑器与列表中移除 model_invokable 开关与标记，触发条件恒为必填 |
| S11 | 去掉 A/B 实验 | 侧栏与页面移除（后端能力保留） |
| S12 | 反馈优化简化（前轮）+ 信息去重 | 工作台：删除与看板重复的选项分布与路由四宫格，改为一行路由摘要链接；模型画像页删除与反馈优化重复的数据集三块，画像版本与数据来源缩为一行 |

## 十三、第七轮：信息架构定型与向导化（v2.6）

导航定型为三大模块 + 底部账户（头像 + 昵称 + 角色）：

```
智能交互    组件工作台（原工作台 + 交互配置合并：KPI + 待处理 + 状态 tab 列表 + 行内回显开关 + 对话式触发调试）
            组件库（搜索 + 类型筛选 + 草稿箱 tab + 固定分区卡片：头/对话预览/底部组件 ID 与调用示例）
            交互测试 · 风格管理
模型路由    模型工作台（接入 / 启停 / 默认兜底）
            调度策略（模型画像 + 路由规则合并：α + 簇×模型矩阵 + 策略列表，参数配置改为右侧抽屉，去掉历史与回滚）
            反馈优化（收集反馈 → 更新画像，低置信自动降权）
数据分析    交互数据 · 路由数据（对话记录入口移除，仅保留下钻深链）
```

| 编号 | 需求 | 落地 |
|---|---|---|
| T1 | 新建配置分步向导 | 弹窗两步：第 1 步描述场景 → 魔法棒 AI 匹配（推荐可选用）或手动选组件；第 2 步设计组件（描述 / 话术 / 选项 + 右侧对话式预览），可返回上一步 |
| T2 | 组件有效期 | 编辑器新增生效 / 失效日期（选填，校验先后）；服务端触发匹配按有效期过滤；列表显示「未生效 / 已过期 / 限期」标记 |
| T3 | 状态筛选 tab 化 | 组件工作台列表：全部｜已上线｜已下线｜草稿箱（带计数），替代下拉框 |
| T4 | 组件卡片布局固定 | 组件库卡片三分区：头部（名称 / 类型 / 创建配置）固定高、对话预览自适应、底部组件 ID + 调用示例恒在同一位置（描边分隔） |
| T5 | 待复核不脱敏 | 复核需看原文判断，/api/labels/review 返回原文，表头去「脱敏」字样 |
| T6 | 看板分区与导出 | 交互数据页：删除搜索与 CSV 工具栏；新增「导出看板数据」（汇总 + 各配置一行的 CSV）；「整体表现」（灰底指标）与「组件交互情况」（白底 + 左侧主色条卡片）拉开样式差异并加标题 |
| T7 | 参数配置抽屉 | 路由规则「调参」更名「参数配置」，右侧抽屉编辑（Esc / 遮罩关闭，滑入动画）；隐藏 A/B 实验组策略 |

## 十四、第八轮：测试抽屉与全站复审循环（v2.7）

- 测试能力重构：触发调试从页面卡片改为「测试」按钮 + 右侧抽屉对话流（组件工作台验证触发；模型工作台版带模型型号选择、思考过程常驻展开）；交互测试独立页入口移除，发布成功与「更多·在对话中测试」均改为拉起抽屉；共享 testchat.js，走线上同一条 /v1/route 链路，测试数据照常回流
- 草稿相关二次弹窗移除：本地草稿静默恢复（轻提示），关闭编辑器不再确认（内容自动暂存）

复审循环（挑剔视角逐页 → 系统性修正 → 复跑验证）修正清单：

| 编号 | 页面 | 问题 | 修正 |
|---|---|---|---|
| C1 | 全站 | 状态词不统一：tab「已上线」vs chip「已发布」 | 统一为「已上线」（列表、看板、toast） |
| C2 | 组件工作台 | 列表行不可点，必须点小「编辑」按钮 | 行主体点击即编辑 |
| C3 | 新建向导 | 步骤信息三处重复；场景提示与 placeholder 重复；禁用主按钮仍亮蓝似可点；组件选中无勾选 | 精简为单层级；提示收进 ⓘ；禁用主按钮灰化；选中卡右上角对勾 |
| C4 | 编辑器 | 第二步铺 8 个禁用组件卡（类型已锁定），空间浪费；有效期低频却占一等位置 | 组件改为一行摘要（chip + 描述 + 更换指引）；有效期收进折叠 |
| C5 | 组件库 | 组件 ID 框拉满整行、调用示例孤角 | 底栏两端对齐、ID 框自适应宽 |
| C6 | 风格管理 | 主次按钮颠倒（「生成」主色、「应用并保存」描边） | 应用并保存为主色，生成为次 |
| C7 | 交互数据 | 埋点健康 3 块与业务 KPI 同权重 | 降为一行摘要 + 达标/低于验收线 chip |
| C8 | 路由数据 | Y 轴出现 -0.000；标签产出图例英文；「明细」语义含糊 | 零值格式修正；图例中文化；改「查看明细」 |

## 十五、第九轮：十二类组件体系与五步新建弹窗（v2.8）

组件体系定型为 12 类「器」（协议新增 picker.timerange）：

| 组件 | component_type | 说明 | 可选样式 |
|---|---|---|---|
| 文本选择器 | select.single / select.multi | 单/多选合一，编辑器内「选择方式」切换 | 列表 / 胶囊 / 输入框浮现 |
| 卡片选择器 | select.card | 标题（12 字）+ 文案（40 字）+ 配图（本地上传 ≤200KB 或 URL） | 卡片 / 列表 |
| 优先级排序器 | rank.priority | 候选按重要程度排序 | - |
| 表格选择器 | matrix.compare+select | 多维指标对比后选定 | - |
| 评分器 | scale.likert | 打分类反馈，刻度可选 | 数字圆点 / 分段条 |
| 信息登记表 | form.structured | 多字段一次收齐 | 逐字段 / 两列 |
| 数值选择器 | slider.range | 区间内取一个数 | 滑杆 / 步进器 / 直接填写 |
| 备注填写器 | input.followup | 一段自由文字 | 多行 / 单行 |
| 日期选择器 | picker.datetime | 选一个日期（可含时间） | 日期 / 日期+时间 |
| 时间段选择器 | picker.timerange | 选一段起止（新增） | 日期段 / 时间段 |
| 地点选择器 | picker.location | 常用地点或直接输入 | 列表+输入 / 仅输入 |
| 文件上传器 | upload.file | 单据凭证类附件（演示不真实上传） | - |

| 编号 | 需求 | 落地 |
|---|---|---|
| N1 | 新建弹窗固定尺寸、分步 | 弹窗定格 1080×720（自适应上限），标题、步骤条、右侧预览、底部操作栏固定，仅中部表单滚动；新建五步：选择组件 → 触发条件 → 配置组件 → 确认 → 获取 Key；编辑态三步可自由点击切换；每步只校验本步字段，保存时若错误在其他步自动跳回 |
| N2 | 选择组件步 | 业务场景输入 +「推荐组件」按钮（icon+文字，替换原 AI 配色魔法棒；推荐结果呼吸光晕 + 行内「最推荐」chip 与标题水平对齐）；组件卡一句话场景介绍（不写配置细节）；未选组件「下一步」置灰 |
| N3 | 配置组件步 | 组件用下拉框切换（编辑已有配置锁定并说明原因）；文本选择器带「选择方式：单选/多选」；提问输入加重样式（加大加粗浅底）与普通字段拉开差异；选项行 = 文案 + 跳转链接（选填，https 校验）+「AI 跟进」话术（选填，展开配置）；卡片选择器每张卡打包为浅框（标题/文案/链接/配图） |
| N4 | 确认步 | 配置摘要表（名称/触发/示例问法/有效期/组件/提问/内容规模，逐项「修改」回跳对应步）+ 数据回收（回显开关、Webhook） |
| N5 | 获取 Key 步 | 保存/发布后进入：结果横幅（已发布上线 / 已存草稿）+ 配置 ID（Key）+ 前端嵌入代码 + 接口契约（推送 payload / 拉取聚合接口），均一键复制；替代原组件库内复制组件 ID 的入口 |
| N6 | 回显面板重设计 | 提交后：结论头（N 人回答 · X% 和你选了一样 / 你是第一个回答的人）+ 分布行（本人选项标记对勾、进度条、百分比）+ 文本回答引用样式；加载与失败均有轻量文案 |
| N7 | 组件库去轮播 | 每类只陈列单一默认样式（去左右滑动切换），底部注明「可选样式：…（创建配置时选择）」；创建配置支持 ?new=1&ct= 深链 |
| N8 | 导入品牌风格页 | 「风格管理」更名「导入品牌风格」（含导航）；预设风格卡（默认 Sky + 已导入品牌）点击即全页预览 +「导入自有风格」卡展开 token 编辑器；按钮只保留「生成预览 / 应用并保存」；下方 12 类组件示例网格实时反映风格（去组件下拉与色板） |
| N9 | 删除人工复核 | 反馈优化页简化为「收集反馈 → 更新画像」两步，待复核表与 /api/labels/review 接口移除，AI 评审产出直接并入回流数据集；工作台待办同步去掉「待复核」项 |
| N10 | 视觉统一 | 画布底色浅蓝灰 #EEF2F6 与白卡拉开层次；日期/时间原生输入统一为文本输入同款外观（描边/圆角/高度/图标透明度）；textarea 去右下角缩放手柄；组件圆角统一（组件 12 / 选项 10）；列表行「编辑/下线」为文字按钮、回显开关带文案 |
| N11 | 工作台列表按钮稳定 | 每行固定四个槽位：回显开关（未上线置灰并说明）/ 编辑 / 状态操作（下线 / 上线 / 发布）/ 更多；删除「编辑中 · 线上 vN」态与列表内「发布新版本」按钮——有生效快照即显示为已上线，发布新版本只在编辑器内发生；服务端允许编辑中的已上线配置直接下线 |
| N12 | 统计口径统一 | 「已上线」口径全站统一为「有生效快照」（含编辑中的草稿）：工作台 KPI、状态 tab 计数、交互数据看板同源；看板文案「渲染」统一为「触发」，评分类分布补「分」单位 |
| N13 | 模型工作台表格精简 | 「默认兜底」独立列（几乎全空）收进模型名旁 chip；覆盖率 100% 且非回填中时只显示文本（去掉恒满的进度条）；「重建画像 / 更新画像」按钮改普通配色 |
| N14 | 品牌风格只作用于组件 | design token 的作用域从全站收敛到 .brand-scope（渲染出的交互组件）：导入 / 切换风格只改对话中的组件（含嵌入客户前端的组件），管理平台界面不受影响；页面文案同步说明；「导入自有风格」改为「导入设计风格参数」并用主色虚线卡与预设风格拉开差异 |
| N15 | 预览始终是完整业务对话 | 每类组件内置兜底场景（提问 / 用户问法 / 说明文案 / 选项），字段未填时预览用真实业务 mock 而非空白或「选项一」占位；模板默认内容同步业务化（开专票/普票、海运直达/海铁联运、时效最快/成本最低/风险最小、订单号/联系电话等）；预览列加宽到 420px，组件不再换行挤压；删除预览下方「提交后将展示群体选择分布（回显）」提示句 |
| N16 | 选组件步 AI 按钮进输入框 | 「推荐组件」改为「AI 自动匹配组件」，作为文字按钮内嵌在业务场景输入框右下角（输入区自动避让） |
| N17 | 细节 | 新 logo（对话气泡内含组件条的单色标）；优先级排序器重绘（圆形序号徽标、首位主色、到顶/到底箭头置灰、行式卡片） |

## 十六、第十轮：整改闭环与体系化（v2.9）

### 16.1 整改三批（对应第十五章后的超级问题清单）

| 批次 | 内容 | 已闭环问题 |
|---|---|---|
| 守门 | 发布强校验下沉服务端（草稿宽 / 发布严）；回显接口校验开关 + 文本截断；策略 / 模型注册参数校验（探索预算 0-1、荒谬值软警告）；搜索 LIKE 转义；CSV BOM + 公式注入防护；组件提交按 (user, render_id) 幂等；测试抽屉流量打标 channel=test，不进回显 / 看板 / 标签；线上服务中的配置禁止直删 | L1 / L4 / L12 / L16 / L5 / L3 / Q34 / Q36 / Q37 |
| 数据主键与生命周期 | 选项位引入 option_ids，发布时按位识别改名生成别名链（链式压缩），回显与看板按别名归并——改文案不再裂数据；多选后续动作逐项执行（跳转逐个触发、AI 跟进合并）；改名查重；回滚拆分为「载入到草稿 / 回滚并发布」；原样重新上线不升版本不惊动引用方；新建草稿恢复改为询问；Esc 只关最顶层弹窗；编辑器明示「线上正在服务 vN · 当前草稿未发布」；评分分布按分值排序补「分」 | Q29 / Q30 / L2 / Q26 / Q27 / Q24 / Q25 / Q1 / Q32 |
| 承诺对齐与 AI 可信 | 嵌入代码改为可落地的接口示例（不再引用不存在的 SDK）；契约签名注明演示未启用；品牌风格升级为租户级设置（服务端存储，多端一致）；品牌导入查重确认 + 可删除 + 填入示例；AI 推荐低分不硬推、推荐 chip 分级；AI 改写「采用」行为一致（追加去重）；发布相似度预警（相近触发条件非阻断提示）；审计日志页（关键操作可查）；退役入口、删凭证改名「删除凭证并暂停」 | L6 / Q8 / L-brand1 / Q41 / Q38 / Q39 / L10 / Q43 / L14 |

### 16.2 设计系统（第九轮定稿）

- 字号五档（token 化，页面不出现此外字号）：hero 20 页面标题 / title 15 卡片标题 / base 13 正文控件 / small 12 辅助 / caption 11 标注；字重只用 400 / 600 / 700
- 原生控件收编：下拉框自绘 chevron、展开框（details）自绘旋转箭头、勾选框主题色、滑杆全自绘（修复聚焦外框与配色异常）、日期 / 时间输入与文本输入同外观
- 防连点与稳定性：编辑器 / 测试抽屉 / 参数抽屉 / 添加模型弹窗单例守卫；列表请求序号防竞态；编辑器局部重绘保持滚动位置（仅切步回顶）；组件溯源信息悬停展示

### 16.3 组件体系 16 器（v2.9 增补）

| 新增 / 变更 | 说明 |
|---|---|
| 追问引导器 suggest.followup（新，引导类） | 回答下方 2-4 个可点引导项，三种去向：不配动作 = 作为追问发给 AI；配 AI 跟进 = 按话术接管回答；配跳转链接 = 打开网站 / 服务入口（工单、官网运单页、小程序深链等）。点击即触发，无提交按钮，点击计入分布 |
| 图片上传器 upload.image（新） | 拍照 / 相册上传 + 缩略图预览；与文件上传器（PDF / Excel 单据）拆分；隐私：payload 只记类型与大小，不上传原始文件名 |
| 地点选择器（重构） | 标准地址选择：搜索框 + 常用地点置顶 + 地址明细行 + 自由输入兜底；选中后展示 OpenStreetMap 实景地图（真实坐标 marker）与详细地址 |
| 商品下单器 commerce.order（新，交易类） | 商品卡列表：方形倒角商品图（无图取首字占位）、名称、详情、标签、销量、现价 + 划线原价 + 自动折扣角标；数量步进、多件自动合计、下单按钮；payload 含 order_items 与 order_total |
| 入口跳转器 entry.link（新，引导类） | 通栏浅底入口横条：圆形图标（可上传）+ 入口名 + 副标题 + 右箭头，点击直达专区 / 活动 / 服务页（参考支付宝阿宝的专区入口形态）；「提问」可留空，横条紧跟回答 |

### 16.4 看板可视化（可视化分析师视角重构）

- 完成漏斗：真·漏斗形态（居中收窄、同一蓝色三档渐深、阶段间转化率标注、放弃作为流失注脚）
- 埋点健康：从一行灰字升级为独立模块（接收量 + 判定 chip + 生成率 / 字段完整率双 meter，对 99% 验收线画阈值刻度，未达标红色）
- 推荐默认项：改单条 100% 堆叠占比条 + 图例 + 结论文案（接受率低时提示复查默认项）
- 组件交互卡按临近原则分层：头（名称 + 类型 + 操作）→ 四格迷你指标（触发 / 提交 / 回答人数 / 完成率，低于验收线标红）→「提问」与其回答分布紧邻 → 底部元信息（配置 ID / 回显态）

### 16.5 业务化数据（供应链基地客服日常）

- 新增客服场景配置：预约上门取件（时间段）、收货地址确认（地点 + 地图）、破损凭证上传（图片）、查件后追问引导（追问 / 催办工单 / 官网运单页）、增值服务下单（加固 / 保价 / 防潮包装）、优惠专区入口（神券团购 / 增值服务商城）
- 语料新增 service 客服域（延误 / 改约 / 取件 / 赔付 / 查件 / 发票），路由画像随之出现客服簇；模拟回答含客服话术
- 看板注入真实感交互事件（含渲染 / 开始交互 / 提交全链路），完成率口径不再出现「提交大于触发」

## 十七、第十一轮：组件交互系统与深度组件走查（v3.0）

### 17.1 组件交互系统（结构层，与品牌风格 token 正交）

品牌 token 管观感（色 / 圆角 / 字体），交互系统管结构（尺寸 / 密度 / 状态），互不冲突：

| 规则 | 落地 |
|---|---|
| 结构 token | --comp-opt-h 44（可点行定高）/ --comp-card-h 78 与 --comp-card-media-h 152（图文卡定高）/ --comp-media 72（媒体位）/ --comp-touch 40 与 --comp-touch-sm 28（触点下限） |
| 定高不撑高 | 卡片标题单行省略、文案两行 clamp；整组件超 340px 自动折叠出「展开全部」 |
| 五态 | rest / hover / selected / disabled / submitted 全组件对齐；提交后冻结 |
| 重置 | 用户交互后组件右上出现「重置」，一键恢复初始状态；提交后随冻结消失；重渲染不重复计触发 |
| icon 三档 | 全站交互 icon 统一 currentColor + 1.6 描边，尺寸只用 12 / 14 / 16 |

### 17.2 本轮组件变更

| 变更 | 说明 |
|---|---|
| 物流轨迹卡 track.map（新，呈现类·模型数据驱动） | 查件场景由 AI 带出：OpenStreetMap 定位当前位置 + 当前节点状态 chip + 节点时间线；组件库以「模型数据驱动」标识陈列，不走创建配置 |
| 地点选择器（再修） | 选完地址不再展示地图（地图移交轨迹卡）；保留搜索 + 常用置顶 + 确认地址卡 |
| 商品下单器（重设计） | 参考支付宝阿宝形态：72px 圆角图（无图渐变首字位）、名称 / 详情单行省略、已售 + 标签、大红价 + 划线原价 + 主色折扣 chip；未选时「下单」主色按钮，点击转数量步进，合计条 + 提交订单 |
| 日期 / 时间段选择器 | 增加快捷选项：今天 / 明天 / 后天；上午 / 下午 / 晚上、今明两天 / 未来三天 / 未来一周 |
| 表格选择器 → 对比选择器 | 名称按用途更名 |
| 组件库陈列 | 名称下增加「适用于 XXX 场景」一行描述；删除 ⓘ；类型筛选增加引导类 / 交易类 / 呈现类 |
| 工作台列表三层重排 | 识别层（名称 / 状态 / 类型 / 告警）→ 内容层（触发条件独占一行）→ 元数据层（ID / 版本 / 时间 / 统计小字）；右侧操作两行（回显开关 / 编辑·下线·更多），更多统一为测试 / 查看数据 / 复制 / 删除，版本历史移入编辑器步骤条 |
| 审计日志 | 导航移至底部账户上方（低频入口沉底） |
| 参数配置抽屉 | 重排为「路由方式 / 成本与探索 / 高级参数（折叠）」三组、双列、开关化 |
| 看板 | 四小卡合并为「转化表现」「触发与采集」两整卡；漏斗与堆叠条颜色改走图表 token 色阶 |

## 十八、第十二轮：数据管道页重写与全站走查闭环（v3.1）

| 变更 | 说明 |
|---|---|
| 反馈优化页重写 | 按数据机制从头设计：管道总览三卡（冷启动底座只读 / 回流数据集对启用阈值进度 / 画像版本 + 待入画像增量 + 更新按钮）；信号源与闸门（择优 0.9 / 赞踩 0.6 / 隐式 0.25 / AI 评审 0.5，各带 30 天计数与开关，关闭即停止入库）；数据集补给完成论文闭环——支持只导题，「运行评测回填」让在线模型逐题作答打分后入库 |
| 服务端配套 | kv_settings 设置表、信号源闸门（events 入库前过滤）、/api/labels/summary、/v1/bank/eval-pending（评测回填）、AI 评审开关闸门 |
| 转化看板 | 从交互数据页拆出为独立导航项（转化表现 + 触发与采集两整卡）；交互数据页专注每配置数据卡（去左侧色条） |
| 自绘下拉 fancySelect | 路由数据筛选器（调用模式 / 模型 / 结果状态）与时间窗、组件风格的组件筛选全部换为主题弹层下拉，不再出现系统原生样式 |
| 组件风格两列化 | 左列选择 / 导入风格（导入改为独立按钮展开编辑器），右列下拉选组件、单组件预览 |
| 添加模型两步化 | 第 1 步填接入信息（显示名 / ID / 接口 / vault 凭证）做连接校验 → 第 2 步自动带出服务商目录单价与能力（演示模拟，可修改）后添加 |
| 工作台 | 状态 Tab 改下划线式 + 计数徽标；列表行三层信息（识别 / 触发独占行 / 元数据小字），右侧操作两行；更多菜单统一为测试 / 查看数据 / 复制 / 删除，版本历史移入编辑器步骤条 |
| 编辑器 | 步骤定名「选择组件 / 触发条件 / 设计内容 / 数据收集（基础信息两卡 + 收集设置）/ 获取配置 ID」；上一步靠左、下一步统一文案；发布风险提示落到获取配置 ID 页横幅；追问引导器去向显式配置（追问 / AI 跟进 / 跳转三选一）；删除新建草稿恢复询问（新建即白纸） |
| 操作日志 | 「审计日志」更名，导航沉底至账户上方，支持导出 CSV |
| 走查闭环 | 20 个界面态截图逐项评审：修复导航改名未生效、策略抽屉崩溃（foot 丢失）、转化看板空白（变量作用域）、重复选项校验死代码、模型页标题、两条测试脏数据清理；参数抽屉与添加模型弹窗补走查深链（?policy=1 / ?register=1）。UI/UE 复检二轮：修复分段控件（显示样式 / 选择方式）样式退化、组件描述行间距 |

## 十九、第十三轮：组件层级合并、15 器与弹窗规范（v3.2）

### 19.1 组件体系收敛为 15 器

| 变更 | 说明 |
|---|---|
| 追问引导器并入入口跳转器 | 每个入口的「去向」三选一：不配置 = 作为追问发给 AI、配 AI 跟进话术 = AI 按话术接管、配链接 = 直达专区 / 服务页。模板库、种子数据与线上配置同步迁移，旧渲染器仅保留兼容 |
| 地点选择器 → 地址卡片 | 平台拿不到用户收货地，语义改为「商家把配置好的仓库 / 门店地址推给用户确认」：地址卡片列表 + 选中态，>4 条出筛选；免输入、去地图 |
| 信息登记表 → 表单收集工具 | 表单式一次收集多个字段（订单号、电话等） |
| 对比选择器表格化 | 本质是表格对比工具：编辑器提供「方案 × 维度」值矩阵可配（留空自动演示值）；渲染改紧凑表格（定宽列 / 整行可点 / 综合列主色高亮），不再有独立按钮列 |
| 评分器 | 打分点与两端文案分两行，不再挤一行 |
| 优先级排序器 | 上下箭头改为拖拽把手（HTML5 drag，拖到任意位置插入） |
| 时间段选择器 | 淘宝取件式：左列日期（今天 / 明天 / 后天…）右列两小时时段，过期时段置灰标「已过期」；日期模式保留起止 + 快捷 chips |
| 购物卡片（商品下单器）紧凑化 | 图片 76px，右侧仅「名称 / 已售 + 标签 / 价格 + 去下单」三行，价格与按钮收进一张图片高度内；折扣角标行内化。去掉底部「提交」栏，改为合计行内「共 x 件 · 合计 ¥xx · 提交订单」 |

### 19.2 组件层级合并与内部设计系统

| 变更 | 说明 |
|---|---|
| 三层信息合并 | 原「灰气泡（模型话术）— 白卡标题（组件文案）— 白卡交互内容」三层压成两层：组件文案并入对话流灰气泡（加粗提问行），白卡只留纯交互内容。预览、组件库、对话测试、终端对话四处统一 |
| 组件内部设计系统 v1 | 容器内边距恒定（12/14），子元素 10px 垂直节奏、自身不带外边距；组件文字整体比对话文本低一档（标题 base/600、正文与选项 12、标注 11、金额 16）；结构 token（选项高 40 / 卡片高 72 / 触点 40·28）；五态（rest/hover/selected/disabled/submitted）；超 340px 折叠出「展开全部」；提交后冻结 + 重置按钮 |
| 群体回显收敛 | 仅选择类（单选 / 多选 / 卡片）、对比选择、商品下单、入口跳转（统计多少人点开）可开回显；其余组件在数据收集步不出现该开关。回显面板作为独立区块插在提交栏上方 |

### 19.3 新建弹窗：规范与流程

| 变更 | 说明 |
|---|---|
| 弹窗设计规范 v1 | 字号五档（16 弹窗标题 / 15 区块标题 / 13 正文与输入 / 12 标签与辅助 / 11 标注）；间距 8 点制（字段间恒 16、label→控件 6、区块间 28、表单列底 24）；控件规格（输入高 36 圆角 12、segmented 高 32）；步骤条加宽（胶囊 6×14、间隔 8） |
| 选项编辑块 | 每个选项一个浅底块：行 1 文案 + 删除、行 2 跳转链接、行 3「AI 跟进」开关（开启才展开话术输入）；「添加选项」为同宽虚线块，与选项块对齐 |
| 选择组件步重构 | 全部组件网格置顶（定高滚动），下方 12 个业务场景标签点选即推荐组件（再点取消）；删除自由文本 + AI 匹配。「登记多项信息」标签更名「收集信息」 |
| 步骤条自由跳转 | 步骤条可点直达任意步，唯一门槛：未选组件不能离开第 1 步；第 2 步定名「交互配置」 |
| 字段与校验 | 配置名称 1-15 字、触发条件 1-200 字（前后端同限）；服务端报错（如重名）时自动跳回出错字段所在步骤；有效期改「是否有效期」开关，开启后分两行填上线时间 / 下线时间 |

### 19.4 工作台与生命周期

| 变更 | 说明 |
|---|---|
| 行改版 | 行首 6 位配置 ID（点击复制）；上线 / 下线改状态开关（校验失败弹窗引导去修改）；编辑 / 更多改 icon；搜索框移到状态 Tab 上方；日期标注「创建」 |
| 版本概念摘除 | 版本历史、旧引用徽标、vN 标识全部从 UI 移除（服务端快照机制保留）；回显开关从行上移除（改为部分组件的配置项） |
| 上线锁定编辑 | 已上线配置不可进编辑器（icon 置灰 + 提示先下线），下线后才能修改，杜绝「边上线边改」的状态错觉 |
| 其他 | 演示账户更名张三；操作日志导航沉底保持 |
| 行操作组重排 | 右侧操作由竖排三形状（胶囊开关 + 两个描边圆钮）改为横排一组：状态开关 · 细分隔线 · 编辑 / 更多幽灵方钮，与标题行对齐 |

### 19.5 转化看板重构（数据设计师视角）

| 变更 | 说明 |
|---|---|
| 模块拆分 | 原「转化表现 / 触发与采集」两张概括卡拆为六个独立模块：KPI 指标行（组件触发 / 开始交互 / 完成提交 / 整体转化率）+ 转化漏斗 + 分组件转化 + 触发分布 + 推荐默认项 + 埋点健康 |
| 分组件转化（新增） | 使用 funnel_by_type 数据做子弹条：浅色 = 触发量、深色 = 完成提交量（同色相两档明度），右列「提交/触发 · 转化率」；触发不少于 5 次且转化率低于 30% 的标红提示 |
| 漏斗精修 | 条形左对齐（原居中梯形式）、细杆化，级间转化率文本对齐条形起点；悬停显示各级占触发比 |
| 可视化规范 | 单色相表量级、状态色仅用于健康判定、文字用文本 token 不上系列色、直接标注只标关键值 |

## 二十、第十四轮：页面式编辑器与反馈优化重构（v3.3）

### 20.1 编辑器弹窗 → 独立页面

| 变更 | 说明 |
|---|---|
| 页面式编辑器 | 新建 / 编辑交互配置不再弹窗，而是占据主内容区的独立页面：左侧导航保留，标题左侧「返回工作台」按钮；沿用弹窗设计规范（步骤条 / 字号五档 / 间距 8 点制），外壳变为通高内容卡；返回时清理深链参数并刷新列表 |

### 20.2 反馈优化底层重构（场景数据集闭环）

| 变更 | 说明 |
|---|---|
| 场景数据集 | 每类业务场景（价格行情 / 运力调度 / 港口作业等）一个数据集，表格展示各场景的冷启动 / 对话回流 / 手动导入 / 待评测 / 最近入库 |
| 冷启动导入 | 支持 CSV / JSONL / 纯文本三种格式粘贴导入，先选归入场景；服务端按 query 文本去重（重复条目跳过并提示） |
| 对话回流采集 | 对话命中业务场景（分类非闲聊 / 通用）的 query 自动采集入库：来源标记 reflow、按文本去重整合、TTL 180 天，失败不影响对话主链路 |
| Judge 评审模型 | 调度策略页新增配置入口（下拉选在线模型，kv_settings 持久化）；未配置时评测回填与 AI 评审均被拦截并引导去配置 |
| 评测回填 | 待评测题目由各在线模型逐题作答，Judge 模型择优打分入库（审计记录 judge_model）；反馈优化页显示 Judge 状态 chip，未配置时红 chip 直达调度策略页 |
| 闪动修复 | 反馈优化页 pollRebuild 完成态残留会触发无限重渲染（大闪动），加 sawRunning 门闩 + 离开板块停轮询 |

### 20.3 模型路由与测试

| 变更 | 说明 |
|---|---|
| 入池回填独立块 | 覆盖率不足 100% 的模型从「已接入模型」表抽出，置顶独立「入池回填」块（进度条 + 状态 + 开始/暂停/继续），回填完成自动并入下表 |
| 模型测试抽屉 | 左下选择器改为「调度策略 / 指定模型」两组；策略可显式指定（服务端 policy_id 覆盖）；回答后输出路由决策 CoT 三段式：策略（名称 / 档位 / 候选数 / 探索）、模型（候选与最终，聚合方注明）、成本（各模型 token 与成本明细 + 合计与延迟）；右上「×」改「结束测试」 |
| 模型状态文案 | 「暂停」统一改「停用」（回填任务的暂停语义保留） |
| 画像卡 | 质量-成本 α 滑杆加 0 / 0.25 / 0.5 / 0.75 / 1 刻度；更新画像按钮移到卡片右上角 |

### 20.4 工作台与全站视觉统一

| 变更 | 说明 |
|---|---|
| 行操作组 | 状态开关 · 分隔线 · 编辑 / 更多文案按钮横排，与标题行对齐 |
| 状态色 | 已上线 = 主题蓝、已下线 = 低饱和橙红、草稿 = 灰，红色仅留异常（已过期 / 已删除），与 Sky 主题统一 |
| 异常标签 | 待处理模块删除，改为行内异常 chip：「交互完成率低」（触发≥10 且完成率<60%）、「跳转异常」（入口链接非 https），悬停见明细；配置 ID 改 6 位数字（ID:XXXXXX，点击复制完整 ID） |
| 有效期 | 上线 / 下线时间精确到分钟（datetime-local），两行展示；服务端兼容日期与日期+时间两种格式 |
| 时间范围 | 数据看板时间窗改为日期区间选择器：起止日期 + 下方近 7 / 14 / 30 天快捷筛选（交互 / 转化 / 路由三页共用）；「导出看板数据」改「导出数据」并与时间控件对齐 |
| 下拉与货币 | 全站下拉框去箭头（fancySelect 与原生 select）；货币符号统一 RMB（¥），单价标注 RMB/百万 token |
| 组件风格页 | 导入按钮灰底、选中态去打勾、删除改垃圾桶 icon（hover / 选中时浮现）、非默认风格均可删（删除生效中风格自动回退默认）；预览头部与预览器同宽右对齐 |
| 组件库 | 分类筛选 TDZ 崩溃修复（评价类 / 填写类点击即断渲染）、分段按钮字重恒定消除抖动 |
| 组件 | 重置按钮并入提交栏右侧（不再悬浮盖住内容）；商品下单器合计行内提交订单 |

### 20.5 模型路由信息架构重梳（与用户共同梳理的五步路径）

五步用户路径：配置模型（入池评测建立效果档案）→ 准备场景数据集（冷启动导入）→ 设置路由策略（α 决定成本还是效果，效果分来自场景数据集评测）→ 数据集持续收集（自动积累待并入区 + 人工逐条审核并入）→ 评测回填与画像更新（各模型作答、Judge 择优、标签入画像）。

| 变更 | 说明 |
|---|---|
| 导航改名 | 调度策略 → 路由策略；反馈优化 → 场景数据 |
| Judge 迁移 | Judge 评审模型配置从路由策略页迁到场景数据页（评测机制的一部分），信号源与场景数据集之间 |
| 入池回填 → 入池评测 | 模型接入页文案重写：新模型在各场景数据集上逐题作答、Judge 打分，建立效果档案 |
| 待并入区 | 自动收集的 query 不再直接入库：进入待并入区（source=reflow_staged，不参与路由支撑集与评测），场景表出「新收集待并入」列，点开抽屉逐条勾选「并入所选 / 剔除所选」 |
| 自定义场景 | 预置九类 + 「添加场景」（场景名 ≤12 字 + 描述，描述供分类与 Judge 理解）；自定义场景通过导入积累题目 |
| 导入格式 | JSONL 支持 scene 与 ideal（参考答案，Judge 对照打分，bank_queries 增 ideal 列）；CSV 两列；纯文本归入所选场景 |
| 文案 | 画像卡标注「效果分来自场景数据集评测」；回流 → 自动收集；管道 KPI「回流数据集」→「线上回流标签」 |

### 20.6 场景数据集三入口改版（大数据量口径）

| 变更 | 说明 |
|---|---|
| 场景表瘦身 | 列收敛为：业务场景 / 数据集题目数量 / 最后保存时间 / 轮次（该场景经历的评测回填轮数）；单击场景行原位展开填写框，回车分隔多条直接追加入库（原位更新计数，不打断浏览位置） |
| 导入数据集（独立页） | 左侧导航保留：选归入场景 → 粘贴 JSONL / CSV / 纯文本 → 「开始导入」后逐条异步校验（格式 / 场景有效性 / 去重），进度条可轮询，可离开页面后台继续；完成给出「入库 / 去重跳过 / 无效」三段结果 |
| 回流数据集校验（独立页） | 左侧导航保留：自动收集且已整合去重的数据全量列出，每条带场景标签（行内下拉可切换标签）；支持单条与批量的确认（进数据集参与评测）/ 移除；全选与已选计数 |
| 图表配色归一 | 平台图表（柱图 / 漏斗 / 埋点健康 meter / 堆叠条）统一主题蓝，不再走品牌调色板（品牌 token 只作用于对话组件）；路由数据筛选行加竖分隔线，下拉选项去「模型」前缀（闭合态保留「模型：XXX」） |

## 二十一、第十五轮：默认双档与题目级数据管理（v3.4，2026-09-03）

- **路由策略**：内置默认双档「全局均衡」（未指定策略时走它）+「默认兜底」（不路由、直连兜底模型，Judge 移除后仍可用）；其余策略命名去场景化（极速档 / 高质档）；卡片操作 icon 化（复制引用代码 / 更多），更多菜单精简为「复制 / 删除」；策略 ID 固定列位；「模型路由规则」入口移至标题行最右，点击展开规则块；Judge 横幅改白底描边 + 品牌色左缘
- **参数配置抽屉**：标题即「参数配置」；字段重构为 策略 ID（只读文案）→ 名称 → 候选模型（下拉添加 + 标签×删除）→ 兜底模型 → 成本-效果权重滑杆 → 画像参考权重系数（默认 1.00 可调高）；间距拉开；「保存应用」；成本值 / 聚合值 / 响应速度退出常用面板（服务端保留）
- **场景数据集**：场景表列改为 场景 / 题目数 / 最后保存 / 操作（编辑 / 删除），行不再可点展开；场景删除向所有场景开放（通用除外，级联删题目与打分）；新增场景详情页：逐题查看 / 批量添加 / 删除 / 下拉更改场景标签 / 导入 / 替换数据集（先确认，导入前校验有效数据防误清，替换标记一次性）
- **对话测试**：独立页双 tab（智能交互测试 / 模型路由测试），从对应导航进入默认落对应模式；页级 tab 统一下划线式（修复 .on 类无样式的隐性 bug，涉及测试页 / API 接入管理 / 底部模式条）
- **看板重排**：路由看板成本类指标聚拢首行、失败与降级上移与路由分布并列、去掉常年空置的「标签产出」与跑题的「组件触发方式」、三个顶栏筛选下拉移除；交互数据配置卡去编辑 / 导出按钮、点名称跳工作台定位、去「回显关闭」灰标；日期快捷项改为弹层底部文字链
- **全站**：tooltips 精简（页级大标题 ⓘ 全撤、过期与重复 ⓘ 清理、外露说明收进提示）；表单类控件全部定宽；首页路由动画改为从 query 出发的三条细曲线（修复 transform 覆盖导致的气泡偏移）；操作日志动作全量中文化、计数与导出移出表格卡
- **Tricky 审查第三批**：整改 1 P0（替换导入先清库后校验的纯数据丢失）+ 2 P1（全局均衡隐性预算帽 / 替换标记可重复触发），报告见 Tricky审查_v3.3_20260902.md 第三批

## 二十二、第十六轮：双平台拆分与产品化对接（v4.0，2026-09-04）

- **平台拆分**：拆为两个独立站点——智能交互平台（原网址）与模型路由平台（badbotai.github.io/smart-model-router）；导航按平台独立渲染（构建注入 SIA_PLATFORM），智能交互首页作门户跨站互跳
- **产品类**：新增 products 表与「产品接入」页；一个产品 = 一个品牌风格 + 多个组件 + 一个 MCP 接入点（mcp_key + 客户端接入配置一键复制）；组件级嵌入代码保留为补充；组件风格页转纯风格库（去全局生效，显示被哪些产品使用）
- **策略即 API**：API 接入管理页裁撤并融入路由策略——新建策略即发 Key（弹窗展示）、策略卡带 Key 脱敏与复制；参数抽屉 = ID/Key 只读 + 名称 + 候选 + 兜底 + 成本-效果权重 + 聚合开关（画像参考权重、高级参数删除）；策略命名去档位化（省钱优先 / 质量优先）
- **画像与决策模型搬家**：模型画像（各场景路由去向 + 重新生成）与路由决策模型横幅（原打分裁判，全站文案统一）移入场景数据集页，数据-打分-成绩同页闭环；路由策略页保留缺失警示与「查看模型画像」入口
- **发布管线**：build_docs 升级为双站构建（各自快照注入 / 哈希 / SW / 跨站链接改写）；新仓库 BadbotAI/smart-model-router 开通 Pages
- **Tricky 第四批**：整改产品绑定无效组件 ID（D3）与跨站深链导航 404（U2），报告见 Tricky审查_v3.3_20260902.md
- **拆分补漏（2026-09-04 第二批）**：两平台首页各自只保留本平台卡片（底部留跨平台文字链）；模型路由平台有独立首页与导航首页项；智能交互测试彻底去路由内容（无策略选择条、无路由步骤与成本展示，思考仅显示轻量指示）；组件工作台按产品分组（未绑定产品单列一组）；候选模型/绑定组件换标准标签多选下拉（框内标签可删、可输入搜索），兜底模型/品牌风格换自绘下拉；名称校验错误就地显示在输入框下方；标题去重（路由策略/场景数据集页内小标题不再重复页标题）；chat/trace 旧页退出智能交互站（trace 保留在路由站）
- **产品与风格改版（2026-09-04 第三批）**：「产品接入」更名「产品管理」（导航与组件风格换位，智能交互导航去掉首页项、logo 即首页）；产品卡片直接展示绑定组件名与 MCP 配置，新建产品只填名称+品牌风格；组件工作台顶部产品封面 cover（产品名/品牌风格下拉切换/MCP 配置抽屉），列表按当前产品聚焦；组件风格页升级为页面内风格编辑器——「选项配置」（六套配色板/四档圆角/组件底色/字色/字号，调整即在右侧预览）与「代码导入」（框内「应用」按钮打到预览器）双模式，保存成卡可编辑（选项回填）；智能交互首页改为两组入口（搭建·智能交互组件 / 验证·测试与数据），左卡 5 种组件 case 轮播（播完停顿切换 + 圆点导航）
- **首页轮播与策略细节（2026-09-04 第四批）**：两平台首页统一为「大轮播展示卡 + 单主按钮 + 次链接行」——智能交互轮播 5 种组件 case（卡内底栏随 case 出标题与描述），模型路由轮播 4 种路由效果（单模型直答 / 聚合回答 / 故障兜底 / 成本-效果权重滑块动效）；模型路由测试只保留「路由策略」单一选择器；策略卡 API Key 改为脱敏可复制 Key 块（sk-route-••••xxxx）；「模型路由规则」入口上移与页标题对齐；复制策略生成「XXX_副本」新卡；Tricky 第五批整改（S8 删除接口文件名校验 / D4 删风格重置产品引用 / R2 静态站画像状态接口缺失）

# 智能交互平台产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 智能交互平台 |
| 文档类型 | 产品需求文档（PRD） |
| 产品形态 | 面向企业管理员的交互组件配置与运营平台，对接入应用提供组件注册表与前端渲染 SDK |
| 文档状态 | 当前产品定义 |
| 更新日期 | 2026-09-12 |

## 2. 产品概述

### 2.1 产品定位

智能交互平台用于把 AI 对话里的输出从纯文本升级为可交互、可统计的组件。管理员在平台配置组件实例与视觉风格，平台把这些实例编译成注册表交给企业自己的 agent 服务；大模型按注册表的说明与参数结构决定何时调用哪个组件、填入什么内容，前端 SDK 完成渲染并把用户操作回传。

平台不参与模型调用，也不决定组件何时出现——它负责组件的定义、样式、注册、渲染与数据回收。

### 2.2 产品目标

1. 以组件实例为单位管理 AI 对话中的交互与展示能力，配置后即可被模型调用。
2. 让组件内容既能由模型按对话实时生成，也能由平台固定配置以便统计。
3. 用风格主题统一控制视觉，同时允许单个组件实例覆盖。
4. 组件渲染结果与用户操作按统一事件结构回传，形成可分析的交互数据。
5. 一个产品对应一套风格主题、一组组件实例和一个接入点，多产品互不影响。

### 2.3 核心原则

- 组件像工具一样注册给大模型：平台给出说明与参数结构，何时调用、填什么由模型决定。
- 组件类型与业务解耦。模板名称只描述形态（选择器、饼图、确认器），不带业务语义。
- 内容分两种形式：AI 生成内容由模型每次填入；固定配置内容由平台定死，模型不可覆盖。
- 群体回显只在固定配置内容下可开——动态内容每次不同，无法聚合统计。
- 风格主题是颜色、字体、字号的默认来源；组件实例的样式覆盖优先级高于主题。
- 模型不可信：注册表下发的参数在渲染前必须经 schema 校验，校验不通过降级为纯文本。
- 配置页上出现的每一项，都要在预览器里有肉眼可见的效果。

## 3. 用户与使用场景

### 3.1 用户角色

| 角色 | 主要职责 |
|---|---|
| 企业管理员 | 添加产品、配置组件实例、编辑风格主题、发布上线、管理接入 Key |
| 企业运营人员 | 查看组件的曝光、交互、完成情况，定位需要优化的组件实例 |
| 接入方开发者 | 把注册表接入 agent 服务，把渲染 SDK 嵌入业务页面 |
| 终端用户 | 在企业应用的对话里使用这些组件：选择、填写、确认、评价 |

### 3.2 典型场景

1. 客服对话中用户反馈物流异常，模型调用选择器给出三种处理方式供用户点选。
2. 模型需要补齐联系人与联系方式时调用表单，一次性收齐结构化信息。
3. 执行不可逆动作前调用确认器，用户明确点击后才继续。
4. 模型给出费用走势时调用折线图，把大段文字变成一眼能读的图。
5. 同一套组件在不同产品下套用各自的风格主题，视觉与该产品品牌一致。
6. 运营人员按组件实例查看曝光与提交情况，找出配置需要调整的组件。

## 4. 产品信息架构

### 4.1 当前产品模块

| 页面 | 功能 |
|---|---|
| 组件工作台 | 按产品管理组件实例：新建、编辑、上下线、查看详情、批量操作 |
| 风格主题 | 维护风格主题的 design token，并实时预览组件效果 |
| 数据分析 | 查看整体交互情况与单个组件实例的数据 |

### 4.2 全局模块

| 页面 | 功能 |
|---|---|
| 产品与接入 | 管理产品、绑定风格主题与组件、查看接入配置与产品 Key |
| 操作日志 | 记录组件发布下线、产品与主题变更等管理操作，支持筛选与导出 |

### 4.3 产品与组件的关系

一个产品 = 一套风格主题 + 一组组件实例 + 一个接入点。切换产品后，工作台列表、风格预览与数据分析的默认范围同步切换。新建产品时自动预置一套标准组件实例，默认全部下线，管理员挑需要的上线。

组件实例名在租户内唯一。预置实例统一以「（默认）」结尾，与已有实例重名时追加序号（如「处理方式选择（默认 2）」）。

## 5. B 端产品功能与交互

### 5.1 组件工作台

#### 5.1.1 页面结构

页面自上而下为：页标题与「新建组件」入口、产品卡、状态 tab 与工具行、组件卡片列表。标题、产品卡与工具行固定不动，只有卡片列表滚动；左侧分类栏同样固定。

产品卡展示当前产品的名称、产品 ID、组件实例数与在线数，并提供风格主题切换与接入配置入口。点击产品名展开切换器，列出全部产品（含头像色块与组件实例数）与「添加产品」。

#### 5.1.2 状态 tab 与筛选

| 状态 | 含义 |
|---|---|
| 全部 | 当前产品绑定的所有组件实例 |
| 已上线 | 已发布且进入产品注册表，模型可调用 |
| 已下线 | 已从注册表移除，模型不再调用 |
| 草稿箱 | 从未发布过的实例 |

工具行包含状态 tab、命中计数、搜索框与「批量操作」入口。左侧分类栏按组件类别（选择、表单、确认、反馈、图表、表格、流程、文本）过滤。

#### 5.1.3 组件卡片

卡片定高，自上而下为三段：

| 区域 | 内容 |
|---|---|
| 头部 | 组件图标、实例名称、组件类型标签、上下线开关 |
| 中部 | 该实例的真实缩略预览（套用它自己的样式覆盖与固定内容） |
| 底部 | 组件 ID（点击复制）、创建时间、「编辑」与「更多」 |

交互规则：

- 实例名称超过 8 个字省略显示，组件类型标签始终完整显示。
- 上下线开关只表达已上线与已下线两种形态。草稿实例不给开关，需从「更多 − 发布」发布后才能使用。
- 点击卡片标题进入组件详情页，不直接进入编辑。
- 已上线的实例不可直接编辑，需先下线。
- 「更多」菜单按状态区分：已上线与已下线为「复制组件 / 查看详情 / 删除」，草稿箱额外提供「发布」。
- 固定内容与群体回显以标签形式显示在样式摘要行，不占用标题行宽度。

#### 5.1.4 空态

当前产品没有组件实例时，展示起步卡：列出可导入的标准组件（名称与类型），提供「一键导入」「新建组件」「不用了，我自己建」。导入的实例默认全部下线。

### 5.2 组件配置编辑器

#### 5.2.1 五步流程

| 步骤 | 回答什么 | 内容 |
|---|---|---|
| 容器选择 | 用哪个组件 | 从 16 个模板中选择；按交互任务分十类（选择、排序、表单、数值、评价、日期时间、确认、图表、表格、信息展示），卡片区定高滚动 |
| 内容设置 | 显示什么内容 | 组件名称、内容形式（AI 生成 / 固定配置），以及该组件类型对应的内容字段 |
| 交互定义 | 用户能做什么 | 交互行为与提交后反馈。**展示类组件整步不出现**，不留空白页 |
| 样式设计 | 长什么样 | 按组件自身结构从外到内分层的样式项，实时预览 |
| 数据结构 | 回传什么 | 两段 JSON：模型入参（或平台固定内容）与提交回传结构 |

底部按钮规则：中间四步都提供「保存草稿」（保存后关闭编辑器回到工作台，只校验组件名称，其余规则留到发布前把关）；样式设计步的主按钮为「下一步」；数据结构步为「仅保存」与「发布」。主按钮始终位于最右。

右侧预览器与线上同一套渲染器，标题为组件名称。预览器头部提供「重置」（清除全部样式覆盖）与「看回显效果」（开启群体回显后出现）。

#### 5.2.2 组件模板

交互类 8 个：

| 模板 | 类型标识 | 说明 |
|---|---|---|
| 选择器 | `select.single` | 在若干候选中做选择；列表与卡片两种呈现，可开启多选 |
| 表单 | `form.structured` | 字段固定的信息收集 |
| 确认器 | `control.confirm` | 不可逆动作前的确认 / 取消 |
| 赞踩反馈 | `feedback.binary` | 对一条回答的赞 / 踩评价 |
| 数值滑杆 | `slider.range` | 在数值范围内选择 |
| 评分量表 | `scale.likert` | 满意度等分档评分 |
| 日期时间 | `picker.datetime` | 选择日期或精确时刻 |
| 排序器 | `rank.priority` | 按重要程度拖动排序 |

展示类 8 个：

| 模板 | 类型标识 | 说明 |
|---|---|---|
| 表格 | `table` | 清单、多行记录 |
| 折线图 | `chart.line` | 数列随时间的走势 |
| 柱状图 | `chart.bar` | 分类对比 |
| 饼图 | `chart.pie` | 构成占比，支持百分比条与环形两种形态 |
| 指标卡 | `metric.card` | 单个关键数值与涨跌 |
| 时间线 | `timeline` | 按时间排列的事件 |
| 步骤条 | `steps` | 办理流程与当前进度 |
| 重点结论 | `text.emphasis` | 一句话核心结论 |

卡片选择器并入选择器（由呈现方式决定列表或卡片）；偏好选择器、对比矩阵、要点清单已下架，渲染能力保留，已创建的实例照常显示。

#### 5.2.3 内容形式

| 形式 | 内容来源 | 群体回显 |
|---|---|---|
| AI 生成内容（默认） | 模型按当前对话实时填入 | 不可开启 |
| 固定配置内容 | 平台在配置时定死，模型不可覆盖 | 可开启 |

选择固定配置内容后，注册表把这部分内容随 `fixed` 下发并从参数结构中移除，模型只需选用该组件。

#### 5.2.4 各组件的内容字段

| 组件 | 固定内容下可配置 |
|---|---|
| 选择器 | 题目、选项（2–8 项，每项可配描述与推荐标签）、提交按钮文案；是否多选、最少与最多可选 |
| 表单 | 题目、字段（字段名、类型、占位提示、是否必填）、提交按钮文案 |
| 确认器 | 要确认的那句话、确认按钮文案、取消按钮文案 |
| 排序器 | 题目、固定条目（2–8 项）、提交按钮文案 |
| 评分量表 | 题目、提交按钮文案（档位与两端说明在样式步） |
| 数值滑杆 | 题目、最小值、最大值、步长、单位、默认值、提交按钮文案 |
| 日期时间 | 题目、收集日期还是精确时刻、提交按钮文案 |
| 赞踩反馈 | 赞踩左侧的问法 |
| 展示类组件 | 该组件的展示数据（表格行列、图表数列、时间线事件等） |

按钮文案一律在内容配置步编辑，不出现在样式设计步。

#### 5.2.5 交互定义

分两组，前后顺序固定：**交互行为**（用户能对它做什么）在前，**提交后反馈**（做完看到什么）在后。
展示类组件没有交互，这一步整步不出现。

| 组件 | 交互行为 | 提交后反馈 |
|---|---|---|
| 选择器 | 选择模式（单选 / 多选）；多选时的最少与最多可选数 | 按钮文案、提交后按钮文案、提交成功提示、展示群体结果 |
| 表单 | — | 同上（无群体回显） |
| 确认器 | — | 确认文案、取消文案、提交后文案、成功提示、展示群体结果 |
| 赞踩反馈 | 点踩后是否追问原因 | 成功提示、展示群体结果 |
| 数值滑杆 | 取值范围与步长在内容设置里定 | 按钮文案、提交后文案、成功提示 |
| 评分量表 | 档位在内容设置里定 | 同上 + 展示群体结果 |
| 日期时间 | 可选范围（不限 / 仅未来 / 仅过去） | 按钮文案、提交后文案、成功提示 |
| 排序器 | — | 同上 + 展示群体结果 |

选择数量的校验：最少不小于 1、最多不小于最少、两者都不超过选项总数。选项总数实时统计，
增删选项后上限立即跟着变。AI 动态内容时选项数未知，不校验上限。

群体回显只在固定配置内容下可用——AI 每次生成的内容不同，无法聚合统计。赞踩反馈例外，
它的赞 / 踩语义是固定的。

#### 5.2.6 样式设计

样式项按**组件自身的结构**从外到内分层，分组名与说明只讲这个组件的事，不套统一模板。

| 组件 | 分层 |
|---|---|
| 选择器 | 01 容器 / 02 布局 / 03 选项 / 04 文字 / 05 标签 / 06 按钮 |
| 表单 | 容器 / 字段控件 / 文字 / 提交按钮 |
| 表格 | 容器 / 表格 / 文字 |
| 折线图 | 容器 / 折线与数据点 / 文字 |
| 饼图 | 容器 / 扇区 / 文字 |
| 时间线 | 容器 / 节点与连线 / 文字 |
| 指标卡 | 容器 / 数值与涨跌 / 文字 |
| 重点结论 | 容器 / 结论与说明 |

每层内部再按子分组归拢，属性归到它真正作用的层——主色放在它决定的那一层（选项、字段控件或按钮），
而不是统一堆在面板层。

**文字按角色拆开，每个角色各自成套**。同一个组件里不同用途的文字分别可配：

| 组件 | 文字角色 |
|---|---|
| 选择器 | 标题 · 描述 · 选项名称 · 选项描述 |
| 表格 | 标题 · 表头 · 单元格 |
| 指标卡 | 指标名 · 数值 · 对比说明 |
| 时间线 | 标题 · 时间 · 事件标题 · 描述 |
| 表单 | 标题 · 字段标签 · 输入文字 |

标题与主体名称给字号、字重、颜色三件套；描述类只给字号与颜色——描述文字本就该是常规字重，
开放出来只增加决策负担。

交互规则：

- 基础项直接显示，高级项折叠；折叠标题上的「N 项」与实际条数一致。
- 标签在左、控件在右排成一行，「恢复默认」跟着控件走。
- 颜色项的色板按用途给：底色是白到浅灰，边框是灰阶并带「无边框」，文字是墨色系，主色与图形色才用品牌色。每行固定九个槽位。
- 未改动过时「恢复默认」置灰；改过之后点一下回到主题值。
- 依赖其它开关才生效的项声明依赖，依赖不满足时不显示。
- 改动某项后面板整块重绘，已展开的分区保持展开；文本逐字输入时预览防抖重绘，不逐键重建。

#### 5.2.7 数据结构

两段 JSON 直接摊开，字段说明收进折叠。内容与正式注册表同源。

| 内容形式 | 第一段 | 第二段 |
|---|---|---|
| AI 生成内容 | `params_schema`：模型调用时要填的参数 | `submit_schema`：用户提交后回传的结构 |
| 固定配置内容 | `fixed_content`：平台固定下发的内容 | 同上 |

固定内容形式下模型不传任何参数，`params_schema` 是空对象，因此第一段换成实际下发的内容。
改了选项、列名或字段，这一段立刻跟着变。

### 5.3 组件详情页

从工作台点击卡片标题进入，只读展示：

| 区域 | 内容 |
|---|---|
| 样式预览（左） | 该实例的真实渲染效果，右上角提供「编辑」 |
| 基本信息（右） | 组件 ID、组件类型、内容形式、群体回显、版本、创建时间 |
| 数据 Schema（右） | 参数结构、平台定死的内容、提交回传结构 |

已上线的实例点击「编辑」时提示需先下线。

### 5.4 风格主题

主题与产品是**一对多绑定**：一套主题可以给多个产品用，一个产品同时只绑一套。创建产品时从预设里选定，之后只有两个动作——**换一套**或**接着改当前这套**，不需要每次重新挑。产品配置里的「编辑主题」直接跳进该主题的编辑器。

#### 5.4.1 页面结构

左侧为主题列表，右侧为组件预览。预览区固定不滚动，每格高度固定——调整卡片内边距只改变组件内部，不影响组件之间的间距。

页头分两态：列表态显示「风格主题」与主题套数，右侧为「新建主题」；编辑态显示「返回」「新建主题 / 编辑主题」与「取消」「保存主题」。标题只出现一次，两态字号一致。

#### 5.4.2 主题编辑

| 分组 | 内容 |
|---|---|
| 主题名称 | 必填，20 字内，忽略大小写与空格后不允许与现有主题重名 |
| 主题预设 | 六套行业风格起步，含浅色与深色两套语义色板 |
| 快速配色 | 主题色、文字颜色、背景色、组件底色、警示色五个关键色 |
| 高级配色 | 逐 token 细调，每个 token 标注用途与对应的 CSS 变量 |
| 圆角 | 0–28px |
| 阴影 | 四档，另提供 X / Y / 模糊 / 扩散 / 不透明度的自定义阴影 |
| 卡片内边距 | 五档（10 / 13 / 16 / 20 / 24px），同时带动卡片内部的行距节奏 |
| 排版 | 字体族、正文字重、正文字号（10–18px）；其余字号按正文字号派生 |

预设不只是配色，形态参数（圆角、阴影、内边距、字号）一并切换：

| 预设 | 观感 | 圆角 / 内边距 |
|---|---|---|
| 政企风 | 权威克制，信息密度高，方正少装饰 | 6px / 13px |
| 科技简约 | 现代干净，留白充足，层次清晰 | 12px / 16px |
| 自然清新 | 柔和有呼吸感，微暖底色 | 16px / 20px |
| 数字电商 | 活力促单，对比强烈，信息紧凑 | 12px / 13px |
| 金融专业 | 数据密集，稳健可信 | 4px / 10px |
| 医疗健康 | 洁净平静，易读优先 | 10px / 20px |

主题名称在输入时即校验重名，服务端同样校验并返回冲突提示。

#### 5.4.3 可读性约束

- 主色按钮的文字颜色按底色亮度自动取白或近黑。
- 组件面板底色被改为深色时，衬底、边框与次要文字颜色一并派生，不出现浅底深字或白底白字。
- 用主色作文字的元素（序号、最优值、当前节点）相对面板底色校正到对比度达标。
- 风格主题的弱化文字色与警示色在自身背景上的对比度不低于 4.5；六套预设的明暗两套色板逐项校验过：正文 7:1、次要与弱化 4.5:1、状态色 3:1。

### 5.5 产品与接入

#### 5.5.1 产品列表

每张产品卡展示产品名称、产品 ID、风格主题、组件实例数与在线数，提供「产品配置」与「更多」（复制产品、重置接入 Key、删除产品）。

#### 5.5.2 产品配置

| 字段 | 规则 |
|---|---|
| 产品名称 | 必填，1–15 字，租户内不允许重名 |
| 风格主题 | 单选。创建产品时即选定，成为该产品的品牌风格；产品内所有组件按它渲染。之后可随时换一套，或点「编辑主题」直接进入这一套的编辑器继续调 |
| 绑定组件 | 多选，决定注册表包含哪些组件实例 |
| 产品 ID | 系统生成，只读 |
| 接入 Key | 系统生成，重置后旧 Key 立即失效 |

#### 5.5.3 接入方式

接入方拿到的是**两段代码**：一段给后端、一段给前端。后端把注册表交给 agent 作为工具声明，前端负责加载、渲染与数据回传。

**第一段：后端 API**

```
GET /v1/products/{product_id}/registry?key={mcp_key}
```

agent 服务端拉取该产品的组件注册表，作为工具声明注入模型上下文。返回内容包含每个组件实例的 `card_id`、`component_type`、`params_schema`、触发说明与 `content_hash`。模型据此决定何时调用哪个组件、要填什么参数。

注册表必须携带产品 Key（`mcp_key`），未携带或不匹配返回鉴权失败。Key 重置后旧 Key 立即失效。

**第二段：前端代码**

```html
<script src="https://platform.example.com/loader.js"></script>
<script>
  PlatformWidget.mount("#container", {
    agentId: "{product_id}",
    token: "short-lived-token"
  });
</script>
```

这段 loader 由三部分构成：

| 构成 | 做什么 |
|---|---|
| 资源加载 | 拉取渲染所需的 js 与该产品的 `sia.css`（风格主题编译产物），样式作用域限定在组件容器内，不影响宿主页面 |
| 解析引擎 | 接收模型返回的组件信封（envelope），按 `component_type` 与 `params` 渲染出真实组件，并处理用户操作与提交 |
| 数据采集 | 组件曝光、用户开始交互、提交等时机自动上报事件，业务方不需要自己埋点 |

**初始化与验证**

1. `mount()` 时用 `agentId` 与短时 token 换取该产品的公开 Key，用于后续拉取样式与回传事件。
2. 拉取 `sia.css`。样式未就绪时组件不渲染，避免出现无样式的中间态。
3. 渲染前对模型返回的 `params` 做 schema 校验：不符合 `params_schema` 的参数被拒绝，降级为纯文本，并上报 `render_degraded`。
4. 固定内容形式的组件，内容由平台下发，模型传入的同名字段一律忽略。

**埋点格式**

事件统一走 `POST /v1/events`，单条或批量（`{"events": [...]}`）均可。每条事件的必填主键：

```json
{
  "event_id": "evt_8f3c21",
  "trace_id": "trace_5d9a",
  "tenant_id": "tenant-demo",
  "session_id": "sess_2b71",
  "turn_id": "turn_04",
  "user_id": "u_91827",
  "event_type": "card_submitted",
  "render_id": "rid_7c02",
  "card_id": "a8650211-3ed7-4eec-910f-d442aace1691",
  "component_type": "select.single",
  "ts": 1757808000000
}
```

默认采集三类数据，对应三个事件：

| 采集项 | 事件 | 额外字段 |
|---|---|---|
| 组件曝光数 | `card_rendered` | 无 |
| 提交次数 | `card_submitted` | 见下 |
| 提交的具体内容 | `card_submitted` | `payload` |

提交事件的 `payload` 只保留白名单字段，其余丢弃：

```json
{
  "event_type": "card_submitted",
  "payload": {
    "user_selection": "GPT-5.6",
    "modified_from_default": false,
    "action": "confirm",
    "reason": "结论错误",
    "dimension": "accuracy",
    "value": 4
  }
}
```

| 字段 | 何时出现 |
|---|---|
| `user_selection` | 选择器、量表、日期、滑杆的选中值；表单为 JSON 字符串 |
| `modified_from_default` | 选择器：用户是否改掉了推荐项 |
| `action` | 确认器：`confirm` / `cancel` |
| `reason` | 赞踩：点踩后选的原因 |
| `dimension` | 赞踩：多维度评价时的维度 key |
| `value` | 量表、滑杆的数值 |

删除产品时，只被该产品引用的组件实例一并清理；被其它产品引用的保留。

### 5.6 数据分析

#### 5.6.1 页面结构

页头提供时间范围：预设（近 7 / 30 / 90 天、近一年）与起止日期常驻一行，改日期即按自定义范围统计。下方分两个 tab。

#### 5.6.2 整体交互情况

| 指标 | 口径 |
|---|---|
| 曝光 | 组件在对话里出现的次数（`card_rendered`） |
| 提交 | 用户点击提交的次数（`card_submitted`） |
| 交互率 | 曝光后有操作的比例（`card_interaction_started` ÷ 曝光） |
| 推荐采纳率 | 用户直接用了模型推荐项、没有改选的比例 |

另包含：使用漏斗（曝光 → 开始操作 → 提交，标注逐级流失率）、每日曝光与提交趋势、按组件类型对比（支持按类别筛选）、组件实例排行（点击一行跳转到单组件分析）。

#### 5.6.3 单组件数据分析

提供产品、组件类型、具体实例三级筛选，默认落到当前时间范围内曝光最高的实例。展示该实例的曝光、交互率与提交次数，以及每日趋势与选项分布。

展示类组件没有提交行为，提交相关指标显示为「—」而不是 0%。

#### 5.6.4 指标原则

只聚合事件表里真实存在的事件。取不到的指标不做，也不在页面上保留占位——决策时长、反馈好评率等指标已整体下线，相关埋点字段一并移除。

### 5.7 操作日志

记录组件的新建、编辑、发布、下线、删除、回滚，产品的添加、更新、删除、Key 重置，以及主题的导入、切换、删除。

| 列 | 内容 |
|---|---|
| 时间 | 操作发生时间 |
| 操作人 | 管理员 |
| 动作 | 中文动作名；未单独命名的系统迁移统一显示为「系统升级」 |
| 对象与详情 | 对象名称与关键字段；对象已删除时显示「（已删除）」而不是内部 ID |

工具行位置与表格列宽固定，不随筛选内容变化。支持按动作类型与关键词筛选，并按当前筛选导出 CSV。

## 6. 组件注册与渲染

### 6.1 注册表

平台把产品绑定的、已上线的组件实例编译成注册表。

外层元信息：

| 字段 | 说明 |
|---|---|
| `registry_version` | 注册表结构版本 |
| `generated_at` | 生成时间 |
| `content_hash` | 内容哈希，接入方据此判断是否需要刷新 |
| `product_id` / `product_name` | 所属产品 |
| `brand_file` | 该产品绑定的风格主题文件 |
| `usage` | 给接入方的使用说明 |
| `components` | 组件实例数组 |

每个组件实例：

| 字段 | 说明 |
|---|---|
| `component_id` | 组件实例 ID，模型调用时携带 |
| `type` | 组件类型标识 |
| `name` | 实例名称 |
| `description` | 组件说明，模型据此判断何时调用 |
| `interactive` | 是否为交互类（决定有无提交回传） |
| `params_schema` | 模型要填的参数结构（JSON Schema 子集） |
| `fixed` | 平台定死的内容，固定内容形式下出现 |
| `submit_schema` | 用户提交后回传的结构 |

### 6.2 渲染流程

1. 模型返回 `{ component_id, params }`。
2. SDK 携带该实例 ID 请求渲染信封，平台按注册表校验参数。
3. 校验通过则渲染组件，校验不通过降级为纯文本。
4. 渲染完成回传 `card_rendered`，用户开始操作回传 `card_interaction_started`。
5. 用户提交回传 `card_submitted` 与提交结构；渲染后离开且未提交回传 `card_abandoned`。

同一渲染 ID 重复提交只记一次。

### 6.3 样式合成

渲染时三层合成，优先级自低到高：

1. 平台默认 design token
2. 产品绑定的风格主题 token（作用域限定在组件容器）
3. 组件实例的样式覆盖（写在实例元素的内联 CSS 变量上）

组件实例的样式覆盖经服务端白名单与取值范围校验，不在白名单内的键被丢弃。

### 6.4 群体回显

固定内容形式的交互组件可开启群体回显。用户提交后，平台按该实例的历史提交聚合出分布，直接标注在每个选项上（选项下一条占比线与百分比，自己选的那项用主色）。候选项取自组件本身：赞踩为赞与踩，评分量表为各档位，排序器为条目，偏好选择器为候选。

回显数据由真实提交聚合，不提供示例配置入口。

## 7. 数据与统计口径

### 7.1 事件主键

每次渲染生成唯一 `render_id`，作为渲染、交互、提交事件的关联主键。事件携带 `card_id` 与 `component_type`，用于按实例与按类型统计。

### 7.2 事件类型

| 事件 | 触发时机 |
|---|---|
| `card_rendered` | 组件渲染完成 |
| `card_interaction_started` | 用户首次操作（点选项、聚焦输入框） |
| `card_submitted` | 用户提交 |
| `card_abandoned` | 渲染后离开且未提交 |
| `feedback_given` | 赞踩组件的评价 |
| `control_invoked` | 确认器的确认 / 取消 |
| `render_degraded` | 参数校验未过，降级为纯文本 |
| `card_ref_missing` | 模型引用了不存在或已下线的组件实例 |

### 7.3 提交字段

提交事件只保留与业务相关的字段：`user_selection`、`modified_from_default`、`action`、`reason`、`dimension`、`value`。不在白名单内的字段被丢弃。

## 8. 权限、安全与审计

- 产品、组件实例、风格主题与事件数据按企业租户隔离。
- 产品接入 Key 仅在创建或重置成功时完整展示，日常页面使用脱敏形式；重置后旧 Key 立即失效。
- 注册表接口必须携带产品 Key，未携带或不匹配返回鉴权失败。
- 模型返回的参数在渲染前经 schema 校验；固定内容形式下，模型无法覆盖平台定死的内容。
- 删除产品、删除组件实例、重置 Key 等操作需二次确认。
- 管理员的关键操作写入操作日志，支持按动作与关键词追溯。

## 9. 关键验收标准

1. 组件配置页上出现的每一项，在预览器里都有肉眼可见的效果；没有效果的项不出现。
2. 图形元素的配置成套（底色、边框、圆角、阴影、间距），文字元素的配置成套（字号、颜色、字重）。
3. 样式项按组件区域分组，每个参数都落进某个区域，折叠标题的「N 项」与实际条数一致。
4. 编辑流程固定为四步，按钮文案一律在内容配置步；三步均可保存草稿，保存后回到工作台。
5. 组件实例的样式覆盖优先级高于风格主题；清除覆盖后回到主题值。
6. 面板底色改为深色后，衬底、边框与各级文字一并派生，任意配置组合下文字对比度不低于 4.5。
7. 固定内容形式下才允许开启群体回显；回显的候选项取自组件本身，不出现与该组件无关的选项。
8. 上下线开关只表达已上线与已下线；草稿实例需经「更多 − 发布」才能上线。
9. 新建产品自动预置一套标准组件实例，默认全部下线，名称带「（默认）」标识；与已有实例重名时追加序号。
10. 删除产品时只清理仅被该产品引用的组件实例。
11. 数据分析只展示事件表可聚合出的指标；展示类组件的提交相关指标显示为「—」。
12. 组件配置保存后重新打开，样式覆盖、内容形式、选项、推荐项与按钮文案完整还原。
13. 同一渲染 ID 重复上报提交事件只记一次。
14. 注册表接口未携带产品 Key 时返回鉴权失败；携带正确 Key 时返回带内容哈希的完整注册表。

以上标准中，1、2、3、4、5、6、8、9、10、11、12、13、14 可由脚本自动核对，核对方式见
`组件基线要求.md` 与 `产品功能开发方案.md` 的质量门禁一节。

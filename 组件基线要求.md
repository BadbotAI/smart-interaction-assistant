# 组件基线要求（最终版）

这份文档把历次评审里对组件提出的要求收敛成一套可逐条核对的基线。
每新增或修改一个组件，都要把下面 6 组共 27 条走一遍；任何一条不达标都算不合格。

核对方式分三种：
- **机器可验**：有脚本能自动判定（`param_audit.js` / `style_rows.js`）
- **走查可验**：需要打开页面看一眼
- **读码可验**：看代码就能确认

---

## A. 配置项有效性（最高优先级）

| # | 要求 | 核对方式 |
|---|---|---|
| A1 | 每个配置项在预览器上都要有肉眼可见的效果；没有效果的配置项不该出现 | 机器可验：`param_audit.js` 对每个参数改值后比对渲染快照与计算样式 |
| A2 | 配置项如果依赖另一项（如「环形厚度」只在环形图下生效），必须声明 `deps`，未满足依赖时不显示 | 机器可验：脚本按 `deps` 先满足依赖再验 |
| A3 | 服务端 `SPEC_*` 白名单要覆盖到每个新增参数，否则会被静默丢弃 | 读码可验：`server/cards.py` |
| A4 | 示例数据要能体现该配置的效果（例如时间格式要生效，示例时间就必须是 ISO） | 机器可验 |

## B. 交互形式

| # | 要求 | 核对方式 |
|---|---|---|
| B1 | 数值类配置一行一项，不要多个数字框挤在一行 | 走查可验 |
| B2 | 单位用下拉框，不用自由输入 | 走查可验 |
| B3 | 数字输入框一律右对齐，文本框一律左对齐 | 走查可验（`shared.css` 已统一口径） |
| B4 | 下拉框要有箭头，展开面板用产品自己的样式，不用系统默认 | 走查可验 |
| B5 | 选中态就用选中态本身表达，不要额外加勾选图标 | 走查可验 |
| B6 | 开关类用开关，不要做成单独按钮 | 走查可验 |
| B7 | 看不出编辑效果的控件（如赞踩的控件高度）宁可不展示 | 见 A1 |

## C. 结构与顺序

| # | 要求 | 核对方式 |
|---|---|---|
| C1 | 样式项按**组件区域**分组：整体面板 / 选项与控件 / 图形 / 数据表 / 节点与连线 / 数值指标 / 文字 / 提交按钮 | 机器可验：`style_rows.js` dump 分区 |
| C2 | 每个参数都要落进某个区域，不能掉进默认区 | 机器可验：`PARAM_ZONE` 覆盖检查 |
| C3 | 基础项直接显示，高级项折叠；折叠标题上的「N 项」计数要与实际条数一致 | 机器可验 |
| C4 | 区域顺序固定：面板 → 选项/图形/表格/节点/数值（按组件类型取其一） → 文字 → 按钮 | 机器可验 |
| C5 | 编辑流程固定 4 步：选择组件 → 内容配置 → 样式设计 → 数据 Schema；没有「获取接入信息」这一步 | 走查可验 |
| C6 | 内容 / 样式 / Schema 三步底部都有「保存草稿」；样式步主按钮是「下一步」，Schema 步右下角是「仅保存 ｜ 发布」 | 走查可验 |

## D. 与风格主题的关系

| # | 要求 | 核对方式 |
|---|---|---|
| D1 | 风格主题是颜色、字体、字号的默认来源；组件上不另起一套并存的「品牌设置」 | 读码可验 |
| D2 | 组件级配置优先级高于风格主题 | 读码可验：组件实例内联 CSS 变量天然压过 `.brand-scope` 类作用域 |
| D3 | 未覆盖的样式项显示「跟随主题」+ 主题色圆点；覆盖后显示「已覆盖」+ 恢复默认按钮 | 走查可验 |
| D4 | 色板里的品牌色块用透明底 + 黑色「品牌」二字，不用主题色实心块（会被误认为可选色） | 走查可验 |
| D5 | 默认组件模板的样式跟随当前品牌风格 | 走查可验 |

## E. 内容配置

| # | 要求 | 核对方式 |
|---|---|---|
| E1 | 要填的内容按组件本身的语义来定：选择类 = 题目 + 选项 + 提交按钮文案；不存在「引导语」这种字段 | 走查可验 |
| E2 | 提交 / 确认 / 取消按钮的文案编辑一律在**内容配置**步，不在样式步 | 机器可验：样式步不得出现 `__submitLabel` 类的文案项 |
| E3 | 组件模板不带业务属性：叫「选择器 / 饼图 / 确认器」，不叫「风险确认 / 操作确认」 | 走查可验 |
| E4 | 用 AI 动态生成内容时也要给出几个示例候选，让用户看得到样式 | 走查可验 |
| E5 | 用户能编辑的标题，组件上必须展示出来（预览与线上一致） | 机器可验 |
| E6 | 时间类字段用时间选择器，并提供不同的时间展示形式 | 走查可验 |

## F. 视觉与文案

| # | 要求 | 核对方式 |
|---|---|---|
| F1 | 全站禁用 emoji 与装饰性 unicode 符号（× 作为关闭按钮除外） | 机器可验：全文件扫描 |
| F2 | 预览器标题用组件名称本身，不写「预览」；不展示「标准 / 窄屏」切换 | 走查可验 |
| F3 | 组件卡片里 icon、标题、标签要水平对齐、间距一致；缩略图完整显示不被裁 | 走查可验 |
| F4 | 确认类组件不要红色边条、不要警示图标 | 走查可验 |
| F5 | 赞踩：没有踩后反馈追问；按钮用倒角形状；群体回显默认关闭 | 走查可验 |
| F6 | 重置按钮与示例卡片对齐，不与其他控件重叠 | 走查可验 |

---

## 核对脚本

```bash
# A 组：每个配置项是否真有可见效果
node param_audit.js

# C 组：每个组件在样式步实际显示了哪些行、落在哪个区
node style_rows.js "<逗号分隔的组件类型>"
```

两个脚本都在 `$CLAUDE_JOB_DIR/tmp/` 下运行（puppeteer-core 装在那里），跑之前先起本地服务 `python3 -m uvicorn server.app:app --port 8787`。

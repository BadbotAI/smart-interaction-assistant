# 智能交互平台 · 产品功能开发方案

- 更新：2026-09-02（v3.3 现状 + 后续规划）
- 配套文档：功能说明书.md（四大模块功能清单 + 各轮变更）· 项目背景与记忆.md · 功能拆解与问题清单.md（审查产物）

## 一、技术架构

| 层 | 实现 |
|---|---|
| 前端 | 原生 JS 多页应用（web/）：shared.css 设计系统 + ui.js 原语库 + components.js 组件渲染引擎 + 各页面；无构建步骤 |
| 服务端 | FastAPI（server/）：app.py 路由与任务、cards.py 配置域、router_core.py JiSi 路由引擎、events.py 埋点准入、dashboard.py 统计、mockmodels.py 确定性模型模拟、db.py SQLite |
| 存储 | SQLite（platform.db）：cards/snapshots/refs、events、labels、traces、models、policies(+history)、bank_queries/responses、kv_settings、audit |
| 对外 | /v1/route（SSE）、/v1/events、/v1/embed/envelope、sia.js 植入 SDK、contracts/ 数据契约 |
| 部署 | 本地完整版（uvicorn）+ GitHub Pages 静态演示（docs/ = 页面拷贝 + mock_data 快照 + mock_api 拦截层） |

核心数据流：配置（cards）→ 发布快照 → 对话命中触发（/v1/route，相似度≥0.35 + 有效期）→ 组件信封渲染 → 提交（/v1/events 准入，channel 隔离 test/embed）→ 回显 / 看板 / 标签池 → 场景数据集回流（staged 待校验）→ 评测回填（各模型作答 + Judge 择优）→ 画像重建 → 路由更准。

## 二、已交付能力（v3.3）

按四大模块，详表见功能说明书第二章（32 项，含优先级与交付版本）：

1. **模型路由**：两步添加（校验并添加 + 画像数据后补，支持 AI 从定价页抓取）、供应商分组表 + 启用开关、入池评测独立块、α 刻度滑杆与画像矩阵、策略卡（含聚合器模型 / 最多聚合路数）与参数抽屉、接入 API Key（sk-route-\*，产品带 Key 调用即绑定策略）、测试抽屉三组选择 + 决策 CoT 三段式、场景数据集四列表 + 行内追加 + 自定义场景、导入独立页（JSONL/CSV/文本，异步校验）、回流校验独立页（场景筛选 / 批量确认移除 / 切标签）、Judge 独立接入、评测回填 + 轮次、聚合效果四卡
2. **智能交互**：15 器 + 呈现类、组件文案并入气泡、五态交互系统、群体回显（限四类）、品牌风格卡管理（弹窗建卡 / 编辑 / 删除 / 定高滚动）、工作台（数字 ID / 异常标签 / 行级局部刷新）、页面式五步编辑器（自由跳转 / 报错跳步 / 有效期到分钟 / 值矩阵）、获取植入代码
3. **数据展示**：交互数据（合并转化六模块 + 日期区间选择器）、路由数据（筛选行 / 聚合效果 / ¥ 口径）、CSV 导出、链路追踪、操作日志
4. **对外接入**：sia.js（Shadow DOM 双向隔离 + 对话/规则/显式三种触发 + sendBeacon 回传 channel=embed）、植入演示页（模拟客户商城）、/v1/embed/envelope、数据契约

## 三、关键技术方案

- **组件协议信封**：schema 版本化；渲染失败 100% 降级纯文本并上报；提交按 render_id 幂等；选项改名走 option_ids 别名链
- **植入 SDK**：一段 snippet（data-key）；Shadow DOM 内注入 shared.css（:root→:host 映射）实现样式双向隔离；规则触发对标 GTM；正式化路线见桌面《交互组件植入技术方案调研》（域名白名单 / HMAC 签名 / WebView 桥）
- **JiSi 路由**：支撑集→粗排→K 路并发→细排→胜出或聚合（阈值 t、聚合器可指定、路数上限）；快车道 / 探索预算 / 配额降级；决策全量记录
- **评测闭环**：公共冷启动集只读 + 租户场景数据集；对话 query 自动收集进待并入区（不污染路由支撑集）人工校验后入库；Judge 择优产出能力标签；线上四路信号分级闸门
- **静态演示**：docs/ 构建脚本做路径改写 + mock 注入；mock 层拦截任意形式 API URL 并合成植入信封；View Transitions + prefetch 保证切页顺滑

## 四、待办与规划

**待确认（功能清单同源）**
- 对外 API 与植入 SDK 鉴权硬化（域名白名单 / 签名防重放，当前演示未启用）
- 正式环境真实模型 API 对接（当前确定性模拟）
- 自定义场景的自动分类（自动收集仅识别内置九类）
- 非 Web 端植入（WebView 桥 / 小程序映射层）排期

**流程性**
- 每个大版本跑「Tricky审查」（skills/23 八关审查）→ 问题清单 → 分批整改 → 回归验证
- 设计系统变更同步 Figma（SIA Foundations/Components）与 shared.css 总注释

**候选增强**
- Figma 组件库升级为真正的 Variant Component
- 场景数据集分页与批量管理（数据量放大后）
- 聚合效果加"择优胜率"维度（聚合 vs 单模型的用户偏好对比）

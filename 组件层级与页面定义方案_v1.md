# 组件层级编辑与页面定义方案 v1.0（2026-09-11 评审会后）

## 一、通用层级模型

所有组件统一按结构分层，每层「主题继承 + 实例覆盖」，每层可恢复默认：

- **L0 容器（面板）**：背景色 / 边框（有无+色）/ 圆角 / 内边距 / 阴影
- **L1 内容区**：各组件的主体区（选项组 / 字段组 / 绘图区…）的排列与密度
- **L2 条目（item）**：可重复单元（选项行 / 字段行 / 表格行…）的高度 / 圆角 / 描边 / 选中态 / hover 态
- **L3 元素**：文案（标题 / 说明 / 占位 / 角标 chip 显隐）、图标、序号标
- **提交区（交互类专属）**：按钮样式（主色 / 圆角 / 尺寸 / 对齐）+ **四态**（默认 / 提交中 / 成功 / 失败红）

## 二、逐组件层级编辑能力（19 类）

### 交互类 9

| 组件 | 容器 L0 | 内容区 L1 | 条目 L2 | 元素 L3 | 提交区 |
|---|---|---|---|---|---|
| 选择表单 | 底色/边框/圆角/内边距/阴影 | 排列：列表/卡片；卡片列数 | 选项行高、圆角、描边宽、选中态样式（填充/描边/勾选位）、hover 底色 | 提示文案字号色；推荐 chip 显隐+形状 | 按钮样式+四态 |
| 信息表单 | 同上 | 字段布局：堆叠/两列 | 输入框圆角/边框/focus 色/高度 | label 位置（上/左）、必填星色、占位文案色 | 按钮+四态 |
| 操作确认 | 同上 | 警示图标显隐/图标底色 | — | 标题字重、描述色、摘要块显隐+底色 | 按钮组排列（右对齐/拉伸）、危险按钮色、次按钮样式 |
| 赞踩反馈 | 形态：卡片/行内 | — | 按钮对形状（pill/方）、图标显隐 | 引导文案显隐 | 选中态色（赞主色/踩危险色） |
| 偏好选择 | 底色/边框/圆角 | 候选卡间距 | 候选卡边框/圆角、选中态（描边/填充）、对勾位 | 标签 chip 样式、引导文案 | —（点选即提交） |
| 数值滑杆 | 同上 | 数值显示位置（右/上） | 轨道色/填充色/滑块尺寸形状 | 单位文案样式 | 按钮+四态 |
| 评分量表 | 同上 | 刻度形态：圆点/条段 | 刻度尺寸/间距/选中色 | 两端标签显隐+字号 | 按钮+四态 |
| 日期时间 | 同上 | 快捷片显隐（今天/明天/后天） | 快捷 chip 样式、输入框样式 | 引导文案 | 按钮+四态 |
| 优先级排序 | 同上 | 行距 | 行高/圆角、拖把手显隐 | 序号标形状（圆/方）+色、引导文案 | 按钮+四态 |

### 展示类 10（无提交区）

| 组件 | 容器 L0 | 内容区 L1 | 条目 L2 | 元素 L3 |
|---|---|---|---|---|
| 表格 | 外框有无/圆角 | 密度（行高档） | 隔行变色开关、分隔线样式（横线/无/全框） | 表头加重显隐、表头底色、数字列对齐 |
| 趋势图 | 底色/边框 | 网格线显隐+密度 | 线宽、面积渐变开关、数据点显隐 | 轴刻度字号色、图例显隐+位置、标题 |
| 对比图 | 同上 | 同上 | 柱宽档、柱圆角、值标签显隐 | 同上 |
| 占比图 | 同上 | 形态：百分比条/环形 | 条高/圆角 | 图例色点形状、百分比显隐 |
| 指标卡 | 同上 | — | — | 主数值字号档、色跟随（主题/语气）、涨跌 chip 显隐样式、基线显隐 |
| 时间线 | 同上 | 连线样式（实/虚）粗细 | 节点形状（圆/方）、当前态强调色 | 时间戳显隐位置、描述显隐 |
| 步骤条 | 同上 | 连线粗细 | 节点尺寸、完成色、当前光晕开关 | 标签位置（下/右）+字号 |
| 方案对比 | 同上 | — | 得分单元数字样式、最优高亮开关+色 | 表头样式、综合列显隐 |
| 要点清单 | 同上 | 行距 | — | 序号标形态（数字圆/点/无）+色、标题显隐 |
| 重点结论 | 底色跟随语气开关 | — | — | 主文本字号档、语气色映射、补充说明显隐 |

### 内容模式（组件属性，评审会核心决策）

- **动态**（默认）：内容由模型调用参数给出；回写（群体回显）**置灰**
- **固定**：内容在平台配死（选项 / 字段 / 刻度标签…业务定义）；可开**回写**，开启时预览回显样式；注册表按模式输出——固定内容从 params_schema 移除（模型不可覆盖）

## 三、页面定义

| 页面 | 一句话定位 | 承载内容 | 核心交互 |
|---|---|---|---|
| 首页 | 产品价值与上手动线 | 能力示例轮播 + 三步入口 | 三步卡直达对应页 |
| 导航 | 全局框架：上下文 + 功能入口 | 顶部产品切换（含 + 新建）→ 产品维度组（工作台/组件模板/风格主题）→ 底部全局区（产品与接入/操作日志） | 切产品即切全站上下文（主题跟随） |
| 组件模板（原组件库） | 官方标准模板市场 | 模板卡（交互/展示 tab，展示内再分：图表/表格/流程/文本）+ 模板详情（schema/样式能力） | 浏览→预览→从模板创建 |
| 组件工作台 | 当前产品的业务组件管理 | 实例列表（状态/内容模式/样式摘要/批量操作）+ 编辑器（选模板→内容模式与内容→分层样式→发布） | 发布自动入当前产品注册表 |
| 风格主题 | 当前产品观感资产 | 主题库 + 编辑器（快速配置 3-4 色 / 高级逐 token 带作用说明 / 明暗 / 组件预览） | 点选预览、整卡替换式编辑 |
| 产品与接入 | 全局产品管理 + 接入交付 | 产品列表（出包状态）+ 配置（两件套：后端 API / 前端 JS 自动注册 + 数据 Schema） | 复制两件、查看 Schema、复制产品 |
| 操作日志 | 全局审计 | 时间/操作人/动作/详情 + 过滤 | 筛选、导出 |

## 四、发散思考（会议之外需要想清的 12 件事）

1. **实例的产品隔离（现有数据模型隐患）**：当前种子实例被 4 个产品共享，改一处全产品同变。固定组件携带业务内容后必须**单产品归属**——跨产品用「复制」而非共享。需要数据迁移方案。
2. **固定组件的数据闭环**：回写开启后群体分布在哪看（数据看板依赖固定组件先落地）；回显本身是组件的一个展示态，需设计分布条样式。
3. **注册表按模式输出**：固定组件的 params_schema 收窄（业务内容只读），模型调用传了也不生效——契约层要讲清楚。
4. **模板版本与实例升级**：官方模板迭代后已建实例是否跟随（模板版本化 / 实例锁版本 / 升级提示）。
5. **继承链可视化**：分层编辑后「这个值来自哪」（主题/实例/默认）要可见，配一键回收，否则覆盖面爆炸不可维护。
6. **动态组件护栏**：动态虽自由，可给实例级约束（最大选项数 / 文案长度上限），防模型放飞。
7. **固定内容变更的发布流程**：业务内容改动比样式改动重——复用草稿机制，发布前 diff 确认。
8. **移动端 H5 表现**：仅 Web 端但接入方有 H5 场景——触控目标（选项行高≥44）、滑杆手势的响应式定义。
9. **按钮文案国际化**：「提交/确认」现固定中文——文案作为实例 token（接入方英文站需求）。
10. **样式 token 裁剪**：分层后 CSS 变量数量增长，SDK 出包按实际覆盖裁剪，控制包体。
11. **图表依赖策略**：复杂图表如走 ECharts，依赖接入方自装并写清文档；平台自绘图保底。
12. **JS 缓存与更新**：sia.js 响应加 no-cache 或版本戳；配置变更后「线上包落后」提示已具备，补前端拉取侧的失效机制。

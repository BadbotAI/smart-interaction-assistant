# 项目记忆 · 智能交互平台（AI 交接文档）

> 目的：任何 AI/工程师读完本文档即可接手开发。姊妹产品「模型路由平台」的文档在 `~/Desktop/smart-model-router/项目记忆_模型路由平台.md`（源码共用本仓库）。
> 最后更新：2026-09-09（对应功能说明书 v7.0；模型路由平台已切 v7 智能路由方案，详见其记忆文档）

## 一、产品定位

面向 B 端的 AI 应用管理平台：管理员把「交互组件」配置进 AI 对话——什么条件下触发、出什么组件、收回来的数据去哪。对话从「用户自由输入—模型自由回复」变为「模型主动引导—用户结构化响应」。demo 客户背景：供应链中试基地的智能客服/助手场景。

一句话架构：FastAPI + SQLite（`server/`）+ 无框架原生 JS（`web/`）+ 静态演示站（`docs/`，GitHub Pages）。

## 二、仓库与发布

- 源码仓：本目录，GitHub `BadbotAI/smart-interaction-assistant`（gh 已登录 BadbotAI）
- 线上演示：https://badbotai.github.io/smart-interaction-assistant/ ← `docs/` 目录
- **双站构建**：`python3 build_docs.py`（改了 API 数据需先起本地服务再加 `--snapshot` 刷快照）会同时构建本站（docs/）与模型路由站（`~/Desktop/smart-model-router/`）。两仓都要 commit+push。CDN 生效约 10 分钟
- 构建做的事：`/web/` 路径改写为 `./`、注入 `mock_data.js`（API 快照）+ `mock_api.js`（请求拦截模拟）+ `window.SIA_PLATFORM` 平台标记、资源内容哈希 `?v=`（防 CDN 新旧混跑）、versioned Service Worker（哈希资源 cache-first / HTML network-first）、`<body class="has-side">` 预置（防侧栏注入闪动）、跨站链接改写
- git 提交身份：`-c user.name="BadbotAI" -c user.email="badbotbusiness@gmail.com"`；commit 末尾带 Co-Authored-By + Claude-Session 行（见最近 git log 格式）

## 三、启动与环境

```bash
cd ~/Desktop/富交互项目
python3 -m uvicorn server.app:app --port 8787 > /tmp/platform_server.log 2>&1 &
# 改 server/*.py 后必须重启：pkill -f "uvicorn server.app" 再起
# 本机 curl 需 --noproxy '*'
# Bash 工作目录会重置：每条命令先 cd
```
首次启动自动建库（`server/platform.db`）+ 种子数据（`server/seed.py` 的 run_all，含增量迁移，幂等）。

## 四、代码约定（重要）

- **无框架**：`web/ui.js` 是全站组件库。`UI.el(tag, attrs, children)` 建 DOM（onXxx 直接挂事件，字符串自动 textNode——天然防注入，**不要用 innerHTML 塞用户数据**）
- 常用件：`UI.api(path,{method,body})`（自动 JSON，错误抛 `{error}`）、`UI.modal/drawer/confirm/toast/menu/withBusy/loading`、`UI.fancySelect`（自绘下拉）、`UI.tagSelect`（标签多选）、`UI.toggle`、`UI.help(text)`（info 图标 + **自绘即时 tooltip**，原生 title 被禁用因为出现太慢）、`UI.icon(name,size)`
- 导航：`UI.nav(activeKey)`，`PLATFORMS` 常量定义两平台的分组导航（ia/router），二级项按 `#hash` 高亮；共享页（audit）按 `window.SIA_PLATFORM` 推断归属
- 页面 = 单 html 文件 + hash 路由（`showSection`），`RENDER` 表映射 hash→渲染函数
- CSS 全在 `web/shared.css`，设计 token 用 CSS 变量（`--primary` #1DA1F2 系、`--font-*`、`--radius-*`）；chip 色类 blue/green/yellow/red/gray/ai
- **表格规范**：`table.data`，th 左对齐 nowrap，`.w-main` 列吃 99% 宽，其余列 nowrap
- 品牌 token 系统：`web/tokens.js` 把品牌 JSON 映射为 `--brand-*` CSS 变量，只作用于 `.brand-scope`（对话组件），平台自身图表恒用主题蓝

## 五、页面清单（本平台）

| 文件 | 页面 | 要点 |
|---|---|---|
| `index.html` | 首页 | 大轮播卡（5 个组件 case 动画）+ 单 CTA；跨站文字链到路由平台 |
| `cards.html` | 组件工作台 | 产品封面（产品切换 icon 菜单/产品 ID 复制/品牌风格下拉/MCP 配置抽屉）+ 按产品聚焦的配置列表；`?focus=` 深链定位；`?edit=` 进编辑器 |
| `library.html` | 组件库 | 15 器分类陈列 + 示例预览 + 一键创建 |
| `products.html` + `design.html` | 产品及风格管理 | 双 tab（同一 nav key "products"）。产品=1 风格+N 组件+1 个 MCP 接入点（`sk-mcp-` key）；design.html 是页面内风格编辑器：六套配色方案/深浅模式/圆角五档刻度滑杆/字体字重字号 + 代码导入，`?new=1` 深链，`window._editingStyle` 防 Brand.init 覆盖预览（竞态修复，勿动） |
| `playground.html#comp` | 智能交互测试 | TestChat（`testchat.js`）页面模式；不暴露任何路由信息（平台边界） |
| `dashboard.html#survey` | 交互数据 | 漏斗/分组件转化/触发分布/埋点健康；日期区间选择器 |
| `audit.html` | 操作日志 | ACTION_NAMES 全中文映射，可导出 CSV |
| `embed-demo.html` + `sia.js` | 嵌入 SDK 演示 | Shadow DOM 沙箱渲染组件，`/v1/embed/envelope/{card_id}` |

组件渲染引擎：`web/components.js`——标准信封（envelope：component_type/params/card_ref/render_id）→ 渲染 15 类组件，失败降级纯文本，render_id 幂等去重。

## 六、数据模型（相关表）

`server/db.py` 建表：`cards`（组件配置，lock_version 乐观锁）、`card_snapshots/card_refs`、`products`（name/brand_file/card_ids/mcp_key 派生）、`events`（交互事件，channel=test 隔离测试流量）、`labels`、`traces/spans`、`audit_log`、`kv_settings(k,v)`（注意列名是 k/v）。品牌风格存 `docs/brand/*.json` 文件。

## 七、关键 API（本平台侧）

- 组件 CRUD：`/api/cards`（POST/PUT/transition 状态机 publish/offline，服务端守门校验）
- 产品：`/api/products` CRUD（名称 1-15 唯一、card_ids 校验、tokens 100KB 帽、删除品牌重置引用产品）
- 品牌：`/api/brands`（保存/激活/删除）
- 对话：`POST /v1/route`（SSE；组件触发=cards.match_cards 命中→ask_card 信封；card_context 续轮按选项动作走）
- 事件：`POST /v1/events`（幂等、日上限、channel 隔离）；回显 `/v1/cards/{id}/responses`
- 看板：`/api/dashboard/questions|overview|insights`、导出 CSV

## 八、验证流程与红线（每改必做）

1. `python3 -m py_compile server/*.py`；内嵌 JS 用正则抽出后 `node --check`（模式见 git log 里的验证脚本）
2. 无头截图目检：`"Chrome" --headless=new --no-proxy-server --virtual-time-budget=8000 --screenshot=...`；library 页加 `--host-resolver-rules="MAP www.openstreetmap.org 127.0.0.1"` 防 OSM 超时；SSE/慢请求在虚拟时间下不完成，动线验证用 curl
3. DOM 冒烟：dump-dom 后**先剥 `<script>` 再断言**（防脚本源码里的文字误报）
4. 静态站：`node tests/mock_smoke.mjs`（mock 行为单测）必须全过
5. **红线**：破坏性验证只允许用临时对象，用完即清（历史上两次误删演示数据的教训）；python 批量改文件所有 assert 必须在写盘之前；**切块删除代码后必须做全端点运行时扫描**（编译不报的 NameError 教训）
6. 每批收尾：功能说明书加轮次章节 + 桌面 docx 同步（python-docx 真表格转换脚本见 git log）+ build_docs + 两仓 push + Tricky审查报告追加

## 九、文案与设计规范

- **emoji 与装饰性 unicode 全站禁用**（用户强规）；允许 × 作关闭
- 术语人话化：说明文字一句话讲清，细节收进 `UI.help` tooltip；禁止口语（"攒满/挑一份/池子"）与拼贴标题（"XX · XX 中"）
- 中文引号「」；货币 RMB ¥；6 位数字短 ID（ID:XXXXXX 点击复制完整）
- 确认框三要素：发生什么、影响什么、怎么办；破坏性操作 danger 红
- 按钮对齐：区块标题行右侧按钮与卡片内按钮右缘对齐（补 padding-right:16px）

## 十、历史与审查

- 功能说明书：`功能说明书.md`（v2.x→v7.0 全部轮次章节）；审查报告：`Tricky审查_v3.3_20260902.md`（14 批，含 P2 记账清单）
- Tricky 审查方法：`~/Desktop/机器坏人程序/skills/23_Tricky审查Skills.md`（八关流程，触发词「Tricky审查」）
- 用户工作风格：连珠炮 mid-turn 反馈、每改必截图验证、批判性 PM/设计师视角主动挑刺直接修
